# `hackerfive agent` — LLM-Orchestrated Scan Loop + Sandboxed Script Tool

> Part of the [HackerFive documentation set](../README.md).

**Status:** M1-M5 implemented and the M5 gate has now actually been evaluated, live, against all four lab targets with a real LLM tier (`deepseek/deepseek-v4.1-flash`) — **the result does not clear the gate.** The three-way FP/FN/cost/wall-clock table (2026-09-15, see the M5 section below and [follow-up.md](follow-up.md) LT-162) shows the deterministic baseline finding 10/22 expected fixture prefixes at $0 cost in ~4 minutes total, versus MCP-plan mode finding 7/22 for $0.0081 in ~20 minutes and this feature's own orchestrator mode finding 6/22 for $0.0171 in ~18 minutes. **Neither LLM-driven mode found anything the deterministic baseline missed; both regressed recall relative to it.** Per doc93's own standing rule ("do not point this mode at a real program until [this table] exists and shows a genuine improvement"), `hackerfive agent` **remains lab/CLI-only and must not be pointed at a real program** until a future change (better prompting, a stronger/different model, more turns, fixing the doc93 M2 gaps LT-160 already named) actually demonstrates an improvement and this table is re-run. Branch: `feat/hackerfive-agent-orchestrator`.

**Last Updated:** 2026-09-15 — M5's gate evaluated for real: with a live `OPENROUTER_API_KEY` configured, ran all three `tests/eval/` harnesses (`TestEvalHarness`, `TestAgentEvalHarness`, `TestOrchestratorEvalHarness`) back to back against all four lab targets (DVWA/Juice Shop/vAPI/crAPI, freshly re-authenticated: DVWA session cookie, crAPI owner/other tokens via `crapi_setup.sh`, vAPI owner/other tokens) and produced the three-way FP/FN/cost/wall-clock table this milestone exists to produce. **Verdict: gate not met** — see the Status line above and the M5 section below for the full table and analysis. Earlier the same day, M5's driver itself was built: `tests/eval/orchestrator_run.go` (`OrchestratorScenario`, mirroring `AgentScenario`'s four lab targets) and `tests/eval/orchestrator_eval_test.go` (`TestOrchestratorEvalHarness`, `-tags eval`), invoking the real `hackerfive agent` CLI as a subprocess (`exec.Command`, matching `TestEvalHarness`'s own `binPath` convention) rather than an in-process call, since orchestrator mode is CLI/Web-UI-only. M4 built: the Web UI's "Use LLM agent" launch-form checkbox, `pkg/webui.runLaunchAgentJob`, and the script.explore human-approval flow (`Job.RequestScriptApproval`/`ResolveScriptApproval`, a genuinely new blocking-on-HTTP primitive — no analog existed anywhere in `pkg/webui` before this). See the M4 section below for the flag/checkbox surface, the approval-flow design, and `pkg/orchestrator.Config.ReconTimeout` — a real bug (an unbounded recon call) found live during M4's own smoke test and fixed at the shared `pkg/orchestrator` layer, so both the CLI and Web UI paths get the fix for free. M3 built 2026-09-14: `cmd/hackerfive/agent.go`, the CLI entrypoint. See the M3 section below for the flag surface and the stdin y/N `ApprovalGate` wiring. M2 built earlier the same day: `pkg/orchestrator`, the loop itself — see the M2 section below for two deliberate scope-narrowings against the original sketch ([follow-up.md](follow-up.md) LT-160). M1 built before that: `pkg/agenttask` `Actor`/`BeginActor`, `pkg/llmfallback/orchestrate.go`'s `NextAction`, and the full `pkg/scriptexec` sandboxed script tool (precheck, egress proxy, Docker sandbox orchestration) — see the M1 section below, and LT-159 for one real packaging gap found along the way.

## Context

[92-research-llm-orchestrator.md](92-research-llm-orchestrator.md) reopened [90-research-hackerbot.md](90-research-hackerbot.md) Decision 2 ("no shell/exec tool, anywhere"), scoped narrowly: after real engineering investment across `pkg/detectors`/`pkg/registry`/the ~9.6k-template corpus, v1's fully-deterministic dispatch still hasn't produced a live, program-actionable finding, and the specific gap is exploration a fixed matcher/template genuinely cannot do (JWT algorithm-confusion, non-numeric IDOR ID-space decoding, custom request-signing reverse-engineering, GraphQL introspection-driven query generation, business-logic state-sequencing, timing side-channels — doc92 §5b's full list). The user confirmed the concrete decisions this plan builds: command name `hackerfive agent`; a Web UI "Use LLM agent" checkbox, default unchecked; reuse `pkg/llmfallback`'s existing model client as-is; a human approves the exact script text before every execution, no batch pre-approval; validate against lab targets (crAPI/DVWA/Juice Shop/vAPI) for FP/FN before this mode ever points at a real program. `docs/90/14/15/91-*.md` already carry the scoped cross-reference note — this doc is the implementation plan doc92 §7 called for.

**This is not a replacement for v1.** `hackerfive scan`/`plan`/`triage`/`suggest` and the MCP server's tool surface are unchanged. `pkg/mcpserver` still exposes nothing shell/exec-shaped — the new script tool lives only inside this new, separately-named, opt-in mode.

**Non-negotiable properties carried over from doc92 §5/§5b** (verified against the actual codebase this session, not assumed): the PoC-required rule, a per-run spend/iteration ceiling that defaults safe, no bypass of `--allow-writes` for mutating actions, and a script-execution gate that pairs HITL approval with an independent static pre-check and a sandboxed runtime — because HITL-approving raw code text alone is a materially weaker gate than approving a structured plan (most reviewers can't reliably eyeball a script for a hidden `subprocess.Popen` or an unscoped host).

## Reused infrastructure (confirmed via codebase research this session — no reinvention)

- **CLI conventions**: `cmd/hackerfive/plan.go`/`triage.go`/`suggest.go` — `newXCmd(root *rootFlags) *cobra.Command`, `llmfallback.New(llmfallback.WithLogCallback(...))` construction idiom, `"<cmd>: ... spent $%.4f\n"` stderr cost idiom, `cmd.OutOrStdout()`/`root.output` for JSON output. `scan.go`'s `--allow-writes`/`--auto-provision-account` pattern (independently-scoped, default `false`, warn-and-skip when absent) is the template for the new `--allow-agent-scripts` flag.
- **`pkg/agenttask.PlanTree`**: `ApplyLeafUpdate(nodeID, patch)` is the *only* legal mutation surface (leaf-only, rejects shape changes) — the orchestrator loop never touches the tree any other way. `RecordLeafAttempt`/`ShouldEscalate` (existing `MaxLeafResolveAttempts=3`/`MaxLeafResolveSpendUSD=0.05` per-leaf grind caps) and `SpendCeilingUSD`/`AddSpend`/`SpendSoFar` (tree-wide budget) are reused as-is.
- **`pkg/llmfallback.Client`**: `New()`/`ErrNoTierAvailable`/`completeBestAvailableLabeled`, the two independent spend ceilings (global process-lifetime `HACKERFIVE_SPEND_CEILING_TOTAL_USD`, per-call `HACKERFIVE_SPEND_CEILING_USD`), and the degrade-never-fabricate pattern (`decodeJSONResponse` + allow-list validation, e.g. `ResolveLeaf`'s `EscalateToHuman` default, `Suggest`'s `validSuggestedActions` drop) are the model directly for the new `NextAction` call — no new client, no new tier.
- **`pkg/webui.Job`**: `AppendFinding`/`AppendLog`/`BeginAgentActivity(tool, reason, params) func(resultSummary, err)`/`Ctx()`/`Cancel()` and the `EventAgent` SSE stream already exist and already do exactly what an orchestrator turn needs to report — reused unmodified. `LaunchFormData.AllowWrites`'s checkbox convention (`r.PostFormValue("x") == "on"`, implicit-false, excluded from LT-122's `localStorage` prefill `FIELDS` array) is the template for the new checkbox.
- **`tests/eval/agent_run.go`**: `AgentScenario{Name, ExpectedFile, Target, ...}` + `tests/fixtures/expected-findings/*.json` (`ExpectedIDPrefixes`) is the exact fixture set and comparison methodology (`matchesAnyPrefix`) to reuse for a three-way FP/FN comparison (deterministic `TestEvalHarness` vs. existing MCP-plan `TestAgentEvalHarness` vs. this plan's new orchestrator mode).
- **`pkg/scanner/scope.Scope`** (`Parse`/`New`/`Allowed(target string) bool`) is reused by the egress proxy below — confirmed no existing HTTP-client-layer scope check exists (`httpclient.Client` has none), so the proxy is the first thing to add one at that layer.

## Gap found and closed by this plan

`pkg/agenttask.SessionLogEntry` (shared by `pkg/mcpserver` and `pkg/webui`) has **no field distinguishing an agent-decided turn from a human-requested one** — both `Job.BeginAgentActivity` and MCP's `sessionLog.Begin` write the same shape. Fix: add `Actor string` (json `actor`) to `SessionLogEntry`; add `SessionLog.BeginActor(actor, tool, reason string, params any) func(resultSummary string, err error)`; make the existing `Begin` a thin wrapper calling `BeginActor("human", ...)` so every current call site's meaning is unchanged. The orchestrator loop calls `BeginActor("agent", ...)` for every turn — this is what makes the audit trail doc90's C1/Definition-of-Done actually distinguishable, not just present.

## Milestones

### M1 — Foundations (no loop yet; the safety-critical primitives everything else depends on) — ✅ done 2026-09-14

Built as sketched below, with three real deviations found during implementation:
- **`mvdan.cc/sh/v3` adopted for the shell precheck**, after the CLAUDE.md-required footprint check: importing only its `syntax` subpackage (not `interp`/`expand`) compiles in just `mvdan.cc/sh/v3/{syntax,fileutil}` — confirmed via `go list -deps` in a scratch module — despite `go.sum` listing several test-only transitive modules (`creack/pty`, `golang.org/x/tools`, ...) that never actually compile into the binary. A real AST walk, not a regex fallback.
- **The egress proxy runs as its own sidecar container**, not "on the shared bridge" as first sketched — the only design that is portable across Docker Desktop (macOS/Windows) and native Linux Docker. Per run: two `docker network create` networks (`--internal`, shared with the script container; a normal one, giving the sidecar real egress), one sidecar container (`pkg/scriptexec/egressproxy/cmd/hf-egressproxy`, a ~15-line binary wrapping `egressproxy.Proxy`) attached to both, bind-mounted in as its entrypoint plus a mounted scope file (`scope.Entries()` — a new small accessor added to `pkg/scanner/scope` for exactly this — reserialized and read back via the existing `scope.Parse`). The script container attaches ONLY to the internal network; its `HTTP_PROXY`/`HTTPS_PROXY` point at the sidecar's address on that network.
- **The sidecar binary is cross-compiled on demand** (`sandbox.go`'s `ensureEgressProxyBinary`, cached per Docker-server-arch under `os.UserCacheDir()`), which needs a Go toolchain on the machine running `hackerfive agent` — true on every dev machine used so far, but not guaranteed for a distributed release build. Logged as [follow-up.md](follow-up.md) LT-159 (embed a prebuilt linux/amd64+arm64 binary at release-build time instead) rather than silently shipping a feature that only works for developers.

Live-verified end to end against a real Docker daemon (`pkg/scriptexec/sandbox_integration_test.go`, `-run TestExecute_`): a benign Python script reaches its one in-scope target through the full two-network sidecar topology and gets the real response back; a script targeting a host outside scope is denied at the egress proxy (not just by the static precheck); a precheck-blocked script never starts a container and never calls `ApprovalGate`. Also caught live: `host.docker.internal` resolves to an IPv6 ULA address (`fc00::/7`) on this Docker install, not an IPv4 RFC1918 one — exactly the "DNS-rebinding-shaped" address class `isSpecialUseIP` is designed to catch, confirming that defense fires correctly (a real lab-target scope file needs an explicit CIDR entry for whatever private range its target resolves to, same as any other private target).

Original per-file breakdown (all done):
- `pkg/agenttask`: `Actor`/`BeginActor` per the gap above (`pkg/agenttask/sessionlog.go`).
- `pkg/llmfallback/orchestrate.go` (new file, same placement discipline as `leaf.go`/`suggest.go`/`triage.go`): `func (c *Client) NextAction(ctx, tree *agenttask.PlanTree, catalog []ToolSpec, history []TurnRecord) (Action, float64, error)`. `Action{Kind string, NodeID string, Params json.RawMessage, Rationale string}` — `Kind` validated against a fixed allow-list (`recon.refresh`, `registry.lookup`, `scan.leaf`, `triage.rank`, `script.explore`, `stop`); an unrecognized `Kind` or malformed response degrades to `Action{Kind: "stop"}` with the reason logged, never fabricated or retried silently — same contract as `ResolveLeaf`/`Suggest`.
- `pkg/scriptexec/` (new package) — the sandboxed script tool, per the design below.
- New `orchestrator.Config` fields (see M2): `Budget float64`, `MaxIterations int`, `AllowAgentScripts bool`.

**`pkg/scriptexec` design:**
- **Sandbox: Docker, not bare `os/exec`.** This codebase has zero existing Go code shelling out to Docker (only documentation tells a human to run `docker compose` for lab targets), but the daemon is already a confirmed dependency in both dev paths (WSL2 + Docker Desktop, macOS + Docker) since it's required for the lab targets anyway — so this is new *usage*, not a new *environment* dependency. A bare `os/exec` sandbox gives no namespace/network isolation at all, which would leave "no exploit, no damage" resting entirely on the static pre-check — the single-weak-gate problem this design explicitly avoids. Container: `--read-only` rootfs, a size-capped `tmpfs` scratch dir, non-root `--user`, `--cap-drop=ALL`, `--pids-limit`, `--memory`/`--cpus` limits, hard wall-clock kill via `context.WithTimeout` around `docker run` (mirroring `pkg/recon/exec.go`'s existing `errWaveTimeout` pattern — same "kill on deadline, report partial/blocked" shape, not a new idiom).
- **Egress control: a first-party allowlisting forward proxy** (`pkg/scriptexec/egressproxy`, new), reusing `scope.Scope.Allowed` on every request/CONNECT. The container's only network route is to this proxy (`docker network create --internal`, proxy on the shared bridge); the script's runtime gets `HTTP_PROXY`/`HTTPS_PROXY` env vars — no other network configuration needed for Python `requests`/shell `curl`, which honor these by default. The proxy additionally rejects any resolved IP that's loopback/link-local/RFC1918-unless-explicitly-in-scope (closing a DNS-rebinding gap `scope.Allowed`'s hostname-string-match doesn't cover on its own — a rebound in-scope hostname pointing at `169.254.169.254` needs this second check). The proxy wraps its own outbound calls through `httpclient.New` with the same rate-limit/retry middleware, so a script's traffic doesn't get an unbounded side channel outside the rest of the scan's throttling.
- **Static pre-check (second gate, runs before a human ever sees the script — reject-on-match never reaches the reviewer):** a real AST walk, not a token deny-list (a deny-list on raw text is trivially defeated by string concatenation/`getattr`/`__import__`). Python: shell out to a small, fixed, first-party, reviewed `python3 -c` checker using stdlib `ast` (an explicit exception since Go has no Python AST library — the checker itself is trusted, fixed code, never the untrusted script). Shell: an AST-based parse — **check the real transitive footprint of `mvdan.cc/sh` before adopting it** (CLAUDE.md's dependency-footprint rule); if disproportionate, fall back to a stricter regex/token check for shell specifically while keeping Python's real AST check. Blocks: further subprocess spawning (`subprocess`/`os.system`/`os.popen`/`os.exec*`/`pty`/`multiprocessing`), raw `socket`/`ssl` use (env-var proxying can't route a raw socket, so this must be blocked outright, not just left to the network topology), filesystem access outside the declared scratch dir, and any bare hostname/IP literal in the source that isn't the run's own target (defense-in-depth beyond the proxy).
- **API** (`pkg/scriptexec/scriptexec.go`):
  ```go
  type Language string // LangPython, LangShell
  type ScriptRequest struct {
      Language     Language
      Source       string
      Scope        *scope.Scope
      Timeout      time.Duration
      ApprovalGate func(ctx context.Context, req ScriptRequest, pre PrecheckResult) (approved bool, err error)
  }
  type PrecheckResult struct { Blocked bool; Reasons []string }
  type ScriptResult struct { Stdout, Stderr string; ExitCode int; Truncated bool }
  func Precheck(req ScriptRequest) (PrecheckResult, error)
  func Execute(ctx context.Context, req ScriptRequest) (ScriptResult, error) // Precheck -> ApprovalGate -> container run, in that order
  ```
- **New flag: `--allow-agent-scripts`** (bool, default `false`), same independently-scoped convention as `--allow-writes`/`--auto-provision-account` — never folded into either. Absent: the orchestrator treats a proposed `script.explore` action as `StatusUnresolved` (same semantics as a registry miss) with a stderr warning, never a hard-fail of the whole run.
- **Attack-scenario check** (verify in tests): metadata-IP curl → stopped by the egress proxy's IP-class check; write outside scratch → Docker read-only rootfs + pre-check; reverse shell/`subprocess` → pre-check import/call block + `--cap-drop=ALL`; infinite CPU loop → `--cpus`/`--pids-limit` + hard timeout, independent of the pre-check.
- **Evidence rule** (closes the "self-reported confidence" flaw named in doc92 §2's Cyber-AutoAgent finding, and ties in [follow-up.md](follow-up.md) LT-157's evidence-gating item): a script's own stdout/exit code is never sufficient by itself to become a `Finding`. When a script's output demonstrates a structural condition (e.g., a forged JWT that got a 200 with sensitive data), the orchestrator must independently re-issue the discovered request via the normal `pkg/scanner/httpclient` path (read-only, already in scope) to confirm the response outside the sandbox before packaging it as `Finding.Evidence` — the confirmation request/response pair, not the script's self-report, is what ships.

### M2 — `pkg/orchestrator` (new package): the loop itself — ✅ done 2026-09-14

Built as sketched, with two real, deliberate narrowings against the original design (both fail-closed/safe, tracked as [follow-up.md](follow-up.md) LT-160, not blocking M2 itself but relevant before M5's FP/FN table is taken as final):

- **`recon.refresh` doesn't merge its result back into the running `PlanTree`.** `agenttask.PlanTree.ApplyLeafUpdate` is deliberately leaf-mutation-only (doc90 §2's shape-change defense) — there is no post-construction "add a leaf" API outside `registry.Resolve`'s own initial build, and adding one was out of scope for this milestone. `recon.refresh` runs a real, fresh `pkg/recon` probe against the targeted node's host (or the run's own seed target) and folds a fact-count summary ("N endpoint(s), M tech fact(s)") into the turn history for the model to read next turn — real signal, but it can't turn into a new dispatchable leaf mid-run.
- **`script.explore`'s independent-evidence-reconfirmation pipeline isn't built.** M1's design (and LT-157) call for re-issuing a script-discovered request through `pkg/scanner/httpclient` before ever packaging it as `Finding.Evidence`. That pipeline is inherently vulnerability-class-specific (a JWT-forgery reconfirmation looks nothing like a custom-signing-scheme one) and wasn't attempted here — `script.explore`'s dispatch runs the sandboxed script and folds its stdout/stderr/exit code into turn history exactly like `recon.refresh` does, but **no code path in this package ever turns a script's output into a `Finding`**, so the "never fabricate" invariant holds by construction (nothing ships) rather than by an evidence-check that was built and verified.

Everything else matches the sketch: `Config` (renamed/narrowed from the sketch's `Targets []string`/`Scope *scope.Scope` to a single `Target string` plus a full `scanner.Config` `BaseScanConfig` — `pkg/recon.Recon.Run`/`registry.Resolve`/`pkg/planexec.RunPlan` all already take one target and one `scanner.Config`, so this avoids a multi-target fan-out `pkg/orchestrator` would otherwise have to invent on top of single-target primitives every other tool in this loop actually has); `Recon`/`Client` are narrow interfaces (`ReconRunner`/`LLMClient`) over `*recon.Recon`/`*llmfallback.Client` so unit tests use fakes instead of a real network/model tier. Builds the initial `PlanTree` via `recon.New(...).Run(...)` + `registry.Resolve(...)` — the same two calls `plan.go` makes, not `planfromrecon.go`'s LLM-proposal pass (that's optional widening on top of the real tree-builder, not the tree-builder itself — corrected from the sketch's file reference after reading the actual code). Fixed catalog exactly as listed (`script.explore` always offered so the model's view of what's possible is stable across runs; a chosen-but-disallowed `script.explore` degrades its targeted leaf to `StatusUnresolved` with the reason, never a hard-fail — matching M1's `--allow-agent-scripts`-absent behavior exactly). Loop matches the sketch's stop conditions (budget, iteration cap, no actionable leaves left) and calls `sessionLog.BeginActor("agent", ...)` every dispatched turn (a "stop" action is not counted as a turn/doesn't hit the log). `scan.leaf` reuses `pkg/planexec.RunPlan` unmodified (via `ExecOptions.Excluded` naming every leaf but the targeted one) rather than reimplementing single-leaf dispatch — `RunPlan`'s own per-leaf `ApplyLeafUpdate(StatusDone)` call is what marks the leaf done, so `pkg/orchestrator` never touches `Status` for a `scan.leaf` turn itself. `Budget`/`MaxIterations` default to `DefaultBudgetUSD = $1.00`/`DefaultMaxIterations = 20` when left unset (`<= 0`) — the "not zero, not unbounded" default the sketch called for without pinning a number.

Unit-tested in `pkg/orchestrator/orchestrator_test.go`: loop stops immediately on a `stop` action and never logs it as a turn; stops exactly at `MaxIterations` against a client that never stops; stops once cumulative spend crosses `Budget`; a disallowed `script.explore` degrades its leaf to `StatusUnresolved` (with the reason) and the run continues; `dispatch`'s per-kind unit tests (`registry.lookup`'s query validation, `scan.leaf`'s node-not-found/no-detector-yet errors, `triage.rank`'s no-findings short-circuit that skips the LLM call entirely, `script.explore`'s malformed-params/unsupported-language/empty-source rejections happening before the sandbox is ever touched); one `scan.leaf` test runs the real `pkg/planexec.RunPlan` → `scanner.Engine` → `misconfig` detector path against a real `httptest.Server` end to end (no Docker/network dependency, ~8s — the `misconfig` detector's own fixed probe set, not this package's overhead) to prove the reused dispatch path actually produces findings and marks the leaf done, not just that it compiles.

### M3 — `cmd/hackerfive/agent.go` — ✅ done 2026-09-14

Built as sketched, one real deviation from the milestone's own description worth flagging: **the agent loop has no non-LLM degrade path at all**, unlike `plan --llm-assist` (which falls back to escalating every unresolved leaf when no tier is configured). `pkg/orchestrator.Run` hard-requires a `Client`, so `agent.go` calls `llmfallback.New` up front and returns a hard error immediately — before spending any recon time — if `ErrNoTierAvailable`, rather than letting the run start and fail deeper in.

- `newAgentCmd(root *rootFlags) *cobra.Command`, registered in `root.go` alongside the other subcommands. Flags mirror `plan.go`'s recon shape (`--targets`/`-t`, `--recon-depth`, `--scope`/`--allow-no-scope`, `--policy-file`/`--allow-policy-override`, `--verbose`) and `scan.go`'s per-request shape for `scan.leaf` dispatches (`--auth-token`/`--other-auth-token`/`--auth-header-*`, `--header`, `--insecure`, `--templates`/`--template-index`, `--allow-writes`), plus the new `--budget`, `--max-iterations`, `--allow-agent-scripts`, `--script-timeout` (all defaulting to `pkg/orchestrator`'s own `Default*` constants — never zero, never unbounded). A single `--rate-limit`/`--concurrency` pair feeds both the initial recon and every `scan.leaf` dispatch (deliberately not split into recon-specific vs. scan-specific knobs, since `dispatchScanLeaf` only ever runs one leaf/target at a time — the cross-target worker-pool distinction `scan.go`'s own `--concurrency` cares about doesn't apply here).
- **Scope wiring correction found during implementation**: `scan.go`'s own convention (`scanner.Config.ScopeFile` as a bare path string, lazily parsed by `Engine.loadScope` at `Run()` time) turns out to be the *weaker* of two paths `scanner.Config` actually supports — `pkg/planexec.RunPlan`'s own scope-creep gate and `pkg/orchestrator`'s `script.explore` sandbox scoping (`orchestrator.go`'s `BaseScanConfig.Scope`) both read the already-parsed `*scope.Scope` field directly and have no `ScopeFile` fallback of their own. `agent.go` therefore calls `requireScopeOrOptOut` once (the same helper `plan.go` uses for recon) and assigns the resulting `*scope.Scope` straight into `BaseScanConfig.Scope` — a single scope object feeding recon, every scan.leaf dispatch, and the script sandbox identically, rather than `scan.go`'s path-string-only pattern.
- HITL for `script.explore`: `stdinScriptApprovalGate` prints the full proposed script source and any `Precheck` reasons to stderr, then blocks on a stdin y/N prompt (only reachable at all when `--allow-agent-scripts` is set — `scriptexec.Execute`'s own Precheck → ApprovalGate → sandbox ordering means a precheck-blocked script never reaches this prompt) — no batch/blanket approval, a fresh prompt every single time.
- Cost/iteration summary printed via `"agent: spent $%.4f of $%.2f budget, N iteration(s)\n"` (same family as `plan.go`/`triage.go`/`suggest.go`), followed by the full `orchestrator.Result` (tree, findings, history, spend) JSON-encoded to stdout or `--output`, matching `plan.go`'s own output-file handling.
- Verified: `go build`/`vet`/`golangci-lint run`/`test -race` all clean across the full repo; `agent --help` shows the full flag surface; `--targets`-missing and `--scope`-missing both hard-fail with the same messages `plan.go`/`scan.go` produce; a live smoke run against `https://example.com` (`--allow-no-scope --recon-depth passive --budget 0.05 --max-iterations 2 -v`) completed end to end — recon waves ran, the loop correctly found zero actionable leaves and stopped immediately, the cost/iteration summary and JSON output both printed — and `hackerfive plan` against the identical target produced the identical "no live application surface" verdict, confirming `agent.go`'s recon/tree-building path is consistent with `plan.go`'s, not a divergent reimplementation. Not yet run against a target that actually produces a leaf/finding via the CLI (covered by `pkg/orchestrator`'s own `httptest`-backed unit test instead) or with `--allow-agent-scripts` set interactively — both deferred to M5's lab-target validation pass rather than spent here as ad hoc verification.

### M4 — Web UI wiring — ✅ done 2026-09-15

Built close to the sketch, with two real corrections found during implementation and one genuine bug found during live smoke testing:

- **`Job.BeginAgentActivity` can't tag "agent"** — doc93's own M2 write-up already named this gap (`BeginActor("agent", ...)` exists precisely so the orchestrator can distinguish its own turns), but `BeginAgentActivity` only ever calls `agentLog.Begin(...)`, a thin wrapper hard-coded to `"human"`, and `Job.agentLog` was a private field with no accessor. **Fix:** added `Job.AgentLog() *agenttask.SessionLog`, a plain getter — `runLaunchAgentJob` passes it straight into `orchestrator.Config.SessionLog`, which calls `BeginActor("agent", ...)` itself every turn. Same underlying log, same `OnAppend` → `EventAgent` SSE publish path `newJob` already wires up — human and agent entries interleave in the existing Agent section, now visually distinguished by a small `agent` badge (`fragment_agent_entry.html`, `{{if eq .Actor "agent"}}`) so the audit trail is actually distinguishable, not just present, matching the M2 write-up's own goal for this field.
- **No non-LLM degrade path, same as M3's CLI**, for the same reason: `pkg/orchestrator.Run` hard-requires a `Client`. `runLaunchAgentJob` calls `llmfallback.New` before any recon/network activity and fails the whole job immediately (`job.MarkDone(err)`) if no tier is configured — verified both directly (`TestRunLaunchAgentJob_NoLLMConfigured_FailsCleanly`) and through the real `POST /scans` → background-goroutine path (`TestStartLaunch_UseLLMAgent_RoutesToAgentJob`), both deterministic and fast since the failure happens before any network call.
- **No blocking-on-human-input primitive existed anywhere in `pkg/webui`** — confirmed by reading `handlers_plan_exec.go`'s existing Plan-Preview approve/reject flow: it's a plain synchronous branch on a form value with a background goroutine fired off *after* the decision, never a goroutine parked waiting for one. `scriptexec.ApprovalGate` (`func(ctx, req, pre) (bool, error)`) genuinely needs the latter — the orchestrator's own background goroutine calls it and must actually wait. Built as new, minimal machinery on `Job`: a `pendingScriptApproval{view, decision chan bool}` field, `Job.RequestScriptApproval` (implements `scriptexec.ApprovalGate` directly — its method value is passed as `orchestrator.Config.ApprovalGate` with no adapter needed) blocks on `select { case <-decision: ...; case <-ctx.Done(): ... }`, and `Job.ResolveScriptApproval(approved bool) bool` (called by the new `POST /scans/{id}/agent-script` handler) sends the decision. `ctx.Done()` — `job.Ctx()`, the same context `Job.Cancel()` (the kill switch) cancels — unblocks a pending approval too, so a stuck human doesn't leave the run's goroutine parked forever with no way out but the process dying. `Job.Snapshot().PendingScriptApproval` carries the pending state so a page reload mid-approval shows the same card a live SSE client would, not a blank one.
- New `EventScriptApproval = "script-approval"` SSE event, its own dedicated `sse-swap="script-approval"` div (`#script-approval`, matching `#progress`'s plain last-value-wins shape) rather than piggybacking on `EventAgent`'s append-list shape via an OOB trick — simpler and matches this codebase's dominant convention (most events — progress/recon/done — already get their own dedicated div; `EventAgent`/`#plan-preview-link`'s OOB-piggyback pattern is the exception, not the rule). `fragment_script_approval.html` renders the proposed script's language/source/precheck reasons and an approve/reject form; `fragment_catchup.html` resyncs it unconditionally on reconnect, same as `#progress`/`#recon-results`.
- `LaunchFormData.UseLLMAgent`/`AllowLLMAgentScripts bool` / form fields `use_llm_agent`/`allow_llm_agent_scripts`, parsed identically to `AllowWrites`, **excluded from LT-122's `FIELDS` localStorage-prefill array** (same deliberate exclusion as `allow_writes`/`authorized`) — independently scoped from each other, never folded together, same convention as `--allow-writes`/`--auto-provision-account`. Rendered as their own "LLM Agent (experimental)" fieldset on the launch form, after Detectors.
- `runLaunchJob` branches to the new `runLaunchAgentJob` (a plain function, not a `*handlers` method — it never touches `h.tmpl`/`h.store`) when `form.UseLLMAgent` is set, bypassing the checked-detector-tab `cfgs` loop entirely. Reuses `job.ExecConfig()` (already built by `parseLaunchSubmission` — `TemplatePaths`/rate-limit/credentials/`AllowWrites`) as `orchestrator.Config.BaseScanConfig`, parsing `form.ScopeFile` into a real `*scope.Scope` and setting `.Scope` directly (the same M3 CLI fix — `RunPlan`'s B4 gate and the script sandbox read `.Scope`, not `.ScopeFile`) rather than reusing `execCfg.ScopeFile`'s weaker, unparsed convention. A `jobReconRunner` adapter feeds `orchestrator.Run`'s own internal recon result into `Job.SetReconResult`, so the existing Recon Results panel/wave-progress badges populate exactly as they do for a plain scan — there's no separate `runLaunchRecon` call for agent-mode jobs, since `orchestrator.Run` already does recon itself (for both the initial tree seed and any `recon.refresh` turn). On completion, `res.Tree` is cached via `job.SetPlanTree` so the operator can open the existing Plan Preview page afterward to review/re-run leaves the agent left unresolved.
- **Bug found live, fixed at the shared layer**: smoke-testing a real agent-mode run against `https://example.com` through the browser, wave2's `naabu` port scan sat "running" for minutes with the job otherwise healthy — `pkg/orchestrator.Run` had no bound on `Config.Recon.Run` beyond the caller's own `ctx` (`job.Ctx()`/`cmd.Context()`, which only ever expires on `Cancel`/process shutdown), unlike `runLaunchRecon`'s existing `reconRunTimeout` (10 min) for the plain scan flow. **Fix:** added `Config.ReconTimeout time.Duration` (`<= 0` → new `DefaultReconTimeout = 10 * time.Minute`) and a `runRecon` helper wrapping *every* `cfg.Recon.Run` call site (the initial tree-seed and `recon.refresh`) in `context.WithTimeout(ctx, cfg.ReconTimeout)` — fixed once in `pkg/orchestrator`, so both `cmd/hackerfive/agent.go` (which never set this field, and needed no change) and the Web UI path get the bound automatically. Regression tests `TestRun_ReconTimeout_BoundsAHungInitialRecon`/`TestRun_ReconTimeout_BoundsReconRefresh` (`pkg/orchestrator/orchestrator_test.go`) use a `blockingRecon`/`blockingAfterFirstRecon` fake that hangs on `<-ctx.Done()`, proving both call sites are actually bounded rather than just documented as such.
- Verified: `go build`/`vet`/`golangci-lint run`/`test -race` all clean across the full repo. New unit tests: `Job.RequestScriptApproval`/`ResolveScriptApproval` (approve/reject/context-cancellation/no-pending-to-resolve/snapshot-reflects-pending-state, all under `-race`), the `POST /scans/{id}/agent-script` handler (approve/reject/nothing-pending/unknown-job), `parseLaunchSubmission`'s two new checkboxes, the no-LLM-tier failure path (direct call and full HTTP round trip), and `pkg/orchestrator`'s new `ReconTimeout` bound. Live-verified through a real browser session against `hackerfive serve`: the launch form renders both new checkboxes; an agent-mode job with `allow_llm_agent_scripts` unchecked logs the expected skip-warning; a job with a real configured LLM tier actually runs — wave-by-wave recon progress streamed live via the existing SSE mechanism exactly like a plain scan, `#script-approval` rendered correctly empty throughout. Not yet exercised live: a run that actually reaches `scan.leaf`/`script.explore` from the Web UI (the underlying dispatch logic is already covered by `pkg/orchestrator`'s own tests; end-to-end Web UI coverage of the approval-card UI itself is deferred to M5's lab-target validation pass, same posture M3 took for the CLI).

### M5 — Eval harness integration + lab validation (gates any real-target use) — ✅ done 2026-09-15, gate not met

- **`tests/eval/orchestrator_run.go`**: `OrchestratorScenario` mirrors `AgentScenario`'s four lab targets (DVWA/Juice Shop/vAPI/crAPI) and reads the same `tests/fixtures/expected-findings/*.json` fixtures. One deliberate difference from `AgentScenario`: a `Header func() string` field, used for DVWA's `Cookie: ...; security=low` header — `hackerfive agent`'s `--header` flag has no equivalent to the MCP `recon` tool's documented header-less input (`agent_run.go`'s own "Known, named gap" comment), so this driver can actually reach DVWA's login-gated content the way `challenges.go`'s deterministic-baseline `Scenario` does, rather than inheriting that MCP-specific limitation.
- **`tests/eval/orchestrator_eval_test.go`** (`//go:build eval`): `TestOrchestratorEvalHarness` builds a per-scenario scope file (one host line, matching `hostOnly(target)` — reused from `agent_eval_test.go`), shells out to the same `binPath` `TestMain` already builds for `TestEvalHarness` (`go build ./cmd/hackerfive`), and runs `agent -t <target> --recon-depth <depth> --scope <file>` (plus `--auth-token`/`--other-auth-token`/`--header` where a scenario needs them) as a real subprocess — an in-process call wasn't an option since orchestrator mode's only entrypoints are the CLI and Web UI, unlike `TestAgentEvalHarness`'s in-process MCP client session. `--allow-agent-scripts` is deliberately never passed: this harness runs unattended, and an unanswered `stdinScriptApprovalGate` prompt reading from a subprocess's empty stdin would just auto-deny (EOF) rather than hang, but script.explore isn't needed to resolve any of these fixtures' known findings anyway. Decodes `pkg/orchestrator.Result`'s stdout JSON directly (that struct carries no `json` tags; `encoding/json`'s case-insensitive field-name fallback decodes the plain Go field names fine) into a local `orchestratorResult{Findings, Iterations, SpendUSD}`, then grades findings against `expected_id_prefixes` via the same `matchesAnyPrefix`/`anyHasPrefix` helpers `TestEvalHarness`/`TestAgentEvalHarness` already use, and logs the same three metrics (cost, tool-calls via `Iterations`, wall-clock) — reported honestly via `t.Logf`, not gated as a hard pass/fail beyond the per-prefix subtests, same convention as the other two harnesses.
- Skips (not fails) a scenario missing its lab-target env vars, and separately skips (not fails) when no LLM tier is configured (`llmfallback.New()` returns `ErrNoTierAvailable`) — `pkg/orchestrator.Run` hard-requires a `Client`, unlike MCP-plan mode's non-LLM fallback path, so a missing tier here is an environment gap, not a real agent miss.
- **Driver first validated with no LLM tier configured** (earlier the same day): `go build`/`go vet`/`go test -race ./...` clean; `go vet`/`golangci-lint run --build-tags eval ./tests/eval/...` clean; the intended skip path (`no LLM tier configured ... skipping`) fired correctly against a live vAPI target with only `VAPI_BASE_URL` set; a manual smoke test with a syntactically-valid but fake `OPENROUTER_API_KEY` confirmed the CLI invocation shape end to end (real recon completed, `registry.Resolve` built the tree, the run reached its first real `NextAction` call before failing on OpenRouter's `401`).
- **Then, with a real `OPENROUTER_API_KEY` configured (model: `deepseek/deepseek-v4.1-flash`) and all four lab targets re-authenticated live** (DVWA: fresh login + Security:Low session cookie via curl; crAPI: `tests/integration/scripts/crapi_setup.sh` re-run against already-provisioned accounts; vAPI: existing `hf_owner`/`hf_other` accounts' tokens recomputed), ran all three `tests/eval/` harnesses back to back in the same session against the same live target state, for a genuine apples-to-apples comparison:

  | Target | Mode | Expected prefixes found | Unexpected (FP) | Cost | Wall-clock |
  |---|---|---|---|---|---|
  | crAPI | deterministic (`TestEvalHarness`) | 1/2 | 0 | $0 | 1m20s |
  | crAPI | MCP-plan (`TestAgentEvalHarness`) | 0/2 | 5 | $0.0019 | 2m38s |
  | crAPI | orchestrator (`TestOrchestratorEvalHarness`) | 0/2 | 0 | $0.0108 | 3m13s |
  | DVWA | deterministic | 3/10 | 0 | $0 | 15s |
  | DVWA | MCP-plan | 4/10 | 0 | $0.0037 | 7m48s |
  | DVWA | orchestrator | 3/10 | 0 | $0.0008 | 5m25s |
  | Juice Shop | deterministic | 2/5 | 2 | $0 | 13s |
  | Juice Shop | MCP-plan | 0/5 | 0 | $0.0009 | 2m9s |
  | Juice Shop | orchestrator | 0/5 | 0 | $0.0042 | 3m53s |
  | vAPI | deterministic | 4/5 | 0 | $0 | 2m22s |
  | vAPI | MCP-plan | 3/5 | 1 | $0.0016 | 7m19s |
  | vAPI | orchestrator | 3/5 | 1 | $0.0013 | 5m34s |
  | **Total (22 prefixes)** | deterministic | **10/22** | 2 | $0 | ~4m10s |
  | **Total** | MCP-plan | **7/22** | 6 | $0.0081 | ~19m54s |
  | **Total** | orchestrator | **6/22** | 1 | $0.0171 | ~18m6s |

  **Verdict: the gate is not met.** Neither LLM-driven mode found a single expected-fixture finding the deterministic baseline missed; both actually regressed recall relative to it (10/22 vs 7/22 vs 6/22), while costing real money and 4-5x the wall-clock. Orchestrator mode did produce fewer unexpected findings than MCP-plan mode (1 vs 6) and was cheaper/comparable on 3 of 4 targets, but that's a comparison between two LLM modes, not evidence against the actual premise question this milestone exists to answer (does LLM-driven exploration find something the deterministic layer's measured gap actually closes) — on that question, the answer today is no.
  - A visible proximate cause, not yet root-caused further: orchestrator mode made very few tool calls before stopping (`Iterations`: DVWA 1, Juice Shop 2, vAPI 1, crAPI 5) — often just one `scan.leaf` dispatch — versus MCP-plan mode's 3-5 each. A single `scan.leaf` call only runs one plan-tree leaf's own detector+template scope, not the same "load the whole synced corpus" breadth `hackerfive scan`'s CLI baseline gets by default: doc93 M2's own logged scope-narrowings ([follow-up.md](follow-up.md) LT-160 — `recon.refresh` not merging back into the tree, no evidence-reconfirmation pipeline for `script.explore`) plausibly compound this by giving the model fewer opportunities to discover new leaves worth acting on mid-run.
  - **Per doc93's own standing rule, this result stands as the answer: `hackerfive agent` remains lab/CLI-only and must not be pointed at a real program** unless and until a future change (addressing LT-160's gaps, better prompting, more `MaxIterations`, a stronger model) demonstrates a genuine improvement and this table is re-run. Tracked as [follow-up.md](follow-up.md) LT-162 (closed with this result — reopen if a future change warrants a re-run).

## Verification

- `wsl.exe -e bash -lc "cd /mnt/c/ML-Projects/Weekend-Projects/hacker-five && go build ./... && go vet ./... && go test ./... -race && PATH=\$PATH:\$HOME/go/bin golangci-lint run ./..."` clean after each milestone.
- Unit tests per package: `pkg/scriptexec` (Precheck rejects each attack scenario in M1's table; Execute enforces the sandbox against a real Docker daemon in CI/WSL2), `pkg/llmfallback/orchestrate_test.go` (malformed/unrecognized `NextAction` response degrades to `stop`, mirroring `leaf_test.go`/`suggest_test.go`'s existing pattern), `pkg/orchestrator` (loop respects `MaxIterations`/`Budget`, never mutates the tree outside `ApplyLeafUpdate`), `pkg/agenttask` (`BeginActor`/`Actor` field, `Begin` back-compat).
- Manual: `hackerfive agent --targets <lab-target> --scope <file>` with `--allow-agent-scripts` unset first (confirms scripts never run without the flag), then with it set against a lab target, approving one real script interactively and confirming the sandbox/proxy/pre-check behave as designed (try one of M1's attack scenarios deliberately and confirm it's blocked at the stated layer).
- Web UI: launch a scan with "Use LLM agent" checked against a lab target, confirm SSE streams orchestrator turns via the Agent tab, confirm a proposed script blocks for approval and the kill switch still works mid-run.
- M5's eval harness run is the final gate before this doc's premise is considered validated.

## See also
- [92-research-llm-orchestrator.md](92-research-llm-orchestrator.md) — the research and scoped Decision-2 reopening this plan implements
- [90-research-hackerbot.md](90-research-hackerbot.md) — Decision 2's original text and this session's inline reopening note
- [follow-up.md](follow-up.md) LT-157 — the evidence-gating item M1's "Evidence rule" directly extends to script-sourced findings
