# Follow-Up: Enhancement Backlog

> Part of the [HackerFive documentation set](../README.md).

Open enhancement items and unresolved review findings, organized by category rather than by when they were raised. Direction: HackerFive is expanding beyond HackerOne-program scanning, so categories here stay useful for detection/reporting work generally. Narrative-style research and decision write-ups live in [discussions.md](discussions.md); this doc is the open-items backlog.

**Trimmed 2026-09-10** to cut re-read cost: every `✅ done` item below is compressed to 1-3 lines (symptom → fix, test pointer, phase-step pointer); every still-open item keeps full detail. **The original, unabridged write-up of every item — full reproduction steps, exact measurements, superseded designs — lives in [follow-up-archive.md](follow-up-archive.md).** When resolving an open item here, add its full write-up to the archive, then compress it here.

**`LT-N` items** form one continuous number sequence wherever they sit in this doc, currently through **LT-185** (LT-168-185 from the 2026-09-20 `hackerfive agent` reasoning-verification round — a recon/scope regression, orchestrator prompt blindness, LLM-stall handling, vacuous `done` leaves; LT-157-167 from the 2026-09-14/20 orchestrator work; LT-144-151 from the 2026-09-11 full-pipeline live round against nettix.com.pe — a JS-static endpoint scope-check gap, an unreachable Webmin-port check, LLM-timeout reliability, a host-dedup gap, PlanTree/D6 disconnect, missing scan progress feedback, real corpus-size numbers, and a cloud01/cloud02 finding asymmetry; LT-152/153 from the 2026-09-12 live round against shop.yosmart.com — a Cloudflare managed-challenge false positive and a public-page-ID-vs-IDOR design note; LT-154/155 from a Phase 8 Step 2 review — a missing `"sqli"` entry in `planexec`'s `recognizedDetectors` and a `sqli`/`mutatebfla` discoverability gap in `tools.search`/`tools_scan.go`; LT-156 from the 2026-09-13 review of a manually-authored VDP report against yosmart.com — a missing `/metrics`-exposure check and a missing host-header-reflected-redirect check). Full per-batch provenance (which live round produced which range) is in the archive's header. Next number: `grep -ohE 'LT-[0-9]+' docs/follow-up.md docs/follow-up-archive.md | sort -t- -k2 -n | tail -1` (the `-h` suppresses grep's `filename:` prefix on a two-file match — without it, the prefix's own hyphens throw off `sort -t-`'s field split and undercount, as LT-152 found live against yosmart.com 2026-09-12). A fresh live round gets its own `## Live Testing — <targets> (<date>)` section that continues the count.

## Near-term batch — "do now" (raised across the 2026-09-07 linkpop / shop.app / ALSCO runs)

**✅ Landed 2026-09-07.** Twelve small correctness/anti-FP fixes, all clean on build/vet/test/lint:
- **Recon fidelity:** LT-80 (scope parser strips inline `#` comments), LT-81=LT-75 (recon's own client sends a desktop-browser UA by default), LT-82 (uniform-wall verdict suppressed when the host has ≥5 distinct endpoints spanning a real 404 among 2xx), LT-72/86a (wave-3 canary-probe failure falls back to the wave-2 httpx root), LT-68 (new `ReconResult.AppSurface` none/thin/full verdict), LT-84a (cdnjs/jsdelivr/unpkg/Google-hosted-libs → `nonActionableTech`), LT-85 (`IsPlausibleURLPath` rejects JS-syntax-punctuation candidates).
- **`misconfig` anti-FP:** LT-69+LT-73 (exposed-path/dir-listing skip 404/429/5xx; disallowed-method skip a 2xx/3xx matching a plain-GET baseline).
- **Engine robustness:** LT-74+LT-88 (adaptive rate-halving → target-abort on sustained 429/503 or a mid-run connect-failure spike, `pkg/scanner/adaptive.go`), LT-79 (`--max-target-duration`, default 15m), LT-70 (an unresolved/dead-end leaf sorts below every dispatchable leaf).

**Routed to a phase step instead:** LT-64/65/83/84b/86b/76/77 → Phase 8 Step 5 (✅ done 2026-09-07) · LT-40/61 → Phase 8 Step 5 (✅ done 2026-09-07) · LT-78 → [Phase 9](18-implementation-plan-ph9.md) Step 3 · LT-87 → [Phase 9](18-implementation-plan-ph9.md) Step 4 · LT-67/71 → [Phase 7](16-implementation-plan-ph7.md) Step 5 (✅ done; renumbered 2026-09-10 from "Step 6a") · LT-107/108 → [Phase 7](16-implementation-plan-ph7.md) Step 7 (✅ both done 2026-09-10; renumbered from "Step 8"; LT-109/110 are its still-unscheduled rungs 3-5) · LT-89/90/91/93/94 → Phase 8 Step 5 (✅ all done 2026-09-08, pulled forward for the crAPI demo). LT-92/95/96 ✅ done 2026-09-09 too — detector-precision fixes, filed as an ad-hoc batch rather than folded into Step 5. (Phase 8's own Steps renumbered 2026-09-11 — old Step 5→4, Step 6→5, Step 10→6, per Step 1's netservice work; Steps 4/7/8/9 were fully removed from doc17, their bodies already duplicated in [Phase 9](18-implementation-plan-ph9.md).)

## Security & Scope Hardening

Resolved, kept for traceability:
- ✅ Scope allow-list, evidence redaction, IDOR normalization, JWT weak-secret check, rate-limit-signal probe, Business Logic Flaw detector (`--allow-writes`-gated) — doc10/11/13.
- ✅ OOB default: 2 public interactsh servers, `--no-oob` opt-out (2026-09-02).
- ✅ `--rate-limit` default 50 → 10 req/s (2026-09-05).
- ✅ OOB `Poller` idle-skips its network poll when nobody's waiting (2026-09-05).
- ✅ `Retry-After`-aware HTTP backoff, `max(exponential, Retry-After)`, 30s ceiling (2026-09-05, Phase 6 Step 3).

(Baseline-mode account-provisioning guidance and a self-hosted `interactsh-server` moved to [Parked](#parked--revisit-on-a-trigger-or-after-an-eval) — both engagement-triggered, not code work.)

## Detection Coverage — Protocol/Capability Expansion

Scheduled 2026-09-05 as Phase 8, split 2026-09-07 into breadth/precision ([Phase 8](17-implementation-plan-ph8.md): TCP banner-grab Step 1 ✅ done 2026-09-11, TLS passive checks Step 2 ✅ done 2026-09-12, JS static analysis Step 3 ✅, semver gating Step 4 ✅ done 2026-09-11, richer crawl Step 5 🟡 done except LT-63) and depth/active ([Phase 9](18-implementation-plan-ph9.md): OOB blind-RCE Step 1, template-format gaps Step 2, AI-agent surface Step 3, WAF-aware + injection detectors Step 4). Phases 6/7 explicitly scope detector/vuln-class expansion out. A large-wordlist sweep or ffuf-style fuzzing stays a separate opt-in-only item (Phase 8 Step 5's out-of-scope note, reaffirmed 2026-09-08 — see Parked).

## Template Engine & Detection Backlog

| Item | Status |
|---|---|
| `xpath` matcher/extractor; `flow:` cross-block `_N` indexing | [Phase 9](18-implementation-plan-ph9.md) Step 2 |
| `substr()` / `date_time()` / `generate_jwt` DSL functions | [Phase 9](18-implementation-plan-ph9.md) Step 2 |
| `flow:` `if`/`set`/`for`/`let`/`var` constructs (~42 templates) | [Phase 9](18-implementation-plan-ph9.md) Step 2 |
| Template signing | [Parked](#parked--revisit-on-a-trigger-or-after-an-eval) |
| DOM-based XSS via Chromedp | [Parked](#parked--revisit-on-a-trigger-or-after-an-eval) |
| **LT-142 ✅ mostly done 2026-09-11** — `netservice` now covers 5 of `interestingPorts`' 7 entries: 21/ftp, 3306/mysql, 6379/redis (Phase 8 Step 1) plus 5432/postgresql (trust-auth `StartupMessage`) and 27017/mongodb (unauthenticated `listDatabases` over OP_MSG — deliberately not `hello`/`isMaster`, which always succeeds pre-auth by design and would false-positive on every secured server) added this batch. **Still open:** 23/telnet and 9200/elasticsearch stay `StatusUnresolved`-only — telnet names no specific check beyond "the port is open", and Elasticsearch's real exposure check is arguably HTTP (`GET /` unauthenticated on 9200), not `tcp:`/netservice, and would want its own recon Wave-2 pass; a separate, unbuilt item, not scheduled. Also still unre-verified: LT-23's original evidence target (`staging.andertone.com`, owned) hasn't been re-scanned against the new `netservice` detector yet — do that before calling LT-23 fully closed in practice, not just in code. |
| **LT-143 (open)** — a `tcp:` template's `extractors:` block is accepted by the (lenient, non-`KnownFields`) YAML decoder but silently does nothing (`TCPRequest` has no `Extractors` field — see schema.go's doc comment): no consumer exists yet since `tcp:` requests support no `flow:`/chaining. Low priority — no real corpus tcp: template sampled at implementation time needed it. |
| Real synced-corpus `tcp:` templates (upstream's `network/` category) aren't in the committed `templates/index.json` yet | Operational, not code — `pkg/templatesync.List`'s own loader rejected every one before Step 1 lifted `tcp` out of `disallowedBlocks`; takes effect on the next corpus sync/re-index run. |
| **LT-154 ✅ done 2026-09-12** — `pkg/planexec/executor.go`'s own `recognizedDetectors` (a hand-duplicated copy of `pkg/scanner/config.go`'s set) was missing `"sqli"` entirely, found wiring in Phase 8 Step 2's `"tls"`: `registry.resolveEndpointFacts` genuinely emits sqli leaves (LT-87's endpoint-driven fan-out), but with `"sqli"` absent, RunPlan's eligibility check (`recognizedDetectors[leaf.Detector] \|\| knownTemplateIDs[leaf.Detector]`) failed both halves for every one — a real sqli leaf silently skipped as an "unrecognized detector/template-ID" on **every** webui/MCP Plan Preview run, reachable only via a hand-typed `--detector sqli` CLI invocation. Fixed alongside `"tls"`/`"mutatebfla"` (see doc17 Step 2's Design section). |
| **LT-155 (open)** — `pkg/registry/registry.go`'s `Capabilities` list (the `tools.search` MCP-discoverability registry) and `pkg/mcpserver/tools_scan.go`'s `scanInput.Detector` jsonschema hint both still name only `idor, misconfig, authbypass, ssrf, businesslogic, netservice` — `"sqli"` and `"mutatebfla"` (both real, shipped, dispatchable detectors) were never added to either when they landed. Lower severity than LT-154: neither is a real dispatch gate (`Detector` is a free-form string field, not a validated enum), just discoverability/documentation an LLM client reads — but still means `tools.search sqli` finds nothing and an MCP client has no schema hint that `sqli`/`mutatebfla` are valid `detector` values. `"tls"` was added to both when it landed (Phase 8 Step 2); the pre-existing `sqli`/`mutatebfla` gap was noticed but not backfilled this round — small, low-risk, someone should just do it next time either file is touched. |

✅ Done 2026-09-05 (LT-22): `+` concat operator, `location`/`server`/`set_cookie` parts, `binary` matcher, 10 stdlib DSL string functions, string/int comparison coercion.

## Scan Output & Logging

- ✅ Scan-duration log line (2026-09-06) — `scan finished in <d> (<n> target(s), <m> raw finding(s), pre-dedup)`, all 3 frontends.
- ✅ Rejected-template log spam (2026-09-05) — compact per-reason histogram by default, full list behind `--verbose`/`--log-rejected`.
- ✅ **Host-error-threshold skip was silent** (2026-09-10) — `hosterrors.Cache` gained `ShouldSkipWarnOnce`; the breaker now logs one `warn` line per host instead of vanishing it from the Logs panel. Tests: `TestCache_ShouldSkipWarnOnce_*`, `TestEngineRun_LogCallback_FiresOnceForHostErrorSkip`.
- **LT-141 (open) — the "this target is a dead end" signal is four unrelated mechanisms with no shared shape.** D6's uniform-wall skip (findings), the adaptive-throttle abort (findings), recon's `AppSurface: none` (stderr only), the hosterrors breaker (a log line, no finding) each report "give up" independently — a UI/report consumer has to know all four. **Possible fix:** a shared "target abandoned" event with a `reason` enum, without collapsing the existing distinct finding IDs. **Not scheduled** — UX polish, not detection coverage; revisit opportunistically or on a 5th mechanism.

## Decision Engine & Recon→Plan Signal Use

Surfaced 2026-09-04 against `andertone.com`: recon was strong but `registry.Resolve` produced 134 mostly-noise leaves and idor/authbypass/ssrf/businesslogic never activated. Root cause: the decision engine reasoned only over `TechStack`, never `Endpoints`/`Ports`, and tag-matching was too broad.

### P0 — precision & correctness (✅ all landed 2026-09-04)

Version-aware CVE ranking (P0-1a); ranked template-tag selection + canonical tech→tag map (P0-2, 107→74 leaves); `Finding.Target` double-slash fix (P0-3); PlanTree leaf dedup by `(target, detector)` (P0-4, 31→8 leaves); non-actionable-tech denylist (P0-5). Detail: [15](15-implementation-plan-ph6.md) Step 2 addenda. P0-1b (true semver range gating) → Phase 8 Step 4.

### P1 — coverage (turn recon signal into leaves)

- **P1-1 ✅** Endpoint-driven resolution pass (2026-09-04) — idor/ssrf/businesslogic went from unreachable to active.
- **P1-2 ✅ (2026-09-04 interim, 2026-09-11 real detector)** — `resolvePortFacts` emits an honest `StatusUnresolved` leaf naming an open port for a protocol nothing checks; for the ports `netservice` now covers (21/3306/5432/6379/27017) it promotes straight to a dispatchable `tcp://host:port` leaf instead (Phase 8 Step 1, closes LT-23; 5432/27017 added 2026-09-11, LT-142). Remaining `interestingPorts` entries (23/9200) still get the visibility-only leaf — see LT-142.
- **P1-3 ✅ (2026-09-04)** — WordPress plugin/theme slug+version facts from crawled endpoints.
- **P1-4** nuclei DSL/`part:` gaps — ✅ mostly closed 2026-09-04 (`content_type_N`, indexed `part:` names, `path:`-multi-request correlation). Still open: `xpath`/`flow:` cross-block `_N` → [Phase 9](18-implementation-plan-ph9.md) Step 2; affected-version index field → Phase 8 Step 4.
- Multi-key + file-based `payloads:` ✅ (2026-09-04); `interactsh_*`/OOB support for nuclei templates ✅ (2026-09-04, `pkg/oob` wired into `pkg/template/nuclei`, one shared `Poller`).
- **P1-5** `techRules` coverage — woocommerce ✅; cloud-provider (aws/s3/gcp) exposure tags ✅ done 2026-09-09, Phase 8 Step 3.

### P2 — LLM leverage & ergonomics (✅ all six done 2026-09-04)

Structured `LeafContext` for `ResolveLeaf` (P2-2); ranked-template prompts for `use_existing_tag` (P2-3); `PlanFromRecon` 4th caller, CLI `plan --llm-assist` only (P2-1/P2-4); index/corpus drift guard (P2-5); `--scope` hard-fail for CLI `plan`/`recon` (P2-6). Detail: [15](15-implementation-plan-ph6.md) Step 2 P2 addendum.

## Live Testing — nettix.com.pe & aceautowreckers.com (2026-09-04)

First real run against owned targets. All 17 items below ✅ done 2026-09-04 unless noted:
- LT-1 Web UI Launch never dispatched ranked leaves → `pkg/planexec` + Plan Preview approve/execute.
- LT-2 `misconfig.rejected()` didn't exclude 5xx → now excludes 502/503/504.
- LT-3 A real OpenAPI/GraphQL spec never triggered idor/misconfig → `resolveAPISpecFact`.
- LT-4 Recon silently failed on self-signed-cert hosts → `recon.ClientConfig` forces `InsecureSkipVerify`.
- LT-5 ✅ done 2026-09-07 — SSE catchup/dedup via a monotonic `eventSeq`.
- LT-6 ✅ done 2026-09-06 — `reporter.SplitAggregates` expands the nuclei missing-headers aggregate, collapses the native/nuclei pair. *Tail still open:* same 1:1 pair for weak-HSTS (needs a template-ID skip-list entry); webui/mcp never call `Dedup`/`SplitAggregates`.
- LT-7 ✅ done 2026-09-11 — `matchTemplateTags` is version-blind → Phase 8 Step 4. Index-time `AffectedVersionRanges()` extraction from real `compare_versions()` usage (the documented `metadata: max-version/min-version` convention measured 0 real hits) + a `matchTemplateTags` range-gate that drops an out-of-range candidate outright. 59/9,452 real templates gained a range (the `*-eol` family, several product CVEs). See doc17 Step 4 for the full corrected design and doc17's Step 4 DoD bullet for the exact numbers.
- LT-8 katana hardcoded `-depth 2`, no JS-rendering → 🟡 `--crawl-depth` done 2026-09-07; headless mode superseded by LT-99.
- LT-9 hostname product hints unused → `hostnameProductHints` exact-token dispatch.
- LT-10 Google Analytics → 3 false template leaves → added to `nonActionableTech`.
- LT-11 `recon`/`plan` emitted zero stderr → `--verbose` flag.
- LT-12 Web UI "misconfig" selection also runs the full corpus → doc fix (real mitigation is LT-16/17).
- LT-13 A malformed rendered URL crashed the whole template → skip-with-warning per path.
- LT-14 Tech facts duplicated across www./bare host variants → `NormalizeHost`.
- LT-15 Bare response headers weren't DSL identifiers → case-insensitive header fallback.
- LT-16 Full corpus scans regardless of detected tech → `registry.TechStackTags`, opt-in. **Superseded 2026-09-06** by LT-17's default-on narrowing + category floor.
- LT-17 ✅ done 2026-09-05 — CLI/MCP parity for tech-stack narrowing (`--recon-file --narrow-by-tech`).

## Live Testing — andertone.com (2026-09-04)

Recon was strong (5 hosts, 28 tech facts, 204 endpoints, ports 21/3306 on `staging.`) but exposed the decision engine's noise problem and a corpus-execution-cost problem.

- **LT-18 ✅ done 2026-09-05/06** — a scan spent its wall-clock on unrelated templates: (a) `--narrow-by-tech` made default-on + a per-detector category floor; (b) bounded intra-target template fan-out (`--template-concurrency`); (c) corpus loaded once per host not once per leaf; (d) rejected-template log hygiene. All [Phase 6](15-implementation-plan-ph6.md) Step 6, complete.
- LT-19 ✅ `canonicalTechTags["nginx"]` exclude set didn't match real corpus tags → fixed.
- LT-20 ✅ businesslogic endpoint match didn't reuse `IsStaticAssetPath` → fixed.
- LT-21 ✅ a cache-busting hash was recorded as a plugin version → shape-validated.
- LT-22 ✅ (2026-09-05) fresh template-rejection measurement (320/9,363, ~3.4%) → `+` concat, named parts, `binary` matcher, 10 DSL functions, string/int coercion all fixed; `xpath`/`flow:` → Phase 9 Step 2; disallowed blocks are by-design.
- **LT-23 ✅ done 2026-09-11** — live-confirmed FTP (21) + raw MySQL (3306) exposure on `staging.andertone.com` behind an otherwise 403-walled HTTP surface, with no reachable check. Closed by [Phase 8](17-implementation-plan-ph8.md) Step 1: `tcp:` lifted out of `loader.go`'s `disallowedBlocks` into a real bounded executor (`pkg/template/tcpproto`, matcher/dsl `data`-part alias), plus a first-party `netservice` detector (anonymous-FTP, empty-password MySQL, unauthenticated-Redis) dispatched via `resolvePortFacts` promoting a covered open port straight to a `tcp://host:port` leaf. Not yet re-verified against the live `staging.andertone.com` target itself (owned, in scope) — see LT-142's re-verification note.

### Step 5 live-verification runbook (2026-09-05, DVWA/WebGoat/Juice Shop + aalberts.com)

- LT-24 ✅ an I4 field-miss LLM call fired on every `plan`, even a misconfig-only one → gated on `planLeafDetectors(tree)`.
- LT-25 ✅ `plan --llm-assist`'s recon-wide proposal call timed out reading a slow frontier response → request timeout bumped 180s→240s.
- LT-26 ✅ done 2026-09-06, two landings — `--recon-file` tech→tag union was too liberal (pulled in corpus-wide `rce`/`kev`/`cve*` tags from one legit match); fixed via a `genericCorpusWideTags` denylist then `techProductTags` (intersect with the fact's own identifying tags only). 9,432→5,333 templates on a real rich-stack target.
- LT-27 ✅ the Web UI Cancel control full-page-navigated away → `hx-post`/`hx-target` in place.
- LT-28 ✅ `pkg/webui` tests timed out when the real recon toolchain was on `PATH` → test-isolation env scrub.

## Live Testing — www.valmo.in / Meesho (2026-09-06 and 2026-09-07)

First end-to-end pipeline run against a modern SPA/CDN target. `www.valmo.in` was a Google-Cloud-fronted React SPA returning one byte-identical `index.html` for every path (incl. a random canary) — recon's unguarded 2xx-checks fabricated signal, and `plan` produced ~19 false leaves out of 21. By the 2026-09-07 re-run the asset had moved behind an Akamai WAF (403 on everything) — a second, different class of the same "recon needs a block-wall gate" problem. Full write-ups: `.engagements/meesho/logs/{baseline,acceptance,dryrun}-2026-09-06/*.md`.

**All items below ✅ done** (demo-batch 2026-09-06, post-demo-batch 2026-09-06, `ph7-step3a/3b/4a/4b` and the near-term batch 2026-09-07) unless marked open:
- **Canary/soft-404 gating (LT-30, LT-30b):** `EndpointFact` gained `BodyLen`/`ContentType`/`Title`; `probeCommonPaths` fetches one canary/host and drops any 2xx/3xx matching its shape; an `APISpecFact` requires a real structured content-type. Highest-value single fix from this round.
- **Non-actionable-tech additions (LT-31, LT-84a):** Google Cloud Storage/S3/CloudFront, cdnjs/jsdelivr/unpkg/Google-hosted-libs.
- **LT-32** a live HTTP host with only non-actionable tech facts got zero misconfig leaf → `resolveLiveHostBaseline` emits a baseline leaf for any host with a direct-probe live endpoint.
- **LT-33** recon-tool names (`katana`/`subfinder`) leaked into `MergeLLMProposals`/`PlanFromRecon` as non-executable "detectors" → filtered to `KindDetector`.
- **LT-34** `plan` always re-ran full recon → `--recon-file` input.
- **LT-35** Wave 1 subdomain/SAN enum ran even with no wildcard scope entry → `scope.HasWildcard()` gate.
- **LT-36** recon sent no policy-mandated headers → `policy.yaml` `request_headers:`, threaded through all 3 frontends.
- **LT-37** `plan --llm-assist` gave zero progress/spend visibility → per-leaf log lines + `spent $X of $Y`.
- **LT-38** a context-killed recon wave was indistinguishable from "found nothing" → `errWaveTimeout` + explicit warning. *Tail still open:* scaling `waveTimeout` by host count (see LT-111 below, which added an explicit override instead).
- **LT-39** `robots.txt`/`sitemap.xml` fetched but never parsed → `EndpointFact`s from `Disallow`/`Allow`/`Sitemap:`/`<loc>`. *Tail still open:* seeding the Wave-3 crawl itself from the parsed hints; recursive child-sitemap fetch.
- **LT-40** ✅ done 2026-09-07 (Phase 8 Step 5) — OpenAPI JSON/YAML spec walker (`specwalk.go`), live-validated against crAPI's real spec (40 paths → 6 idor candidates incl. the real BOLA route). *Tail (a) still open:* GraphQL SDL/introspection walking.
- **LT-41** `ResolveField`/`TriageFindings` reachable only from mcpserver/webui → `pkg/fieldsuggest` shared package + CLI `triage --llm-assist` + `scan --recon-file` self-fill.
- **LT-42** an engagement's prose `policy.md` was invisible to D2 preflight → warns when no `policy.yaml` sits beside it.
- **LT-43(1)/(2)** misconfig's `panel` floor tag (~1,591 templates) admitted on a bare 401/403 → gated on a real auth-boundary/admin-path signal; paired with the new `pkg/uniformwall` primitive (D6) that short-circuits the per-target corpus entirely on a uniform wall/catch-all verdict.
- **LT-44** PlanTree was flat, no grouping/priority/seeding → `GroupIntoClassNodes`, `PlanNode.Priority`/`Class`, `ExecOptions.SeedFn` (a completed leaf's finding can seed a still-pending one). Full `DependsOn` graph deferred → LT-56.
- **LT-45** CLI `scan --detector authbypass` hard-required `--auth-token` (MCP/webui didn't) → `SkipAuthTokenRequired` on the CLI path too.
- **LT-46** scan-completion log counted pre-dedup findings with no label → labelled "raw finding(s), pre-dedup".
- **LT-47** native weak-HSTS `max-age` check added (`misconfig-weak-hsts-max-age`).
- **LT-48** no deterministic "junk fact" suppressor → relevance-score floor (`minTemplateLeafScore=60`) replaced the blunt per-tech leaf count cap. A genericness-keyed fan-out cap is NOT done (folded toward LT-49's LLM veto, itself also done).
- **LT-49** no plausibility veto over a confidently-wrong `StatusPending` leaf → `VetPendingLeaves`/`VetoImplausibleLeaves` (opt-in `--llm-assist`; demote/drop only, never strengthens a leaf).
- **LT-50** tech facts and endpoints were never cross-correlated → `productEndpointSignatures` promotes a pending leaf to high confidence when a product-distinctive endpoint is observed on the same host.
- **LT-51** a `400` on an `/api/*` path is a stronger IDOR/param signal than a `404`, untreated as such → `apiRouteConfidence` lifts endpoint-driven leaves to high confidence.
- **LT-52** an out-of-scope crawled link still reached `ReconResult.Endpoints` → diverted to `OutOfScope`.
- **LT-53** `scan` didn't auto-apply policy `request_headers` (recon/plan did) → fixed.
- **R-c** `/robots.txt` double-fetched (Wave 0 + Wave 3) → Wave 3's duplicate dropped.
- **LT-61 / LT-63 (routed, not this run)** consume the CDN-ASN fact to skip/shorten naabu (✅ done, see the Akamai re-run below); companion mobile-app API discovery via subfinder's `crtsh` source → Phase 8 Step 5, still open.

## Live Testing — www.valmo.in / Meesho, Akamai re-run (2026-09-07)

Asset had moved behind an Akamai WAF returning a 403 "Access Denied" on every path (verified across UA/protocol/header variants) — zero reachable surface from this vantage. `plan` produced an empty tree; native misconfig correctly reported `misconfig-waf-blocked` in 5.8s; the `--recon-file`-narrowed full-corpus run ran 30+ minutes for nothing. All items ✅ done 2026-09-07 (`ph7-step4a`/`lt57-60-waf-plan-gaps`):
- LT-57 `resolveLiveHostBaseline`'s live-status gate excluded 401/403/429 → widened, so a WAF-walled host still gets a baseline misconfig leaf.
- LT-58 `reconShowsAdminSurface` re-admitted the `panel` floor on a blanket WAF 403 → now requires a real auth-boundary/admin-path signal, not just any 403.
- LT-59 **the D6 primitive itself:** `pkg/uniformwall.Classify` (recon Wave 3) → `ReconResult.UniformResponses` → `scanner.Engine` skips `runTemplates` for a walled host, emitting one honest finding. Override: `--scan-uniform-anyway`. (Originally one fact per whole run — widened to one per host by LT-140 below, 2026-09-11.)
- LT-60 an empty plan gave no diagnostic → `emptyPlanDiagnostic` stderr line.
- LT-61 CDN-edge ASN table (`pkg/recon/cdnasn.go`) skips naabu port-scanning a recognized CDN-edge host. *Tail still open:* per-resolved-IP ASN classification (today only the seed domain's own ASN is checked).
- LT-62 recon gave no "I'm blind here" signal on a high block ratio → `UniformResponseFact.BlockedRatio` + a `plan` stderr diagnostic.
- **LT-63 (open)** — a scope entry with a documented companion mobile app triggers no API-host discovery. **Fix:** a passive, scope-checked companion-API pass reusing subfinder's `crtsh` CT-log source for `api.`/`gw.`/`mobile.` siblings. → Phase 8 Step 5.

## Live Testing — linkpop.com / Shopify (2026-09-07)

A decommissioned asset: root 301s to the out-of-scope `www.shopify.com`; every other path is a GCS bucket serving one static 404 shell. All items ✅ done 2026-09-07 (Phase 7 Step 6a / Phase 8 Step 5 first tranche / near-term batch) unless noted:
- LT-64 recon followed a cross-host redirect and attributed the destination's content to the original target → `redirect_chain`/`final_url` recorded, first-hop status kept, out-of-scope destination warned loudly.
- LT-65 tech facts fingerprinted post-redirect were attributed to the original host → withheld; paired with `dropCDNTechWithoutHeader` (a CDN brand with no corroborating header on the host itself is dropped).
- LT-66 ✅ done 2026-09-10 — the D6 catch-all verdict was host-level; a bucket host whose canary happened to 404 could still record an individual `200` path as a high-confidence endpoint. Fixed: `probeCommonPaths` now keeps a per-call body-hash/bucket-marker record, and `dropCatchallCommonPathEndpoints` drops any batch endpoint whose body duplicates another probed path's or whose headers carry an `x-goog-*`/`x-amz-*`/`x-guploader-uploadid` marker, when the host classified `catchall`. Test: `pkg/recon/catchall_endpoints_test.go`.
- LT-67 8 `shopify-*` secret-grep template leaves planned against a 746-byte static shell → suppressed when the host shows no dynamic-content evidence.
- LT-68 (see the valmo batch above — done in the near-term batch, first hit on this run).
- LT-69/73 (see the anti-FP fixes above — the 200-equals-baseline and 429-as-exposed shapes resurfaced here, same fix).
- LT-70 (see above — done in the near-term batch).
- LT-71 ✅ done (Phase 7 Step 6a, F4) — `LoadDirByIDs` ID-peek fast path for a plan-executor specific-template leaf; skips a full corpus parse. *Still open:* the pure-`--tags` scan path (no plan) still full-loads.

## Live Testing — accounts.shopify.com / shop.app (2026-09-07)

Both behind Cloudflare managed-challenge. `accounts.shopify.com` was the model D6 outcome (correctly classified, corpus skipped, one honest finding in 10s). `shop.app` was not: a 58-minute scan into the wall produced 11 noise findings from self-inflicted rate-limiting, because its wave-3 canary had errored rather than returned a readable block page, so no uniform-wall fact was ever set. All items ✅ done 2026-09-07 (near-term batch / Phase 8 Step 5 first tranche) unless noted:
- LT-72 D6 verdict derived only from the canary probe, with no fallback on a canary error → falls back to the wave-2 httpx root observation.
- LT-73 (see anti-FP fixes above).
- LT-74 no adaptive backoff/abort on sustained 429/503 → the adaptive-throttle middleware (LT-88 extends it to connect-failure spikes).
- LT-75 (= LT-81, see above).
- LT-76 low-confidence recon endpoints (robots/sitemap, unprobed) were discarded wholesale, missing a 110-endpoint payment/OAuth surface → `probeUnprobedEndpoints` probes a name-ranked sample.
- **LT-77 (🟡 partly done)** — no redirect/OAuth-flow detector rule for `*/bounce`, `/oauth/authorize`-shaped endpoints. `IsRedirectFlowPath` now dispatches the corpus's `open-redirect-generic` template against such hosts. **Still open:** a first-party per-param off-origin-`Location` probe beyond that one template → folded toward [Phase 9](18-implementation-plan-ph9.md) Step 4.
- **LT-78 (open)** — the `llms.txt`/`SKILL.md`/`/mcp/` AI-agent surface is unmodeled (prompt injection into agent instructions, unauthenticated MCP `tools/list`, agent-reachable mutating tools). **Fix:** a passive recon signal (fetch+record the manifest) + a follow-up leaf class (injection-marker scan, unauthenticated `tools/list` probe). → [Phase 9](18-implementation-plan-ph9.md) Step 3, needs its own design pass.
- LT-79 no per-target wall-clock budget → `--max-target-duration` (default 15m).

## Live Testing — ALSCO / Secure Gateway sandboxes (2026-09-07)

First genuinely reachable (no-CDN) bug-bounty target in four runs — but the program's premise is bypassing their own WAF/upload filters, and the run ended with the scanning IP blocked at origin. All items ✅ done 2026-09-07 (near-term batch / Phase 8 Step 5 first tranche) unless noted:
- LT-80 `--scope` didn't strip inline `#` comments → fixed (same fix applied to `scan -t <file>`).
- LT-81 (= LT-75, see above).
- LT-82 D6 verdict set without cross-checking recon's own crawl evidence (54 real endpoints incl. a genuine 404 among 2xx) → `crawlEvidenceRefutesWall` suppresses the verdict on ≥5 distinct endpoints spanning ≥2 status codes.
- LT-83 numeric query params (`?article=3`) ignored as ID-shaped IDOR candidates → `numericQueryIDCandidates`.
- LT-84 false `Cloudflare`/CDN facts and asset-host brands (cdnjs/jsdelivr) spawned unresolved leaves → denylist additions + the LT-65 "CDN fact absent from host's own headers" fix.
- LT-85 (see above — JS-syntax-fragment candidates).
- LT-86 the repeated-error breaker discarded a host's good observations when only *some* paths tarpit → a timed-out path now only counts as a per-path skip; the wave-2 root is retained.
- **LT-87 (partially fixed 2026-09-11 — see below; WAF-detect fact, 403-is-signal retry, and `uploadbypass` still open)** — no WAF-fingerprint step, no payload-mutation-on-403 retry, no upload-bypass detector, and — the base gap — no native `sqli`/`xss`/`lfi` detector at all, those classes covered solely by generic corpus templates. **Fix:** (1) WAF-detect recon signal; (2) retry a blocked payload with a mutation/encoding set and flag any bypass; (3) native `sqli`/`xss`/`lfi` detectors; (4) an `uploadbypass` detector. → [Phase 9](18-implementation-plan-ph9.md) Step 4, sequenced last (wants Phase 8's param surface first). A docs/22 target-selection caveat is already applied. **Applied (sub-item 3, sqli only — xss/lfi still open):** a first-party `pkg/detectors/sqli` package (error-based + boolean-based-blind + time-based-blind, each a small fixed payload set appended to a param's existing value, never an unbounded fuzzing engine) closes the "no native sqli detector at all" gap. A new `recon.SuggestSQLiTargets` mines the same ID-/value-shaped query-param surface `idorCandidatesAndSeeds` already mines for IDOR (an ID-named key with an ID-shaped value, or LT-83's unnamed-numeric-key-varying-across-≥2-observations signal), grouped by path; `registry.resolveEndpointFacts` fans out one `sqli` leaf per candidate path (`leaf.SQLiPath`/`SQLiParams`, mirroring LT-91's idor fan-out), `planexec.applyLeafReconFields` copies them into a blank `scanner.Config`, and `pkg/scanner/engine.go` dispatches `sqli.Detector.Run` (new `--detector sqli` value, `--sqli-path`/`--sqli-param` CLI flags for a manual run). Each technique is deliberately conservative to hold the <5% FP target: error-based requires a DBMS-specific error signature present in the payload response but *absent* from the baseline (never matched if the baseline already carries one); boolean-based requires the tautology response to match baseline *and* the negation to diverge (a param with no real effect produces true==false==baseline and is correctly never flagged); time-based requires the delay to repeat on an independent confirm request and skips entirely when the baseline itself is already slow. **Known, deliberately deferred gaps** (same precedent as ssrf's body-params having no CLI flag): no webui Launch-page tab, no MCP tool wiring, and no `fieldsuggest.Deterministic` auto-fill for the bare `scan --recon-file` CLI path — the recon→decisionengine→planexec pipeline (Plan Preview, MCP agent) is fully wired and is the primary path this was built for. Tests: `pkg/recon/suggest_test.go` (`TestSuggestSQLiTargets*`), `tests/unit/detector_sqli_test.go` (error/boolean hit + 4 decoy/no-finding cases, multi-param independence, auth-header propagation), `pkg/detectors/sqli/timebased_test.go` (internal white-box, test-scale sleep durations: hit+repeat-confirmed, delay-doesn't-repeat, slow-baseline-skipped).
- LT-88 (see LT-74 above — folded in as an extra trigger condition).

Worked correctly this round: LT-26 tech-tag narrowing, LT-35 exact-host-scope warning, the D6 `--header`-UA workaround.

## Live Testing — crAPI actionable-findings prep (2026-09-08)

Capability-gap review (not a live run): every prior live target was WAF-walled/decommissioned, so the recon→detector→**confirmed finding** loop had never run against a dense-vuln target. crAPI has that surface (BOLA×N, JWT `alg:none`, SSRF, mass assignment); the detectors were already capable, the gaps were on the recon→feed side.

- LT-89 ✅ done 2026-09-08 — `recon --openapi-spec <file|url>` ingests a spec recon can't discover on its own (local/behind-auth/non-standard-path), scope-checked, folded into `ReconResult` like a served spec.
- LT-90 ✅ done 2026-09-08 — `walkOpenAPISpec` reads OpenAPI `security:` declarations onto `EndpointFact.AuthRequired`, feeding an endpoint-driven `authbypass` leaf.
- LT-91 ✅ done 2026-09-08 (Step C) — one idor leaf with N candidates meant a downstream field-miss picked the wrong one (crAPI: a harmless community-posts route, not the real BOLA orders route) → `resolveEndpointFacts` now fans out **one idor leaf per candidate**, each self-sufficient (own `EndpointTemplate`), capped at 12.

### Step B — crAPI live round 1 (diagnostic), 2026-09-08

First end-to-end recon→detector→confirmed-finding loop, two accounts, spec ingested via LT-89. Isolated detector runs (`--templates <empty-dir>`):
- **authbypass** — 6× critical (JWT `alg:none` forged token → real PII, manually reproduced; crAPI genuinely doesn't verify signatures), 1× BFLA (flagged via token-reuse, see LT-92), 5× token-reuse noise (hedged, acceptable), 1× no-rate-limit on login (hedged).
- **idor** — 5× high, verified: baseline-mode BOLA on `/workshop/api/shop/orders/{{id}}` (orders 1-5 return full card data to both test accounts; 6-100 all 500, 0 FP). crAPI's canonical BOLA, pre-LT-91 only reachable by hand-picking the endpoint.
- **LT-92** ✅ done 2026-09-09 — new `authbypass.checkBFLA` fires a structural signal (>1 distinct account identifier in the body of a `/all`/`/admin`/`management`-shaped path) instead of relying on the diff-based token-reuse check, which was tightened to require a non-empty, identifier-bearing body before flagging at all — quiets the 5 benign shared-catalog hits without touching the real BFLA detection.

### Step E — crAPI live round 2 (full pipeline + fp/fn), 2026-09-08

The whole loop, no manual per-leaf scan: `recon --openapi-spec` → `registry.Resolve` → `planexec.RunPlan` (the same executor webui/MCP wrap), two tokens on `baseCfg`. Surfaced and fixed LT-93, LT-94 (below); after those, one serial run produced **26 findings, 0 FP**:

| crAPI vuln | Result |
| --- | --- |
| Broken auth — JWT `alg:none` | ✅ 8× critical, manually reproduced |
| BOLA — shop orders + card data | ✅ 5× high (orders 1-5; 6-100 all 500, none flagged) |
| `/.env` served with real DB/Mongo passwords | ✅ 1× high misconfig |
| Missing security headers on API base | ✅ 4× misconfig |
| Excessive-data / token-reuse hedged hits | 6× medium noise (LT-92) + 1× medium no-rate-limit |
| BOLA — vehicle location (UUID IDs) | ❌ miss → **LT-95** (idor only enumerated small ints) |
| BOLA — mechanic reports (empty `report_id=`) | ❌ miss → **LT-95** (spec walker didn't template an empty ID-shaped query key) |
| BFLA — delete another user's video (DELETE) | ❌ miss → **LT-132** (no detector probes non-GET methods) |
| SSRF — `contact_mechanic` forwards attacker URL in POST **body** | ❌ miss → **LT-96** (SSRF param-suggestion only inspected query-param names) |
| Mass assignment; NoSQL/SQL injection | ❌ miss → **LT-133** (no native detector; injection needs a corpus run, not attempted) |
| JWT weak-secret; OTP rate limiting | ❌ miss → **LT-134** (wordlist/path didn't hit) |

- LT-93 ✅ done 2026-09-08 (Step E) — every `registry.resolve*` helper set a leaf's `Target` to the bare hostname (no scheme/port), so every request against a real host was malformed (0 findings) → `Resolve` rewrites a dispatchable leaf's `Target` to the real `scheme://host[:port]` recon observed.
- LT-94 ✅ done 2026-09-08 (Step E) — `planexec.RunPlan` wasn't self-sufficient: the webui execute path (unlike MCP) never pre-filled `ProtectedPaths`/`SSRFParams` from recon → the derived fields now ride the leaf itself (`PlanNode.ProtectedPaths`/`SSRFParams`), filled by `applyLeafReconFields` just before dispatch.
- LT-95 ✅ done 2026-09-09 — (1) an empty ID-shaped query key (`?report_id=`) now templates as `{{id}}`; (2) new `idor.RandomUUIDStrategy` (fresh UUIDs as denial-baseline filler + one real observed UUID as the sample) covers UUID-keyed BOLA without inventing target IDs.
- LT-96 ✅ done 2026-09-09 — the spec walker now reads a POST operation's JSON body schema onto `EndpointFact.BodyParamKeys`; `SuggestSSRFBodyParamsFromRecon` keyword-matches them; `ssrf.Detector` gained a body-param check (`checkBodyParamTargets`).
- **LT-132 ✅ done 2026-09-12 (DELETE only — PUT/PATCH deferred, see below)** — no authbypass/idor check ever probed a non-GET method, so crAPI's real `DELETE /workshop/api/merchant/video/delete/{id}` BFLA (delete another user's video) was structurally unreachable. **Applied:** a new first-party `pkg/detectors/mutatebfla` package — the second detector in the project (after businesslogic) to perform a genuinely mutating request, and the first that is *intentionally* destructive when it confirms a real bug (a real DELETE against the owner's own resource, fired with the other account's token). Safety-first design, matching the concern named when this item was opened:
  - the resource under test is never enumerated/guessed — the caller supplies a concrete `--mutatebfla-delete-path` (real ID already substituted) that the operator has independently confirmed the owner account owns; there is no idor-style ID-range mode here;
  - the detector re-confirms that ownership itself before ever mutating anything: a baseline GET as the owner account must return `200` and contain an explicit, caller-supplied `--mutatebfla-marker` substring (typically the resource's own ID) — a `Target` with no marker, or whose baseline doesn't contain it, is skipped with **zero requests sent beyond the baseline GET**, never a guess;
  - the finding is only ever recorded from a **follow-up GET as the owner**, after the other account's DELETE, showing the marker genuinely gone — never inferred from the DELETE response's own status code, which a real backend can misreport (a `200`/`204` that didn't actually delete anything);
  - gated behind its own flag, `--allow-mutating-bfla` — a *third*, independently-scoped exception to the read/enumerate-only default, deliberately never folded into `--allow-writes` (businesslogic) or `--auto-provision-account` (pkg/provision), per CLAUDE.md's "any new mutating capability needs its own equally-scoped flag" rule. Absent, the detector is skipped with a once-per-scan stderr warning, same treatment as `--allow-writes`.
  Requires both `--auth-token` (owner) and `--other-auth-token` (other) — there is no single-account mode, since a one-token run has no way to know whose resource is under test. `--mutatebfla-verify-path` defaults to `--mutatebfla-delete-path` (the common RESTful case: same path, GET vs DELETE) but can point at a different endpoint (e.g. a list endpoint) for a target that splits them the way crAPI's own `.../video/delete/{id}` does. **Known, deliberately deferred gaps:** (1) **PUT/PATCH** are not built — same harness, just an inverted marker polarity (absent → present) plus a request body, explicitly parked as a near-identical follow-up rather than folded in here; (2) **no recon/decisionengine/planexec/webui/MCP auto-derivation** — unlike `EndpointTemplate`/`SQLiPath`, recon has no way to determine which concrete ID belongs to which account, so this is CLI-flag-driven only for now, by design, not an oversight. Tests: `tests/unit/detector_mutatebfla_test.go` (real-deletion hit, properly-protected no-finding, the misleading-status-code decoy, the unconfirmed-baseline-never-mutates safety test, the gate-blocks-every-request test, missing-token no-op, a crAPI-shaped split verify-URL case, auth-token propagation, and Finding.ID sanitization).
- **LT-133 (open)** — two homeless vuln classes from the same crAPI observation. **Mass assignment** (an extra/undocumented JSON field silently accepted on write) has **no detector anywhere**, native or corpus, and doesn't fit Phase 9 Step 4's `sqli`/`xss`/`lfi`/`uploadbypass` scope. **NoSQL/SQL injection** *is* Step 4's job once it lands. **Value: medium** (real class, not independently confirmed exploitable this round). **Effort: high** for mass assignment (needs its own design pass — safely diffing "extra field accepted" without a destructive write); SQLi/NoSQLi effort already accounted for in Step 4. **Route:** SQLi/NoSQLi → Phase 9 Step 4; mass assignment → needs its own future design note, not silently folded in.
- **LT-134 (open)** — `checkJWTWeakSecret`'s fixed wordlist didn't contain crAPI's actual secret, and `checkRateLimitSignal` never probes an OTP/2FA-verification endpoint (only `LoginPaths`). **Value: low** (target-specific wordlist/candidate-list gap, not structural). **Effort: low** (append `WeakJWTSecrets` entries / add an `OTPPaths` list, same pattern as `LoginPaths`/`LogoutPaths`). **Route:** low-priority roll-up, pick up opportunistically.
- LT-123 ✅ done 2026-09-10 (design review) — `--auto-provision-account` + `--provision-email <template>`: registers a throwaway second account against a recon-observed signup endpoint (new `ReconResult.SignupEndpoint`, spec-derived or path-guess) so idor/authbypass's second-account checks don't need an operator-supplied token. A second, independently-scoped exception to read/enumerate-only (CLAUDE.md) — fails closed on an email-verification gate, no cleanup attempted. CLI-only for v1 (MCP/webui wiring gap → LT-131).
- LT-135 ✅ done 2026-09-10 — `businesslogic`'s coupon checks were previously crAPI-only in practice: hardcoded field/response-field names on top of the already-overridable paths. `responseGrantedAmount` generalized to a bounded recursive scan for any numeric field within [90%,150%] of the injected amount; new spec-derived `ReconResult.CouponEndpoint`/`CouponFact` (schema v1.12) extracts both the mint+apply paths *and* their real field names from an OpenAPI spec (no path-guess fallback — recon must stay read-only and a coupon-mint endpoint is POST-only/mutating). `--allow-writes` + a spec-carrying `--recon-file` now needs zero manual coupon flags. Tests: `pkg/recon/specwalk_test.go`, `tests/unit/detector_businesslogic_test.go`, `pkg/registry/decisionengine_test.go`.
- **LT-136 (open, design idea)** — a JS-static-extracted secret (Phase 8 Step 3) is reported on pattern match alone, never confirmed live. **Idea:** for a curated, small set of well-known shapes, add one bounded read-only validity probe against the credential's own vendor API (AWS STS `GetCallerIdentity`, GitHub `/user`, Slack `auth.test`) and fold a confirmed-live result into `Confidence`/evidence — never `Severity` (detector-sets-severity invariant preserved). **Value: medium-high** (large trust jump, "confirmed live" vs. "looks like," for one extra benign call). **Effort: low-medium** (each vendor check is one fixed-endpoint call; the real work is the shape→probe dispatch table and never persisting the credential value beyond that one call). Not scheduled; natural fit alongside Phase 8 Step 3 if picked up.

## Live Testing — yosmart.com (2026-09-12)

Full recon → plan → `scan --detector misconfig` round against the real, disclose.io-authorized `shop.yosmart.com` (doc22), a Shopify-hosted storefront behind Cloudflare.

- **LT-152 (open)** — `misconfig`'s exposed-path check fired `misconfig-exposed-path-admin` (medium/high-confidence) against `/admin` on a 403 response whose body was Cloudflare's own generic **managed-challenge interstitial** ("Verifying your connection...", `Cf-Mitigated: challenge` header, the `_cf_chl_opt`/`cf_chl_rt_tk` JS challenge markers) — not real application content. Hand-verified: a fresh, independent request to the same URL minutes later got a clean `301` to `https://<store>.myshopify.com/admin` (Shopify's real, expected, unauthenticated-safe admin-login redirect) — the challenge page only appeared transiently mid-scan, plausibly triggered by the scan's own request volume tripping Cloudflare's bot-heuristics on that one path. This is the same false-positive *class* LT-113/151's Akamai-WAF-page keyword-match bug was for `meesho_bbp`'s `investor.meesho.com`, but a different trigger shape: not a uniform catch-all across every path (which the existing D6/baseline-canary suppression already handles), but a **per-path, intermittent, bot-mitigation interstitial** on an otherwise-healthy host — the canary/uniform-wall heuristic never fires here because the homepage keeps returning normal 200s throughout. **Fix idea:** recognize known CDN/WAF challenge-page markers (Cloudflare's `Cf-Mitigated: challenge` response header is a clean, vendor-supplied signal — no body-sniffing needed) and suppress/downgrade a misconfig finding built from *that specific response*, independent of whether the rest of the host looks walled. **Value: medium** (a live-reproduced FP on a real, otherwise-clean target — exactly the false-positive-erodes-trust risk the <5% target exists for). **Effort: low** (one header check, same shape as the existing WAF-block detection, just applied per-response instead of only per-host). Not fixed this round — filed for follow-up rather than same-session, since it's a precision/FP issue, not a scope-safety one.
- **LT-153 (open, design note)** — the deterministic `plan` command synthesized an `idor` leaf (`endpoint_template: "/{{id}}"`, confidence "medium") from a bare numeric path segment recon pulled out of `robots.txt` (`/30806376585`). Manually cross-checking the site's own response headers showed this number is Shopify's internal **page-cache key** (`Etag: W/"page_cache:30806376585:IndexController:..."`) — a public, identical-for-every-visitor homepage identifier, not a per-account or per-user resource. Running IDOR enumeration against it could never produce a real finding (there's no differential access to detect) and was skipped this round rather than burning requests on it. **Idea:** the numeric-ID-in-recon-path → `idor` leaf heuristic (`registry.Resolve`) has no way to distinguish a per-account resource ID from a public catalog/page/CDN-cache ID; on a well-known SaaS platform (Shopify, and likely similarly for Wix/Squarespace/BigCommerce), a large class of these "numeric ID in a URL" signals are structurally public. **Value: low-medium** (mostly wasted-request avoidance + a cleaner Plan Preview on this common site shape, not a missed-vulnerability risk). **Effort: medium** (needs a per-platform "known-public-ID-source" allowlist, e.g. Shopify's own product/collection/page/variant ID shapes, to suppress confidently rather than guessing). Not scheduled — noted for whenever Shopify-storefront testing recurs.
- Positive confirmation worth recording: the `plan`→tech-narrowed `scan` correctly loaded and ran the Shopify-token-exposure/`weglot-api-key-exposure`/jQuery-CVE templates the tech fingerprint (Shopify, Weglot, jQuery 2.2.4) justified, and all came back clean (no secrets/tokens exposed) — real coverage, correctly scoped, zero false positives from that set.

## Live Testing — yosmart.com VDP-report review (2026-09-13)

Reviewed a manually-authored, non-HackerFive VDP report (`.engagements/yosmart/findings.md`) against `api.yosmart.com`/`www.yosmart.com` for validity per [docs/23-live-testing-playbook.md](23-live-testing-playbook.md) §3 — independently re-verified all 6 findings live via read-only `curl`, matching the report's own method. All 6 reproduced: a publicly exposed Prometheus `/metrics` (high, real fleet-size/infra/internal-`csid`-identifier leak), genuine Access-Control-Allow-Origin *reflection* + credentials on `api.yosmart.com` (high, correctly hedged pending cookie-auth confirmation — a materially different, more dangerous case than LT-139's literal-wildcard finding on aalberts.com, and confirmed as *not* down-ranked by that fix), missing HSTS and a host-header-reflected redirect on `www.yosmart.com` (both low, correctly caveated), and Shopify-owned `robots.txt`/`agents.md` agent-instruction content (correctly out-of-scope, not acted on).

- **LT-156 ✅ done 2026-09-13** — neither of two real, common misconfiguration classes the report's own findings 1 and 5 hit were covered by `misconfig`'s `ExposedPaths` list or any other check: a Prometheus `/metrics` scrape endpoint, and CWE-601 host-header-poisoned redirects. **Fix:** (1) added `{Path: "/metrics", Keywords: []string{"# HELP ", "# TYPE "}, Severity: "high"}` to `pkg/detectors/misconfig/rules.go`'s `ExposedPaths` — Prometheus's exposition-format markers are effectively zero-FP. (2) new `checkHostHeaderRedirect` (`pkg/detectors/misconfig/detector.go`): spoofs `req.Host` to a `.invalid` canary on a GET `/`, using a new `httpclient.Client.DoNoRedirect` (shares the existing client's Transport, never follows a redirect — so a reflected Location is inspected without this detector ever actually connecting to whatever it points at) and flags a `misconfig-host-header-reflected-redirect` (low/high) finding when the response's `Location` echoes the spoofed Host back. Found live that the vulnerable behavior was specifically on `www.yosmart.com`'s plain-HTTP listener, not its (correctly SNI/Host-matched, 404-on-mismatch) HTTPS listener — a common real-world shape, since the plain-HTTP listener's only usual job is the http→https upgrade redirect and is often configured more loosely. So `checkHostHeaderRedirect` tries the target's own scheme first, then falls back to the plain-http variant (`httpDowngrade`) if that found nothing. Tests: `TestMisconfigExposedPath_PrometheusMetrics_Hit`, `TestMisconfigHostHeaderRedirect_Reflected`, `TestMisconfigHostHeaderRedirect_FixedTarget_NoFinding`, `TestHttpDowngrade`, `TestClient_DoNoRedirect_NeverFollows`. Both new checks confirmed live: re-scanning `api.yosmart.com`/`www.yosmart.com` after the fix produced `misconfig-exposed-path-metrics` (high) and `misconfig-host-header-reflected-redirect` (low, `target: http://www.yosmart.com` — the fallback path, exactly as expected).

## Live Testing — G1 three-way eval re-verification, isolated lab targets (2026-09-20)

Re-ran [93-implementation-plan-agent-orchestrator.md](93-implementation-plan-agent-orchestrator.md) M5's gate after this session's five fixes (LT-157, both LT-160 items, LT-165 item 1, LT-166's related dedup bug, LT-138 item 1) — `TestEvalHarness` (deterministic) and `TestOrchestratorEvalHarness` (orchestrator), against the same four lab targets and fixtures as the 2026-09-15/09-19 runs, with a live `OPENROUTER_API_KEY` (`deepseek/deepseek-v4.1-flash`).

**A real contamination bug surfaced mid-run and forced a methodology change** (see new LT-167): with all four targets running simultaneously (the convenient default, and what every prior eval run in this doc also did), `Scope.Allowed`'s host-only matching let a `recon.refresh` action wander from one target onto another colocated-on-`localhost` lab target entirely undetected — a first joint run's crAPI leg reported 50 findings, but a manual repro showed a real chunk of that was actually DVWA's output mislabeled as crAPI's. The orchestrator legs were therefore re-run one target at a time, with every other lab container stopped, to get a trustworthy number; the deterministic baseline was not re-run in isolation since it never calls `recon.refresh` with an LLM-chosen target — every deterministic invocation's target is a fixed CLI flag, immune to this class of drift.

| Target | Mode | Expected prefixes found | Unexpected (FP) | Cost | Wall-clock |
|---|---|---|---|---|---|
| crAPI | deterministic (idor) | 1/2 | 0 | $0 | 1m19s |
| crAPI | orchestrator (isolated) | 2/2 | 5 | unknown† | 20m0s† |
| DVWA | deterministic | 3/10 | 0 | $0 | 15s |
| DVWA | orchestrator (isolated) | 4/10 | 0 | $0.0057 | 8m34s |
| Juice Shop | deterministic | 2/5 | 2 | $0 | — |
| Juice Shop | orchestrator (isolated) | 2/5 | 2 | $0.0002 | 5m30s |
| vAPI | deterministic (misconfig) | 4/5 | 0 | $0 | 2m17s |
| vAPI | orchestrator (isolated) | 5/5 | 0 | $0.0225 | 12m55s |
| **Total (22 prefixes)** | deterministic | **10/22** | 2 | $0 | ~4m |
| **Total** | orchestrator (isolated) | **13/22** | 7 | ≥ $0.0284 | ~47m |

† crAPI's isolated run hit the harness's 20-minute context timeout before producing a final `Result` — both expected prefixes were still confirmed via streamed `finding` events (LT-163 item 3's fix making this possible at all), but `cost`/wall-clock for that leg are a floor, not the true total; see LT-166 (root cause found 2026-09-20: every `scan.leaf` dispatch on this host redundantly reloads/refires the full template corpus, not a real per-leaf re-test) as the likely reason it needs more turns than it should.

**Verdict: for the first time, orchestrator mode beats the deterministic baseline (13/22 vs. 10/22) instead of regressing relative to it** — DVWA and vAPI both improve on their prior numbers, Juice Shop ties, and crAPI now finds `nuclei-http-missing-security-headers`, a check *neither* deterministic crAPI invocation (idor-only or authbypass-only) found. Per doc93's own standing rule, this clears the gate for the first time since M5 was built — see LT-162 (now closed) and doc93's M5 section for what this means for `hackerfive agent`'s status going forward, and LT-167 for the scope-model gap this exercise surfaced along the way.

## Live Testing — `hackerfive agent` reasoning verification, isolated lab targets (2026-09-20, post-LT-167)

Ran the real `hackerfive agent` (model `deepseek/deepseek-v4.1-flash`, `--budget 0.30`, 20-min wall-clock cap per target) against DVWA, Juice Shop, vAPI and crAPI one at a time, all other lab containers stopped, and read each run's full turn history (`Result.History`: per-turn `rationale` + `result_summary`) rather than only the fixture score. Methodology otherwise matches the [doc93](93-implementation-plan-agent-orchestrator.md) baseline (same fixtures, `--recon-depth` active / crAPI full, vAPI `--templates ./templates/`, crAPI `.nuclei-templates-cache`), except for the bare-host scope files LT-168 forced.

| Target | Expected prefixes found | Iterations | Cost | Ended |
|---|---|---|---|---|
| Juice Shop | 2/5 (= baseline) | 1 | $0.0003 | clean, 18m (one 3,745-template `misconfig` leaf) |
| vAPI | 5/5 (= baseline) | 8 | $0.0096 | **exit=1** — two consecutive 4-min `NextAction` timeouts at turn 9 (LT-173) |
| DVWA | 4 (= baseline 4) | 9 | $0.0053 | 20-min cap, partial result kept |
| crAPI | 1/2 (baseline 2/2) | 4 | $0.0065 | 20-min cap; `misconfig` leaf (the one producing the missed prefix) still pending |

**Headline: the score reproduces the baseline, but almost none of it is the LLM's doing.** The reasoning read-through is the useful part — ten items below, ranked by value. Data, not opinion, in each. All read-only observations; no code was changed this round.

- **LT-172 (IMPLEMENTED + live-verified 2026-09-20/21, branch `fix/agent-orchestrator-lt168-171-172`) — the LLM adds latency, not judgement, for the cheap deterministic part of the tree.** vAPI turns 1-6 (five single-template leaves plus the `netservice` port check) and DVWA turns 1-8 (all single-template leaves) each finished their scan in 0.1-1.6s with 0 findings, each preceded by a 1-4 min `NextAction` round trip. DVWA's model walked leaves in literal numeric order (leaf-1 … leaf-8; rationales on turns 4-8: "Continue scanning the next pending high-confidence Apache CVE check…"). The one broad, high-yield leaf — generic `misconfig`, which produced *all* of DVWA's 13 and vAPI's 10 findings — was picked **9th** and **7th**. Tool diversity was zero on DVWA (9/9 `scan.leaf`; no `recon.refresh`/`registry.lookup`/`triage.rank`). **Fix:** a deterministic *fast lane* — dispatch pending leaves in a yield/cost priority order (capability leaves before single-template leaves, then confidence) with no LLM call, and call `NextAction` only at genuine decision points: fast lane drained, a finding that warrants follow-up, an `unresolved` leaf, thin recon. This is the "LLM only at the few genuine gaps" rule from [CLAUDE.md](../CLAUDE.md) and answers LT-165 item 2's open question ("skip `NextAction` when one viable leaf — needs eval data") with data: on vAPI+DVWA 14 of 17 dispatched turns were exactly this shape. Expected effect: vAPI 14 min → ~3, DVWA bounded by scan time not LLM latency. Pairs with LT-171 (what the model sees at the points where it *is* called). **Shipped:** `orchestrator.Config.FastLane` (CLI `--fast-lane`, default true; `--fast-lane=false` restores one-model-call-per-turn; the Web UI agent path sets it on). A leaf is fast-lane when it is pending with a detector and is either a raw template-ID leaf (`registry.LeafClass == "templates"`) or a parameter-free sweep (`misconfig`/`netservice`/`tls`); those run in `Priority` order (the decision engine's own `leafPriority`) with no `NextAction` call, each leaf at most once, recorded as `TurnRecord.Auto` turns and counted in the new `Result.FastLaneTurns` (they count toward neither `MaxIterations` nor the `MinIterations` floor — the floor stays about the model's own turns, or a drained fast lane would satisfy it instantly and re-open LT-162's early-stop). idor/authbypass/ssrf/sqli/businesslogic leaves, `unresolved` leaves, recon.refresh, triage and stop remain model decisions. A cancelled context is checked each fast-lane iteration. **Live, port-pinned scope (the LT-168 form), same model, one target at a time:** DVWA — before: 9 model turns, hit the 20-min cap, exit 0 with 4 fixture prefixes; after: **0 model turns, 11 fast-lane turns, $0, exit 0, all 11 leaves done, same 4 prefixes** (1001s, of which 16 min is the one broad `misconfig` scan — see LT-179). vAPI — before: 8 turns, exit 1 after two 4-min stalls, ~14 min; after: **0 model turns, 16 fast-lane turns, exit 0, 133s, 5/5 prefixes**. Juice Shop — 1 leaf, fast lane, 0 model turns, 2/5 (unchanged; the 3 misses are the stale-fixture / probe items already logged). **Consequence worth knowing:** on DVWA/vAPI/Juice every leaf is parameter-free, so `hackerfive agent` there is now fully deterministic and *no longer exercises the LLM at all* — these labs stopped being an agent-reasoning test; crAPI (endpoint-specific idor leaves) is the one that is. Tests: `TestIsFastLaneLeaf`, `TestNextFastLaneLeaf_HighestPriorityFirstThenTreeOrder_SkippingTried`, `TestHasActionableLeavesExcept_IgnoresTriedPendingLeaves`, `TestRun_FastLane_RunsParameterFreeLeafWithoutAskingTheModel` (0 `NextAction` calls), `TestRun_FastLaneOff_StillAsksTheModelForTheSameLeaf`, `TestRun_FastLane_CancelledContextEndsTheRunWithAnError`. **Not done:** the "a finding that warrants follow-up" decision point (a fast-lane finding never pauses the lane; the model sees it only when the lane drains) and a yield-ordered lane that puts the broad sweep ahead of single-template leaves regardless of `Priority`.
- **LT-171 (IMPLEMENTED + live-verified on crAPI 2026-09-21, branch `fix/agent-orchestrator-lt168-171-172`) — the orchestrator prompt is blind to the three things it would need to reason.** `buildOrchestratePrompt` (`pkg/llmfallback/orchestrate.go:170`) renders leaves as `id/target/detector/status/confidence/rationale`, tools, and history; a `scan.leaf` turn's result is only `"N finding(s), M log line(s)"`. So: (1) **no finding detail** — the model never learns a finding's ID/severity/target/whether it's new, so it can't chain (`.env` exposed → try the leaked creds; 401 on an API → `authbypass`; `/metrics` → infra recon) and `triage.rank` has nothing to be chosen *for*. **Live proof of the damage — crAPI turns 3-4:** both dispatches returned `"1 finding(s)"`; the finding was the identical `nuclei-openresty-detect-0` info hit re-firing (LT-166), not an IDOR result — yet turn 4's rationale states "leaf-24 and leaf-23 already confirmed IDOR on the /workshop/api/mechanic API", a false belief built from a bare count. (2) **No recon facts** — tech+versions, endpoint list, open ports, `AppSurface`/WAF/uniform-wall verdict never reach the model, so `recon.refresh` is blind and any `script.explore` script has nothing concrete to ground on (the design premise of that tool — JWT alg-confusion, ID-space decoding — needs the endpoint/token facts). (3) **Leaf detail is buried** — an `idor` leaf's `EndpointTemplate` is only visible if the rationale string happens to embed it. **Fix:** three compact, capped, read-only digest blocks in the prompt (findings digest with a per-turn "N new / M duplicate", recon digest, leaf key fields). Touches no invariant: the model still can't write a `Finding`/`Severity`/`Confidence` (doc02 Design Principle 5; LT-157's evidence-gate still applies). Also make `runScanLeafOnce`'s summary dedupe against findings already seen so a corpus re-fire reads "0 new". **Shipped:** `NextAction` now takes a `llmfallback.RunDigest`; `buildOrchestratePrompt` renders (1) each leaf's `endpoint=` template when it has one, (2) a `recon facts` block (technologies with host+confidence, app-surface verdict, API spec, endpoint counts, JS-secret count, uniform-wall hosts, up to 5 truncated recon warnings; built by `pkg/orchestrator/digest.go`, unions tech across `recon.refresh`), (3) a `findings confirmed so far (distinct)` block (id/type/severity/target, capped at 25, overflow counted), and (4) history that collapses `Auto` turns to a count so a long fast lane can't push the model's own decisions out of the 10-turn window. The system prompt now says the tree, recon facts and findings list are ground truth and that a count in a turn summary is never evidence of a specific finding. `scan.leaf` results now read `N new finding(s) [id severity target; …], M repeat(s) of already-recorded findings ignored, K log line(s)`, and a repeat is neither appended to `Result.Findings` nor re-streamed via `OnFinding` (this also removes LT-166's *symptom* from the reported output — the redundant re-scan itself is still open). All prompt text is lab-neutral. Every turn is also logged via `OnLog` with the model's rationale (`turn N: scan.leaf <id> (why: …) — <result>`; also LT-170 (b)). **Live, crAPI:** before — turn 4 claimed "leaf-24 and leaf-23 already confirmed IDOR" from a bare `1 finding(s)` that was a re-fired info detection. After — the model's turn 1 picked the `mechanic_report` idor leaf (9 real `idor-*` findings), turns 3-5 read `0 new finding(s), 1 repeat(s) of already-recorded findings ignored`, and its rationales now cite what is actually on the findings list ("the sibling …/mechanic_report endpoint already confirmed 9 high-severity IDORs" — true, and grounded; turn 5 misstates 9 as 8). Turn 2 chose an `authbypass` leaf that had no `protected_paths` and skipped, so the digest did not steer it away from a leaf recon had no candidate for (see LT-180). Tests: `TestBuildOrchestratePrompt_ShowsEndpointTemplateReconFactsAndFindings`, `…_EmptyDigestSaysSo`, `…_DigestSectionsAreBounded`, `…_AutoTurnsCollapseToACount`, `TestRun_ModelSeesReconDigest`, `TestReconDigest_*`, `TestScanLeafSummary`, `TestDispatchScanLeaf_RefiredFindingIsNotRecordedTwice`. **Not done:** a per-turn "new vs already-known" flag on each digest finding, an endpoint list in the recon digest, and any A/B measurement of whether the richer prompt changes leaf choice (one crAPI run is anecdote, not an eval).
- **LT-173 (✅ fixed 2026-09-21, branch `feat/llm-finding-capability-phase0`; original diagnosis below) — one LLM stall kills an otherwise healthy run; the decision call has no output cap.** vAPI: turn 9's `NextAction` timed out at 4m0s, retried once, timed out again → `Run` returned `orchestrator: NextAction: … context deadline exceeded`, exit=1, with 10 findings in hand and 13 leaves still pending (`triage.rank` had run once on turn 8). crAPI's turn 5 timed out the same way (4m0s). `chatRequest` (`pkg/llmfallback/client.go:214`) is `{model, messages, temperature}` only — **no `max_tokens`, no reasoning-effort control** on a ~150-token JSON decision, so a reasoning-capable model can burn minutes on hidden reasoning tokens (the likely cause of LT-165's 60-150s+ latency, not just prompt size). **Fix:** (a) pass `max_tokens` (~600) and, where the provider supports it (OpenRouter's `reasoning` object), low/disabled reasoning effort for the orchestrator's decision calls — verify per-model support before relying on it; (b) on a `NextAction` failure after its one retry, *degrade* rather than error-exit: fall through to LT-172's fast lane (or stop cleanly, exit 0, "LLM unavailable — N leaves left undispatched") and still run `triage.rank`. Matches the standing preference for logged-warning-over-blocking on unattended paths. **Shipped:** (a) `NextAction` runs under an output envelope — `max_tokens` 2048, OpenRouter `reasoning: {effort: "low"}` (frontier tier only; overridable with `HACKERFIVE_DECISION_REASONING_EFFORT`, empty = send no `reasoning` field), and a 120s per-attempt deadline instead of 240s (a stalled call now costs 4 min including the client's one retry, not 8). `max_tokens` is deliberately generous because on most providers it covers hidden reasoning *and* the answer (OpenRouter's own docs), so a tight cap on a reasoning model comes back empty with `finish_reason: "length"`; that case is now an error (`errTruncatedResponse`, cost still counted) rather than an empty decision. (b) After `MaxConsecutiveLLMFailures` (2) consecutive failed decision calls `Run` stops asking the model, runs whatever the fast lane can still do, and ends cleanly with `Result.Degraded` set (reason plus how many leaves were left because they need a decision) instead of returning an error with findings in hand; a cancelled context still errors, and one failure followed by a success resets the count. **Live probe** (`TestLive_DecisionEnvelope`, gated on `HACKERFIVE_LIVE_LLM=1`, 2-3 trials per arm so directional only): `deepseek/deepseek-v4.1-flash` mean 12.6s → 7.7s and ~40% cheaper, 2/2 valid decisions in both arms; `openai/gpt-5.6-luna` 3.3s → 2.7s, 3/3 valid in both. **Not reproduced:** the 4-minute stall itself — neither model stalled on this small prompt, so whether the cap *prevents* the stall is unproven; the degrade path is what guarantees a stall no longer kills a run. Tests: `TestCompleteWith_*`, `TestDecisionCallOpts_*`, `TestRun_ModelFailsRepeatedly_DegradesInsteadOfErroring`, `TestRun_Degraded_CountsLeavesThatNeededADecision`, `TestRun_SingleTransientModelFailure_IsRetriedNotDegraded`, `TestRun_NonConsecutiveModelFailuresDoNotDegrade`, `TestRun_ModelErrorWithCancelledContext_StillErrors`.
- **LT-168 (FIXED 2026-09-20, commit 1f36f2f; was a regression that blocked the eval harness) — a port-pinned scope entry silently disables recon Wave 2.** LT-167's fix made `Scope.Allowed` honor `host:port`; but `filterScope` (`pkg/recon/passive.go:339`) checks every Wave-1 candidate as `Allowed("https://" + bareHost)` — port 443 — so under `--scope localhost:3000` the seed host is rejected, `inScope` is empty, and `runWave2` returns at `active.go:79` (`len(inScopeHosts)==0`) before dnsx/naabu/httpx run at all. **No warning is emitted** (the recon result's `warnings` list only carries the LT-35/WHOIS lines); `hackerfive agent` then sees an empty tree and exits 0 (LT-170). Reproduced live: same target, `--scope localhost` → 4 tech facts, host found; `--scope localhost:3000` → 0 tech, 0 hosts, `out_of_scope: [localhost]`. **This is exactly what `tests/eval`'s `hostScopeEntry` (LT-167's own fix) now writes**, so `TestOrchestratorEvalHarness`/`TestAgentEvalHarness` run recon-blind for every non-default-port target (Juice :3000, vAPI :8000, crAPI :8888) — the 2026-09-20 baseline predates that change and can't be reproduced as documented. **Fix:** the seed target already passed the full-URL `Allowed(target)` check at `recon.go:445`, so the seed's own host:port should bypass `filterScope` and reach httpx (the `runWave2` doc comment already says "target's own host:port is always included"); add a `ReconResult` warning whenever Wave 2 is skipped for lack of in-scope hosts; and skip naabu's top-100 port sweep when the scope pins a port (it probes ports outside the pin). Regression test: recon against an `httptest` server on a non-default port with a port-pinned scope. **Fix as shipped:** `filterScope` now takes the seed hostname and neither drops it silently nor reports it out of scope (Run already authorized its exact host:port); `runWave2` probes just `host:port` with httpx when the seed is authorized only at its pinned port, skips dnsx and the naabu top-100 sweep for it (the pin does not authorize other ports), and emits a `wave2: --scope pins … (LT-168)` warning. Tests: `TestRun_PortPinnedScope_ProbesOnlyThePinnedOrigin` (httpx gets exactly `host:port`, no naabu, seed not in `out_of_scope`, warning present) and `TestRun_PortPinnedScope_OtherPortStaysOutOfScope` (the exception does not widen scope). Not fixed here: LT-169 (port-blind fact attribution) is what makes a *bare*-host scope report a neighbouring lab's stack; a pinned scope now avoids it for the seed because httpx sees only that origin.
- **LT-169 (open, design) — Wave 2 is port-blind for everything but the seed, so a shared host cross-contaminates facts (the real root cause behind LT-167's symptom).** With a bare-host scope and target `localhost:3000`, recon reported **Apache 2.4.25 / PHP / MySQL** — DVWA's stack on ports 80/3306 — as Juice Shop's: the bare `localhost` scope entry is added to httpx's input (default ports) and naabu sweeps top-100 ports of the host, then every `HostFact`/`TechFact`/`PortFact` is keyed by hostname only (`PlanNode.Target` is hostname-only end to end — see LT-167's own note on why threading a port through the tree was rejected). LT-167 fixed the scope layer; the fact layer still merges origins. Real-world relevance: any target that shares a host across ports (8080/8443 admin vs app) gets mis-attributed tech → wrong leaves. **Fix (needs a design pass):** key recon facts by origin (`scheme://host:port`), or restrict httpx/naabu to the seed's own origin when the seed has a non-default port. Until then the isolated-run protocol (stop every other service on the host) remains mandatory for lab evals.
- **LT-174 (open) — a leaf whose template never loaded is marked `done` with "0 finding(s)", indistinguishable from tested-and-clean.** vAPI turns 1-6: each specific-template leaf logged `fast template load did not account for every requested id — reloading the full corpus (F4)` → `loaded 0 nuclei-compatible … 29 filtered by tag` → `scan finished in 100-900ms, 0 raw finding(s)`, then `planexec/executor.go:313` set `StatusDone` because nothing errored. Cause here: `templates/index.json` is built over the synced corpus but the eval scenario passes `--templates ./templates/` only, so the leaf IDs can't load. The wider hazard is any index/corpus drift (a stale index after a re-pin). **Fix:** a per-leaf execution receipt (templates loaded, requests sent) — a specific-template leaf that loaded 0 templates becomes `skipped: template <id> not loadable in the loaded dirs` (never `done`), and the receipt goes in the turn summary so the model and the human report can tell "clean" from "didn't run".
- **LT-175 (open) — the JS-join step cross-multiplies query suffixes into junk IDOR leaves.** crAPI's tree had 12 `idor` leaves; only 3 (`mechanic`, `download_report`, `mechanic_report`) are plausible IDOR surfaces, and at least 7 are nonsense — `/identity/api/auth/{login,signup,unlock,verify,check-otp,forget-password,v4.0/user/login-with-token}?report_id={{id}}`. Mechanism: `pkg/recon/jsstatic.go:663-677` appends **every** `?x=` suffix constant found in the bundle to **every** verified base path (`epURL := pairURL + suffix`), and `verifyJSJoinCandidates` verified the *path* against a prefix canary, never that the parameter does anything. Each junk leaf costs one `NextAction` turn (1-4 min) and, until LT-166 is fixed, a full corpus re-fire. **Fix:** verify parameter effect before emitting a suffixed candidate (a differential response between two `report_id` values, or vs. the param-less path), or only join a suffix to the base constant adjacent to it in the bundle.
- **LT-170 (partly done — (b) shipped, (a) and (c) open) — `hackerfive agent` can't show you its reasoning or its blindness.** (a) An empty tree exits 0 with only `no pending/unresolved leaves remain — stopping`; `plan`'s `emptyPlanDiagnostic` (LT-60) and `ReconResult.Warnings`/`AppSurface` are never printed by `agent`, so LT-168's silent blindness looks like a clean run. (b) `-v` prints recon waves and scanner progress but **no per-turn line** — a 20-minute run's reasoning is only visible in the final JSON (the Web UI's session log shows it; the CLI has no equivalent). (c) Every scanner log line is printed twice (`loaded 3745 nuclei…` then `agent: [info] loaded 3745 nuclei…`) — the loader's own stderr plus the `OnLog` mirror. **Fix:** print recon warnings + `AppSurface` + the empty-plan diagnostic; one stderr line per turn (`turn 3 scan.leaf leaf-23 — <rationale> → <result>`); drop the duplicate sink. (b) is done: every turn, model or fast-lane, logs one `OnLog` line with rationale and result (commit with LT-171/172).
- **LT-166 (still open — new evidence).** crAPI turns 3 and 4 each ran a 1m30s-1m46s scan for an `idor` leaf and returned only the redundant `nuclei-openresty-detect-0` — the corpus re-fire, not the leaf's own check — and 3 of the 12 raw crAPI findings are that one finding three times. See LT-171: this isn't only wasted time, it fed the model a false "IDOR confirmed" belief. **Partial mitigation 2026-09-21 (LT-171):** the orchestrator now drops a finding it has already recorded (same ID+type+target+description), so a re-fire no longer inflates `Result.Findings`, no longer re-streams, and reads as "repeat(s) ignored" to the model. The wasted work is unchanged: crAPI turns 3-5 each still re-ran the whole scan for a leaf that found nothing new.
- **LT-177 ✅ resolved as stale-fixture — closes LT-138 item 2.** The premise ("`misconfig-method-*` fires in the CLI baseline") no longer holds: the *deterministic* CLI path also emits no `misconfig-method-*` against DVWA or Juice Shop today (`scan --detector misconfig --templates <empty>` → 5 and 4 findings, none `method`; vAPI still does). The cause is LT-69/73's anti-FP rule (skip a 2xx/3xx method response that matches the plain-GET baseline) applied to a catch-all (Juice Shop) and a login-redirect (DVWA) root — the rule is doing its job. So the agent path was never the problem. **Action:** re-baseline `tests/fixtures/expected-findings/{dvwa,juiceshop}.json` — both still list `misconfig-method-` (recorded before 2026-09-07), meaning every eval table's denominator carries two prefixes no mode can reach.
- **LT-176 (open, small) — the model inflates a rationale into a fact.** vAPI turn 1: "CVE-2024-4577 … **exact version match** with medium confidence" against PHP 7.4.33 — that CVE is PHP-CGI on Windows, PHP 8.1-8.3; the leaf's own rationale says only that the template was "ranked among the most relevant" for the fact. Same failure class as LT-157's triage overclaim. **Fix:** feed the leaf's deterministic version-gate verdict (or "no range known") into the prompt line, and apply LT-157's `evidenceGateText` idea to `NextAction` rationales (flag an unhedged "exact match"/"confirmed" phrase with no supporting leaf field). Also: the decision engine still emits CVE leaves whose affected range excludes the observed version whenever the template carries no derivable range (Phase 8 Step 4 gates only those that do).
- **LT-178 (open, small) — `misconfig`'s `checkWebmin` probes `<host>:10000` on every scan** regardless of the target's port (all four runs: `request to http://localhost:10000 failed`). One wasted request per run on a lab; on a real target under a port-pinned scope (LT-167) it's an out-of-scope port. **Fix:** only run it when recon's port list shows 10000 open, or when the seed itself is `:10000`.
- **LT-179 (open) — the broad `misconfig` leaf is a full-corpus scan and is the whole wall-clock.** With the fast lane removing model latency, what is left is scan time: DVWA's run was 1001s, of which one `misconfig` dispatch took 16m0s (`scan finished in 16m0s … 3910 template(s)`, ~4 templates/s at the default 10 req/s cap) and the other ten leaves took ~1s each; Juice Shop's single leaf was the same 16-17 min. It is a single blocking dispatch with only a `217/3910 template(s) completed` line for feedback, no per-leaf timeout, and — because every later `scan.leaf` reloads and re-fires the corpus (LT-166) — the same cost can recur. Options: run the corpus once per host and let leaves select from its results (the real LT-166 fix); or bound a leaf dispatch with its own timeout that degrades to `skipped: timed out` instead of consuming the run. (crAPI at `--rate-limit 60` took ~2594s total including 5 model turns, so the cap, not the model, dominates.)
- **LT-180 (open) — the model still picks leaves that cannot run.** crAPI turn 2 chose an `authbypass` leaf and the dispatch came back `skipped: … no --protected-paths given and recon/I4 found no usable candidate`: the leaf's own status is `pending`, so nothing in the tree or the LT-171 digest says it is blocked on a required field, and a model turn (plus the per-turn latency) was spent to learn that. **Fix:** surface runnability in the leaf line (`needs=protected_paths` / `blocked: <reason>`, from `fieldMissDetectors` + the leaf's own required-field state), and/or keep a leaf that skipped for a field miss out of the offered list for the rest of the run (the fast lane's `tried` set already does this for its own leaves).
- **LT-181 (open, environment) — `pkg/scriptexec`'s `TestExecute_BenignPythonScript_ReachesInScopeTargetThroughProxy` fails on this Windows/WSL2 + Docker Desktop setup, on unmodified `main` too.** The sandbox container's egress proxy returns `502 Bad Gateway` for an in-scope `httptest` upstream (verified on a clean `main` worktree, 2026-09-20/21; the first run also had to pull `python:3.12-alpine`). Every other package passes under `go test ./... -race`. Likely the proxy container reaching a listener bound on the WSL2 side, not a logic bug — needs a look before this test can be trusted as a gate on this machine.
- **LT-182 (open, design proposal — the systematic follow-on to LT-171/172; full write-up in [94-llm-finding-capability-strategy.md](94-llm-finding-capability-strategy.md)).** LT-171/172 made the agent cheaper and less blind but not more productive: three of four labs no longer call the model, and where it does run (crAPI) it only orders leaves recon and the registry already built; the misses (LT-164, LT-180) are upstream of ordering. Proposal: six LLM jobs, each schema-bound with a deterministic validator, none able to create a Finding or raise severity/confidence (LT-157): **J1** app model (resources, ownership, roles, ID params, from recon + response-shape skeletons), **J2** hypothesis engine (detector + filled parameters, or a `script.explore` spec, with expected-if-vulnerable and a negative control), **J3** finding-driven follow-up, **J4** skeptic/counterevidence pass with negative-control re-issue (downgrade, never delete), **J5** coverage ledger + a state-based stop that replaces the `MinIterations` floor, **J6** root-cause grouping + report drafting (draft only). Cross-cutting: lab-neutral skill packs. Sourced from a source-level read of Strix (threat model, coverage ledger, counterevidence/closure discipline, dedup judge, skills), Cyber-AutoAgent (hypothesis format, observation-is-not-a-vulnerability rules, event-triggered reflection) and HexStrike (nothing LLM-shaped: its server has no LLM-client code). **Phase 0 comes first:** an ablation harness on `tests/eval` (deterministic vs +J1/J2 vs +J4 vs +J5; recall, FPs against the lab's documented list, $/finding), the LT-173 reliability envelope for every new call, and response-shape capture in recon (key names/types only). Suggested order: 0 → J1+J2+skills → J4 → J5 → J3 → J6. **Phase 0 status (2026-09-21, branch `feat/llm-finding-capability-phase0`):** (1) LT-173 envelope + degrade — done, see LT-173. (2) Response-shape capture — done: `EndpointFact.ResponseShape`, a bounded Wave 3 pass (`pkg/recon/shape.go`, 25 GETs max, in-scope, GET-only, operator headers applied, no redirects) storing the JSON structure with values replaced by type names; object keys that look like data (emails, UUIDs, numeric ids) collapse to `<key>`. Live-verified against a local JSON site (both endpoints shaped, no value appeared anywhere in the recon output). Found live and fixed: katana crawl facts carry a status but no content type, so the first version probed nothing on a real crawl; candidates are now tiered (spec/known-JSON, API-looking path, the rest). **Nothing consumes the shape yet** — it is J1's input. (3) Ablation harness — built, not yet run against live labs: `hackerfive agent --no-model` (zero LLM calls, fast lane only, no API key needed; the control arm), `tests/eval/{grade,ablation_arms,ablation_test}.go` (arms x labs x N runs, JSONL per run under `tests/eval/results/` which is gitignored, comparison table with min-max ranges, results never merged across models — the configured model changed between sessions without any code change), and `tests/fixtures/known-vulns/crapi.json` (7 documented crAPI vulnerabilities from the Step E table, verdicts as recorded 2026-09-08). Run: `HACKERFIVE_ABLATION_RUNS=3 HACKERFIVE_ABLATION_LABS=crAPI go test -tags eval ./tests/eval -run TestAblation -v -timeout 6h` with only that lab's containers up. **Why a second fixture kind:** `expected-findings/*.json` lists prefixes the deterministic scan already produces, so every arm passes it and it cannot show what a model adds. **Open (LT-183).**
- **LT-183 (open) — Phase 0's measurement exists but has produced no numbers yet.** (a) **[done 2026-09-21 — see the update at the end of this item] No baseline table:** the first run (`TestAblation`, crAPI, `HACKERFIVE_ABLATION_RUNS=3`, model pinned) has not been done; until it has, there is no `no-model` vs `fast-lane+model` comparison to build J1-J6 against, and a 1-run model row is an anecdote. (b) **Ground truth only for crAPI:** DVWA, vAPI and Juice Shop have no `known-vulns` fixture (their `OrchestratorScenario.KnownVulnsFile` is empty, so those labs grade on `expected-findings` alone and cannot show a model's contribution). They must be sourced from each lab's documented challenge list and live-verified, not written from memory. crAPI's own list omits mass assignment, NoSQL/SQL injection and OTP rate limiting (no finding class or ID exists to grade them) and its `baseline` values are the 2026-09-08 verdicts, several since addressed by LT-95/96/132 and not re-measured. (c) **`crapi-bfla-delete-video` is unreachable by any agent arm** (`mutatebfla` needs `--allow-mutating-bfla` and an operator-supplied, ownership-confirmed path), so it will read as a miss in every arm; either drop it from the agent comparison or give the agent a gated route. (d) **`TestOrchestratorEvalHarness` still carries its own copy of the JSONL parser** (`agentStreamEvent`/`orchestratorResult`); `grade.go`'s `ParseAgentStream` is the shared one — fold the old test onto it. (e) **Model drift:** the `.env` model is now `openai/gpt-5.6-luna`; LT-162/171/172's numbers were `deepseek/deepseek-v4.1-flash`. Any comparison against those earlier tables is across models. (f) `Unlabeled` counts findings matching no expected prefix and no known vuln — a candidate-FP signal, not a measured FP rate; a real precision number needs each lab's *true* finding list, which nobody has. **Update 2026-09-21 — first crAPI baseline run** (3 arms x 3 runs, bundled templates, `--rate-limit 60`, `openai/gpt-5.6-luna`, 9/9 runs completed, table in doc94 "First baseline"): control 6 findings / 1 of 2 expected / 1 of 7 known, 22 s, $0; fast-lane+model and model-every-turn both 15 findings / 2 of 2 / 2 of 7, about $0.017-0.020 and 4 minutes. The model's whole contribution is dispatching one real `idor` leaf out of 12; it found nothing no leaf already covered. **(g) [done 2026-09-21 — see the second update below] Missing control:** a fourth arm, `no-model` + run every runnable leaf, is needed before crediting that to reasoning rather than to the fast lane's exclusion rule. **(h)** Five of seven known vulns were missed by every arm because no leaf (or no usable leaf field) exists — LT-184, LT-180, and the missing recon candidates. **Second update 2026-09-21 — the fourth arm (`no-model+all-leaves`: `--no-model --run-every-leaf`) was added and run** (3 runs, same settings): 15 findings / 2 of 2 / 2 of 7, 194 s (191-198), $0 — the same as both model arms on every stored measure, 20-25% faster, free. On crAPI the model's leaf choice adds nothing a run-everything policy does not; the value has to come from creating leaves (J1/J2). Caveat: 14 runnable leaves is a small tree; a large tree would make run-all expensive, so selection is unproven rather than useless. **(i) Finding IDs are not recorded:** `RunRecord` stores counts and known-vuln hits, so "same findings" across arms is inferred, not shown; store the sorted finding IDs per run and diff them in the summary. **(j)** A larger-tree lab (Juice Shop or a real scope with several hundred leaves) is needed before concluding anything about selection at scale.
- **LT-184 (open, observed on the 2026-09-21 crAPI baseline; cause not yet traced) — recon builds 12 `idor` leaves from one `report_id` query key, spread over unrelated paths.** The control's undispatched leaves were `idor …?report_id={{id}}` on `/identity/api/auth/{login,signup,forget-password,unlock,verify,v3/check-otp,v4.0/user/login-with-token}` and `/workshop/api/mechanic{,/download_report,/receive_report,/service_requests}`, plus the real `/workshop/api/mechanic/mechanic_report`. Only the last is an object endpoint. The key looks like it was attached to many paths from one real observation (suspects: LT-95's empty-ID-key templating, or LT-164's service-prefix + API-path join; not confirmed). **Cost:** a decision turn each for a model, or 12 scans for any run-everything policy, and a misleading candidate list for anything that reasons over it. **Fix:** find where the key is attached; require per-path evidence (the key observed on that path, or documented for it in a spec) before an `idor` leaf is built.
- **LT-185 (open, design + harness; the measurement half of J1/J2/J5) — a missed known vulnerability is reported as "missed", not as *where the pipeline lost it*.** The baseline says 5 of 7 known vulnerabilities were missed by every arm, but the reasons (no leaf built, LT-180's empty `protected_paths`, unreachable by design) were worked out by hand from a tree dump, and one is still "unverified". Without the stage, J1/J2 cannot be aimed at a measured gap and their success cannot be scored ("moved 3 vulnerabilities from stage 1 to a finding"). **Stages, in pipeline order:** (0) recon never observed the endpoint; (1) observed, but no leaf was built for it (registry/decision engine produced nothing, or attached the wrong parameter, cf. LT-184); (2) leaf built but blocked on a required field (idor `EndpointTemplate`, authbypass `ProtectedPaths`, ssrf params — LT-138/LT-180); (3) runnable but never dispatched; (4) dispatched, detector raised nothing (detector limit or wrong parameter); (5) a finding was raised but does not match the known entry (ground-truth mismatch); (6) unreachable by design (gated mutating class). **Build:** classify each `known-vulns` entry from what one run already produces — `Result.Tree` leaf targets/detectors/fields/status and the history's skip reasons — plus one new input the stream does not carry today: recon's endpoint list (add a `recon` stream event or `--dump-recon <file>`: URL, method, source, auth flag — no values). Report the furthest stage reached per known vulnerability and arm, and a "lost at stage N" histogram per arm. **Keep the classifier a library, not harness-only code:** the same function over recon + tree + history with no ground truth is J5's coverage ledger (endpoint x class rows, each carrying a stage), so the harness proves it on labs and the agent uses it on real targets. **Expected first result** (hypotheses to check, not conclusions): shop-orders BOLA, vehicle-location BOLA and the `contact_mechanic` SSRF land at stage 0 or 1 (which of the two is exactly what J1's design depends on); authbypass-jwt at 2 or 4 (settles LT-180's "unverified"); DELETE-video at 6. **Second mode:** also report a stage for *every* recon endpoint against the class list, so the size of the miss population can be estimated on labs and targets that have no ground truth.
- **Also noted, low priority:** on loopback, naabu's top-100 sweep hits Wave 2's 1-minute cap every time (`wave2: naabu: hit the 1m0s wave time cap — results may be partial`), so every active-depth run spends ≥1m03s in Wave 2 with a possibly partial port list. And LT-165's latency is now measured three times across runs: `NextAction` calls of 30s-4m+, the only large cost on a $0.005-0.02 run.

**What did work, worth keeping:** crAPI turn 1 picked the real `mechanic_report` IDOR leaf first from world knowledge of the fingerprinted app ("crAPI is a deliberately vulnerable API…"), and `--recon-depth full`'s JS-join (LT-164) surfaced the endpoint end-to-end — the one place the model's choice beat tree order. No cross-target contamination in any finding set (every finding host matched its target, isolated); SIGINT at the cap was handled gracefully (partial `Result` with the error, streamed findings intact); `--allow-agent-scripts` skip-warning behaved as designed; total spend across all four targets was $0.0217.

**Status 2026-09-21:** LT-168 fixed; LT-172 and LT-171 implemented and live-verified (see their entries); LT-173 is still the run-killer — the crAPI re-run above died at turn 6 exactly as vAPI did before (`NextAction` context deadline after 4 min, exit 1, 5 model turns + $0.015 spent, 19 findings kept). With the fast lane it is now rarer (fewer model calls) but not gone. Do LT-173 next. **Suggested order (value ÷ effort):** LT-168 first (a regression that silently breaks recon and the eval harness; small), then LT-172+LT-171 together (the real agent-quality lever; medium-large), LT-173 (small-medium, removes the run-killer), LT-166 + LT-174 (correctness of what a `done` leaf means), then LT-175/170/176/178/177 opportunistically. LT-169 needs its own design pass. Re-run this exact protocol after LT-168+172+171 land and log it as a new dated entry against the [doc93](93-implementation-plan-agent-orchestrator.md) M5 baseline.

## Live Testing — G1 three-way eval comparison (2026-09-15)

[93-implementation-plan-agent-orchestrator.md](93-implementation-plan-agent-orchestrator.md) M5's actual gate evaluation: all three `tests/eval/` harnesses (`TestEvalHarness` deterministic baseline, `TestAgentEvalHarness` MCP-plan mode, `TestOrchestratorEvalHarness` this feature's new orchestrator mode) run back to back in one session, against the same four freshly-re-authenticated live lab targets, with a real `OPENROUTER_API_KEY` (`deepseek/deepseek-v4.1-flash`) and the operator's own configured spend ceilings (`HACKERFIVE_SPEND_CEILING_USD=0.10`, `HACKERFIVE_SPEND_CEILING_TOTAL_USD=1.20`).

| Target | Mode | Expected prefixes found | Unexpected (FP) | Cost | Wall-clock |
|---|---|---|---|---|---|
| crAPI | deterministic | 1/2 | 0 | $0 | 1m20s |
| crAPI | MCP-plan | 0/2 | 5 | $0.0019 | 2m38s |
| crAPI | orchestrator | 0/2 | 0 | $0.0108 | 3m13s |
| DVWA | deterministic | 3/10 | 0 | $0 | 15s |
| DVWA | MCP-plan | 4/10 | 0 | $0.0037 | 7m48s |
| DVWA | orchestrator | 3/10 | 0 | $0.0008 | 5m25s |
| Juice Shop | deterministic | 2/5 | 2 | $0 | 13s |
| Juice Shop | MCP-plan | 0/5 | 0 | $0.0009 | 2m9s |
| Juice Shop | orchestrator | 0/5 | 0 | $0.0042 | 3m53s |
| vAPI | deterministic | 4/5 | 0 | $0 | 2m22s |
| vAPI | MCP-plan | 3/5 | 1 | $0.0016 | 7m19s |
| vAPI | orchestrator | 3/5 | 1 | $0.0013 | 5m34s |
| **Total (22 prefixes)** | deterministic | **10/22** | 2 | $0 | ~4m10s |
| **Total** | MCP-plan | **7/22** | 6 | $0.0081 | ~19m54s |
| **Total** | orchestrator | **6/22** | 1 | $0.0171 | ~18m6s |

**Verdict: neither LLM-driven mode found anything the deterministic baseline missed, and both regressed recall relative to it**, while costing real money and 4-5x the wall-clock. See LT-162 above and doc93's M5 section for the full write-up and what this means for `hackerfive agent`'s status (lab/CLI-only, gate not met).

## Live Testing — G1 agent-driven eval (2026-09-10)

Real MCP-client-driven `recon`→`plan`→execute(→`findings.triage`) run against all four lab targets (`tests/eval/agent_run.go`/`agent_eval_test.go`, doc16 Step 6's G1). Full write-up: [90-research-hackerbot.md](90-research-hackerbot.md) §G1.

- LT-137 ✅ done 2026-09-10 — `plan` execution never applied doc15 Step 6a's tech-based template narrowing at all (`DerivedTags`/`Tags` left empty in both `tools_plan.go`'s `buildBaseExecConfig` and webui's plan-exec path), so every leaf ran the *entire* synced corpus — a first G1 run timed out on 3/4 scenarios from one misconfig leaf's 3+-minute corpus pass alone. Fixed: both paths now set `DerivedTags`, `planexec.executor.go`'s `runLeaf` unions each leaf's own detector-tag floor on top (mirroring `cmd/hackerfive/scan.go`'s existing `unionScanTags` composition). **Residual, honest limitation:** misconfig's floor tags are broad category words matching most of the corpus, so floor-only narrowing (no tech fingerprint) only trims ~25% — matches the CLI/webui paths' own long-documented behavior.
- **LT-138 (item 1 ✅ fixed 2026-09-19 for `pkg/orchestrator`; item 2 ✅ resolved 2026-09-20 as a stale fixture, not an agent bug — see LT-177)** — two agent-specific gaps surfaced against this item's original 2026-09-10 MCP-plan-mode observation, neither fixed at the time:
  1. crAPI's idor/authbypass leaves resolved *pending* but were *skipped at execution* (missing `--endpoint`/`--protected-paths`) under `--recon-depth active` (no Wave-3 crawl); I4's field-miss resolution never ran — the write-up hedged that `llmfallback.ResolveTreeLeaves`'s field-suggestion path "appears scoped" to an I3-*unresolved* leaf, not a *pending*-but-execution-blocked one, without pinning down the exact function (`ResolveTreeLeaves` is actually I4's *first* caller — leaf-shape resolution for a `StatusUnresolved` leaf — a different function from `pkg/mcpserver/tools_plan.go`'s `resolveFieldSuggestions`/`ResolveFieldMiss`, I4's *second* caller, which already existed and already called I4 for this exact 0-candidate case in MCP-plan mode — see `applyFieldSuggestion`'s own doc comment: an LLM-resolved field miss is deliberately "surfaced ... but never auto-injected into execution," so MCP-plan mode's behavior here was arguably already correct-by-design, not a bug, pending human review between `plan` and `execute`). **The real, unambiguous gap was `pkg/orchestrator`'s own `hackerfive agent` loop** (didn't exist yet when this item was filed): unlike MCP-plan mode, it had *no* field-miss handling at all — a `scan.leaf` dispatch blocked on a required field just skipped permanently, with no human turn in between to notice or resolve it. **Fixed:** `dispatchScanLeaf` (`pkg/orchestrator/orchestrator.go`) now detects a real (non-"excluded by operator") skip on a `fieldMissDetectors` leaf (`idor`/`authbypass`/`ssrf` — the set `fieldsuggest.Deterministic` covers) and calls new `resolveMissingLeafField`: a fresh, targeted recon probe of the leaf's own host, run through the exact same `fieldsuggest.Deterministic` `pkg/mcpserver`/`pkg/webui` already use. A single deterministic candidate auto-fills a copy of `cfg.BaseScanConfig` (no LLM call, no new gate — already unconditional in mcpserver/webui) and the dispatch is retried once, immediately, in the same turn. A genuine miss (0 or >1 candidates) is resolved via I4 (`cfg.Client.ResolveField`, a new method on the orchestrator's `LLMClient` interface, cost tracked against the tree's own spend ceiling) but — matching `applyFieldSuggestion`'s exact precedent — is only ever surfaced in the turn's result summary, never auto-applied: auto-applying a model's own guess at which endpoint to enumerate would fire real, consequential requests based purely on that guess with no human review, the exact gate doc90 Decision 5/6 requires before anything consequential runs. Tests: `pkg/orchestrator/fieldmiss_test.go` (deterministic single-candidate auto-fill and apply, genuine-miss I4 resolution never auto-applied plus its cost tracked, I4 escalation, a failed recon probe surfaced as a note, an end-to-end `dispatch`-level retry against a real HTTP server that ends `StatusDone`, and a non-`fieldMissDetectors` detector's skip passing through completely unchanged with zero extra recon calls).
  2. ✅ **resolved 2026-09-20 — the premise below is stale (LT-177): the deterministic CLI path also no longer emits `misconfig-method-*` against DVWA/Juice Shop (LT-69/73's anti-FP rule), so nothing agent-specific was ever wrong; the expected-findings fixtures need re-baselining.** Original write-up: the native `misconfig-method-*` check (`checkDisallowedMethods`) never fired against DVWA/Juice Shop/vAPI through the agent path despite firing in the CLI baseline against the same targets, identical `Concurrency`/`RateLimit`/`Timeout` defaults ruled out as cause; root cause not yet found — needs a follow-up live session with dedicated time (no Docker/lab-target access was available to investigate this via this pass).

## Live Testing — pre-v0.7.0-tag re-verification against www.aalberts.com (2026-09-10)

Full `recon --recon-depth active` → `scan --detector misconfig` → `triage --llm-assist` → `suggest --llm-assist` pass against the live, published-VDP `www.aalberts.com`, before tagging `v0.7.0`. `triage --llm-assist` worked cleanly (11 findings ranked, $0.0005). Two real bugs surfaced and fixed before tagging:

- LT-139 ✅ done 2026-09-10 — `misconfig-cors` conflated a literal wildcard `Access-Control-Allow-Origin: *` with genuine origin-reflection, both rated high/high — but a literal `*` alongside `Access-Control-Allow-Credentials: true` is spec-non-functional in every standards-compliant browser (Fetch/CORS spec), so the finding overstated severity for aalberts.com's actual (non-reflecting) case. Fixed: `checkCORS` now splits `echoedProbe` (genuine reflection, stays high/high) from `literalWildcard` (down-ranked to low/medium, description corrected). Test: `TestMisconfigCORS_LiteralWildcardWithCredentials_DownRanked`.
- LT-139b ✅ done 2026-09-10 — `SuggestedAction.Detail map[string]string` couldn't decode a real model response (`suggest --llm-assist` correctly proposed a `triage_group` action with an array-valued `finding_ids` field), so `decodeJSONResponse` failed the *entire* response and silently degraded to ledger-only — a good, safe proposal thrown away per the "degrade, never fabricate" contract, but a real capability loss. Fixed: widened `Detail` to `map[string]any`; prompt documents the array shape. Test: `TestSuggest_TriageGroupWithFindingIDsList_Decodes`.

## Live Testing — nettix.com.pe full-pipeline round (2026-09-11)

Built the binary and ran the full recon → decision-engine → scan → suggest → triage pipeline live against the real, owned `nettix.com.pe` estate (`recon --scope '*.nettix.com.pe' --recon-depth full` → 24 hosts; `plan --llm-assist`; `scan --detector misconfig --recon-file ... --narrow-by-tech` against the 8 non-DNS-only hosts; `suggest --llm-assist`; `triage --llm-assist`), specifically to find enhancement opportunities across recon efficiency, decision-engine determinism, LLM leverage, missed detection, and robustness. Full logs under `.engagements/owned-sites/logs/live-test-2026-09-11/`.

- **LT-144 (fixed 2026-09-11)** — `runJSStaticAnalysis` (`pkg/recon/jsstatic.go:313-325`) calls `agg.addEndpoint` for every `extractJSEndpoints` hit with **no `r.scope.Allowed(...)` check** — its own sibling function 15 lines above, `recordCloudBucketRef` (`jsstatic.go:298-306`), scope-checks for exactly this reason ("a page can reference any number of third-party buckets that have nothing to do with the target's own infrastructure") but the fix was never applied to the plain-endpoint path. Live-reproduced: a crawled page's `xmlns="http://www.w3.org/1999/xhtml"` (XHTML namespace declaration, not a link) became `EndpointFact{URL: "http://www.w3.org/1999/xhtml", Source: "js-static"}` — `extractJSEndpoints`'s absolute-URL branch (`jsstatic.go:109-120`) only checks `IsPlausibleURLPath`/`IsNonRouteAssetPath` (path *shape*, not host/scope). `SuggestIDOREndpointCandidates` (`suggest.go:160`) then templated it into `/{{id}}/xhtml`, and `registry.Resolve` (which trusts recon already scope-filtered, per its own doc comment) built a real, dispatchable `idor` leaf: `{Target: "http://www.w3.org", EndpointTemplate: "/{{id}}/xhtml"}` in the live PlanTree (`plan.json`'s `host:www.w3.org` node). Approved via Plan Preview (default-checked leaves) or driven through MCP, this fires real enumeration requests at an entirely unrelated third-party domain — a direct violation of CLAUDE.md's scope rule. **Fix:** add the identical scope check `recordCloudBucketRef` already has, right before `runJSStaticAnalysis`'s `agg.addEndpoint` call; on rejection, `agg.addOutOfScope(hostOnly(epURL))` like every other out-of-scope path in this codebase, so it's loud, not silent. **Applied**: `runJSStaticAnalysis` (`jsstatic.go:313-341`) now checks `r.scope.Allowed(epURL)` before `agg.addEndpoint`, calling `agg.addOutOfScope(hostOnly(epURL))` on rejection — same shape as `recordCloudBucketRef`. Regression test `TestRunWave3_JSStaticAnalysis_OutOfScopeAbsoluteEndpoint_NotDispatched` (`jsstatic_test.go`) reproduces the exact live `xmlns`/www.w3.org case and asserts the URL never reaches `result.Endpoints` and its host lands in `result.OutOfScope` instead. Full suite (`go build`/`go vet`/`go test ./... -race`) and `golangci-lint` clean.
- **LT-145 (fixed 2026-09-11)** — naabu found port **10000 (Webmin) open on 8 of nettix's 24 hosts** this round, and a first-party `checkWebmin` check already exists (`pkg/detectors/misconfig/detector.go:1223`) — but it's structurally unreachable: `interestingPorts`/`netserviceCheckedPorts` (`pkg/registry/decisionengine.go:218-226,240`) cover only 7 raw-TCP-protocol ports (ftp/telnet/mysql/postgresql/redis/elasticsearch/mongodb), so port 10000 gets **no leaf at all**, not even a visibility one; and even if it did, `resolveTechFact`'s misconfig leaf always targets the host's default HTTP(S) port (`reconHostBaseURL`), never `:10000`, so `checkWebmin`'s `doRequest(..., target, ..., "/", ...)` (detector.go:1224) would hit the wrong port anyway. Same gap plausibly applies to any other HTTP-on-a-custom-port admin panel. **Fix (simplest):** mirror `checkDolibarrOutdated`/`checkNextcloudStatus`'s existing "always try me, unconditionally" posture — have `checkWebmin` also probe `https://host:10000/` directly, self-contained, no registry/PlanTree plumbing needed (same pattern these already use to bypass the corpus-time-budget problem, LT-98). A more general fix (thread a "known HTTP admin port" table through `resolvePortFacts` into a misconfig leaf carrying the right port) would also cover a future product without a bespoke self-probe. **Applied**: `checkWebmin` (`detector.go`) now probes twice — its leaf's own target as before (unchanged, the rare reverse-proxied-onto-default-port case), plus an unconditional second probe at `webminPortTarget`'s rebuilt `<host>:10000` address, same scheme as the original target. Factored the original single-address logic into `checkWebminAt`; both probes' findings are merged. The `resolvePortFacts`/registry-plumbing alternative was not pursued — this self-probe needed no other file changed, consistent with `checkDolibarrOutdated`/`checkNextcloudStatus`'s existing posture. Regression test `TestMisconfigWebmin_AdminPort10000_ProbedEvenWhenMainSiteIsSomethingElse` (`tests/unit/detector_misconfig_test.go`) binds a real listener on `127.0.0.1:10000` serving Webmin while the main target serves unrelated content, and asserts the finding still surfaces (skips itself if port 10000 is unavailable in the test environment). Known limitation, not new: a self-signed cert on the `:10000` probe fails without `--insecure`, same pre-existing constraint every other HTTPS probe in this detector already has.
- **LT-146 (fixed 2026-09-11)** — `llmfallback`'s shared HTTP timeout (`requestTimeout = 240 * time.Second`, `client.go:50`, already raised from 60s→180s→240s over LT-25's history) still isn't enough: **2 of 6 real LLM calls timed out** in one `plan --llm-assist` run against the configured `deepseek/deepseek-v4-flash-0731` model — `ResolveLeaf` for `www.nettix.com.pe-leaf-10` ("reading response: context deadline exceeded") and the `VetPendingLeaves` plausibility-review call (same error). Total wall-clock for the LLM-assist phase was ~14 minutes for what should be ~6 calls; total spend was $0.0061 of the $0.10 ceiling, so cost wasn't the constraint, latency reliability was. Compounding this: **zero mid-call visibility** — the verbose log only prints on a leaf's completion/failure (`resolve.go`), so a slow/hung single-shot call (`PlanFromRecon`, `VetPendingLeaves` — both un-batched, one call for the whole tree) gives no "still waiting on X" signal, indistinguishable from a genuine hang. **Fix ideas:** a heartbeat log line when a call has been outstanding past some threshold (e.g. 30s); consider giving the single-shot whole-tree calls (`PlanFromRecon`, `VetPendingLeaves`) a *longer* timeout than the per-leaf ones, since they carry more input and currently share the one constant; or add one bounded retry for a bare timeout (not a real API error) before giving up. **Applied**: all three fix ideas landed. (1) `Client` gained a `WithLogCallback`/`logCB` seam (same shape as `idor`/`misconfig`'s existing one) wired into every frontend with a log sink today — CLI's `plan`/`triage`/`suggest` (to `cmd.ErrOrStderr()`) and webui's Plan Preview resolve + scan-launch field-fill (to `Job.AppendLog`); MCP's plan/triage tools have no log sink of their own yet (LT-131) and simply omit it, a no-op since `logCB` is nil-safe. (2) `requestTimeout` (240s) now covers only per-leaf/per-field calls (`ResolveLeaf` + its draft-template follow-up, `ResolveField`); a new `requestTimeoutLong` (420s) covers every single-shot whole-dataset call — not just the two that timed out live, but every call of that same shape (`PlanFromRecon`, `VetPendingLeaves`, `TriageFindings`, `Suggest`), since they'd hit the same wall on a large enough scan. (3) `completeLabeled`/`completeOnce` (`client.go`) wrap every real call in its own per-call `context.WithTimeout` (the shared `http.Client.Timeout` is now fixed at `requestTimeoutLong` purely as an outer backstop, never the binding constraint), a periodic heartbeat logged every `heartbeatInterval` (30s, a `var` for test control) naming the call (`"ResolveLeaf <leaf-id>"`, `"PlanFromRecon"`, etc.), and exactly one retry when the failure was specifically *this* per-call deadline firing (`isOwnTimeout` — distinguished from the caller's own outer ctx being canceled/expired, which gets no retry, and from a real API/decode error, which also gets no retry). Tests: `pkg/llmfallback/reliability_test.go` (7 cases covering the retry/no-retry/heartbeat/Option-wiring behavior).
- **LT-147 (fixed 2026-09-11)** — `aggregator.addHost` (`pkg/recon/aggregate.go`, right after `addEndpoint`) appends unconditionally, unlike `addTech`'s `NormalizeHost`-keyed merge (LT-14). Live-reproduced: the apex target itself (`nettix.com.pe`) got two separate `HostFact` entries in one recon run — one from Wave 1's ASN/WHOIS lookup (`Source: "passive-whois-asn"`, carries `.notes`), one from Wave 2's httpx/naabu probe (`Source: "httpx"`, carries `.ports`) — every other one of the 24 discovered hosts got exactly one entry. Cosmetic today (a split-looking row in the Web UI's Hosts table) but the same class of bug LT-14 fixed for `TechFact`. **Fix:** give `addHost` the same `NormalizeHost`-keyed merge (union `.notes`/`.ports`/`.headers` fields rather than dropping the second entry). **Applied**: `addHost` now mirrors `addTech`'s shape exactly — a `NormalizeHost`-keyed index (`hostIndex`/`hostSources`), comma-joined `Source`, best-`Confidence`-wins — but unions `Notes` (append) and `Ports` (via a new `mergePortFacts` helper, so a port both waves happened to observe isn't duplicated) instead of merging a `Name` field (`HostFact` has none). No `Headers` field exists on `HostFact` to union — the fix's original note overstated that part. Tests: `TestAddHost_SameHost_MergesInsteadOfDuplicating` (reproduces the exact live passive-whois-asn/httpx pairing), `TestAddHost_WwwAndCaseHostVariants_MergeInsteadOfDuplicating`, `TestAddHost_DifferentHost_StaysDistinct`, `TestAddHost_SameSourceTwice_SourceNotDuplicatedInString`, `TestAddHost_LowerConfidenceArrivesSecond_DoesNotDowngrade`, `TestAddHost_DuplicatePort_NotDuplicated` (`pkg/recon/aggregate_test.go`).
- **LT-148 (half-fixed 2026-09-11 — the LT-6-tail duplicate-finding symptom; the PlanTree-honesty half is still open)** — `registry.Resolve` builds full per-host template leaves with no awareness of D6's uniform-response-wall classification (`ReconResult.UniformResponses`, LT-140) at all — that's purely a *scan-time* short-circuit (`pkg/scanner/uniformwall.go`). Live-reproduced: the PlanTree from a target recon already flagged 100%-WAF-blocked (`ns1`/`web01`/`web02`/`sinchi01`, canary status 401) still carries 8-9 "will run this CVE/admin-panel check" leaves each, indistinguishable in Plan Preview from a leaf that will actually fire. Worse, `planexec.runLeaf` (`executor.go:442,550`) dispatches each leaf through its own fresh `scanner.Engine.Run` call — so uwGate's per-*target* check (`engine.go:328`) re-evaluates once per *leaf* on a walled host, producing N duplicate `misconfig-waf-blocked`/`misconfig-uniform-catchall` findings when driven through webui/mcp (the CLI `scan --recon-file` path batches all of a host's dispatch into one `Engine.Run`, so it doesn't show this). Ties directly to the already-known, still-open LT-6 tail ("webui/mcp never call `Dedup`/`SplitAggregates`") — Dedup would collapse the duplicates if it ran there. **Fix ideas:** have `Resolve` itself drop/demote a host's template leaves (keep the built-in-detector leaf only) when `result.UniformResponseForHost(host)` is non-nil, so Plan Preview shows the truth up front; separately, either call `Dedup` on webui/mcp's own accumulated findings (closing LT-6's tail) or have `planexec.RunPlan` batch a host's leaves into one `Engine.Run` invocation. **Applied (the LT-6-tail half)**: `planexec.RunPlan` (`executor.go`) now runs its final accumulated `findings` through the identical `reporter.SplitAggregates` → `reporter.DropSupersededNucleiFindings` → `reporter.Dedup` pipeline `cmd/hackerfive/scan.go:354` already ran for the CLI path — closing LT-6's tail for both of `RunPlan`'s callers (webui, mcp) in one place, guarded to a no-op when `findings` is empty so the function's nil-return contract for an empty/fully-skipped plan is unchanged. `opts.OnFinding`'s live per-leaf streaming callback is untouched (still raw, pre-Dedup) — only the final batch `RunPlan` returns is deduped. Regression test `TestRunPlan_UniformWallFinding_DedupedAcrossLeaves` (`executor_test.go`) reproduces two same-host leaves each independently triggering the D6 short-circuit and asserts exactly one `misconfig-waf-blocked` finding survives. **Still open**: `registry.Resolve` itself still builds the misleading leaves in the first place — Plan Preview still shows 8-9 "will run" rows for a walled host before any dispatch happens; only the post-dispatch finding list is now honest. That half (dropping/demoting template leaves at PlanTree-build time when `result.UniformResponseForHost(host)` is non-nil) is unaddressed.
- **LT-149 (fixed 2026-09-11)** — neither `recon`/`plan --verbose` nor `scan`'s template-dispatch phase print any progress timestamp or heartbeat. `verboseProgress` (`cmd/hackerfive/recon.go:170-174`) is exactly `"%s: %s\n", wave, status` — no `time.Now()` — so a `wave3: running` → `wave3: done` pair gives no idea which wave is slow or whether a wave's own internal cap fired (this run: `naabu`/`katana` each silently hit their 2-minute wave-timeout with partial results, only visible after the fact via the warnings array, not the progress log). `scanner.Engine`'s corpus dispatch (`engine.go`) is worse: **zero output between the "loaded N templates" line and the final "scan finished in Xm" summary**, however many minutes that spans (this run: a `--max-target-duration 15m` scan against 6 targets gave no signal at all for 15 straight minutes). **Fix:** add elapsed-time to `verboseProgress`'s two lines; add a periodic ("dispatched N/M templates for target X so far") heartbeat to `Engine.Run`'s corpus loop, mirroring `--verbose`'s existing posture elsewhere in this codebase (LT-11/LT-37's precedent). **Applied**: `verboseProgress` now prefixes each line with `[+<elapsed>]` since the callback itself was registered (`verboseProgressNow`, an indirected `time.Now` so a test can drive it deterministically instead of racing a real clock). `runTemplates` (`engine.go`) gained a periodic heartbeat, factored into a standalone `startDispatchHeartbeat` (new `heartbeat.go`) for testability: every `dispatchHeartbeatInterval` (30s, a `var` so a test can shrink it) it logs `"<target>: <completed>/<total> template(s) completed so far"` via the existing `e.warnf` seam, tracking completions with an atomic counter (`fire`'s many goroutines each increment it) against the same `len(nativeTemplates)+len(nucleiTemplates)` total the existing time-budget/adaptive-abort findings already report. `stop()` blocks until the heartbeat goroutine has actually exited, so `runTemplates`' deferred call is guaranteed to leave nothing running past its own return. Tests: `TestVerboseProgress_WritesWaveStatusToStderr` (updated for the new format), `TestStartDispatchHeartbeat_LogsProgressUntilStopped`, `TestStartDispatchHeartbeat_ZeroTotal_NeverLogs` (`pkg/scanner/heartbeat_test.go`).
- **LT-150 (open)** — answers "are there over 500 templates loaded, are many rejected": **yes, by a wide margin**. Real numbers this round: 9,481 total synced+bundled templates; 200 rejected by both loaders (breakdown: 46 `flow:` unsupported, 38 internal-matcher-outside-flow, 32 absolute-URI raw request lines, 31 unsupported matcher/DSL syntax, 17 xpath, 15 unsupported extractor syntax, 12 `javascript:` blocks, 3 malformed YAML, 2 each `headless:`/`code:`, 1 `ssl:`, 1 not-a-template); `--narrow-by-tech`'s 19 resolved tags (5 misconfig-category-floor + 14 tech-matched) still scoped to **5,343 templates** — the floor tags alone (`misconfig`/`exposure`/`config`/`default-login`/`panel`) are each individually huge, and a near-universal fact like "Nginx" (present on ~20 of 24 hosts) contributes its own large tag population on top. Consequence, live-measured: at `--rate-limit 50` across the 6 non-walled hosts, only 1 of 6 finished its full 5,343-template pass inside the 15-minute per-target cap; the other 5 stopped at 22%/59%/80%/85%/94% completion (`scan-partial-time-budget` findings). The earlier, unscoped 23-host run at `--rate-limit 8` projected to ~4+ hours. **This is the real cost `--narrow-by-tech`'s doc comment already flags but likely understates for a diverse multi-tech host** — narrowing by tag alone doesn't bound wall-clock the way a human expects "scoped to this detector" to. Not a bug — a genuine design tension between coverage (keep the floor broad) and speed. Worth a coverage-vs-cost measurement pass on the floor categories themselves (which floor tags' templates have ever produced a real true-positive across this project's live rounds) before trimming, rather than trimming blind.
- **LT-151 (logging fix applied 2026-09-11; root-cause still needs a live re-scan to confirm)** — `cloud01.nettix.com.pe` and `cloud02.nettix.com.pe` serve byte-identical `/status.php` (confirmed live via direct `curl`: both `{"version":"28.0.5.1",...}`), yet this round's scan produced `misconfig-nextcloud-status-disclosure` + `misconfig-nextcloud-outdated` for `cloud02` and **zero findings of any kind** (not even missing-security-header ones) for `cloud01` — reproducing the exact symptom LT-113 (2026-09-08) was supposed to have fixed ("cloud02 got its Nextcloud findings but cloud01, identical, got 0"). No warning/error logged for `cloud01` anywhere in `scan-misconfig.stderr`. Leading hypothesis, not confirmed: every native product check (`checkDolibarrOutdated`, `checkNextcloudStatus`, `checkWebmin`, `checkWPUserEnum`, ...) treats *any* `doRequest` error as a silent `return nil, nil` — "not this product," indistinguishable from a genuine transient network hiccup (this same session independently observed `www.nettix.com.pe` returning one transient `502` mid-run). If `cloud01`'s very first priority-check request hit a blip, the finding is gone with zero trace. **Fix (if confirmed):** have the priority-check tier log (at `warn`/`info`, not silently) when `doRequest` itself errors, distinguishing "no request could be sent" from "product signature didn't match" — cheap, and would have made this round's asymmetry immediately diagnosable instead of requiring an independent live `curl` to even notice. **Applied**: `misconfig.Detector` gained the same `WithLogCallback`/`logFn` seam `idor.Detector` already had; all five priority checks (`checkWPUserEnum`, `checkDolibarrOutdated`, `checkNextcloudStatus`, `checkPhpMyAdmin`, `checkWebmin`) now call `d.logPriorityCheckRequestErr(check, target, err)` — a `warn`-level line — instead of silently returning `nil, nil` on a `doRequest` failure; `pkg/scanner/engine.go`'s `runDetector` wires it to `e.warnf`, same as `idor`'s existing callback. Regression test `TestMisconfigPriorityCheck_RequestErr_LoggedNotSilent` (`tests/unit/detector_misconfig_test.go`) points the detector at a closed listener and asserts at least one `warn:` line naming the failing check. This closes the *symptom* (silent failures are now loud) but the specific cloud01/cloud02 asymmetry's root cause is still unconfirmed — a future live re-scan against nettix.com.pe with this fix in place would show a warn line for cloud01 if the hypothesis is right, or rule it out if not.
- Positive confirmations worth recording (not gaps): `checkDolibarrOutdated`'s deliberate no-catch-all-guard (LT-113) fired correctly on `erp`/`ixn` even though D6 independently classified both as a uniform catch-all — the two mechanisms coexist as designed. `hostnameProductHints`' Guacamole entry (LT-9) correctly seeded `guacamole-default-login`/`apache-guacamole` leaves for `guacamole01` with zero TechFact backing. `classifyAppSurface` (LT-102) correctly returned `full` against a 24-host result with 8 walled hosts. `suggest --llm-assist` and `triage --llm-assist` (unlike the `plan --llm-assist` calls above) both completed cleanly and produced genuinely useful output on the first try — `suggest` correctly proposed a `correlate_hosts` action tying together the `www`/`soporte` WordPress-user-enum findings and a `run_leaf` action naming this round's own `soporte` 15-minute time-budget shortfall by name.

## Live Testing — nettix.com.pe Part A/B verification (2026-09-10)

Re-ran a full recon + targeted scans against the real, owned `nettix.com.pe` estate to check whether LT-92/95/96/123 actually engage on a real target, and to watch live traffic for further gaps.

**Verdict: the Part A fixes are real and already proven correct (crAPI, unit tests) but this target has none of the endpoint shapes that exercise them** — no `{id}`-shaped route, SSRF-able param, or self-service signup flow; `--auto-provision-account` correctly failed closed. Digging into *why* authbypass got zero leaves surfaced LT-124/125, the most valuable findings of the round:

- **LT-124 ✅ done 2026-09-10** — every authbypass check blindly trusted `resp.StatusCode` after the shared client transparently follows a redirect — a live FALSE POSITIVE: `GET /wp-admin/` (302→`wp-login.php`, correctly protected) was reported as `authbypass-missing-auth-wp-admin` high/high because the client followed the redirect and graded the login page's 200. Fix: `redirectedAwayFrom(req, resp)` gates all five authbypass checks (porting `misconfig`'s existing pattern). idor/ssrf's identical blind-status pattern left as-is (self-corrects via baseline/diff).
- **LT-125 ✅ done 2026-09-10** — `SuggestAuthBypassPathsFromRecon` only recognized 401/403 as "protected path" evidence, missing the extremely common "3xx redirect to a login page" shape — which is *why* this round had zero authbypass leaves despite `/wp-admin/` sitting right there as a recon-observed `302`. Fix: `redirectsToLoginBoundary` bucket ahead of the other cases. Landed with LT-124, sequenced after it.
- **LT-126 (open)** — `probeCommonPaths` records a probed path's *pre-redirect* URL alongside the *post-redirect* response's status/body — a same-host redirect gets none of LT-64's cross-host treatment. Live-confirmed: `erp.nettix.com.pe/api` recorded as `200` (actually Dolibarr's "API module must be activated" page at `/api/`, confirmed by hand; `/api` itself is a 301). **Fix:** same shape as LT-64 for the same-host case — rewrite `EndpointFact.URL` to the final URL, or carry the redirect chain regardless of host.
- **LT-127 (open)** — a wiki/CMS whose "page doesn't exist" response is HTTP 200 with a page-name-templated title defeats LT-30's canary suppression (body differs *by design* per path) — DokuWiki fabricated 3 high-confidence fake API endpoints this round. **Fix:** fetch a second, differently-named canary and diff it against the first; if they differ only in the echoed-path substring, classify as templated-catch-all.
- **LT-128 (open)** — `signupPathCandidates` (LT-123) is too narrow for WordPress/WooCommerce-style registration (a query-string action on an existing path, or a form embedded in an existing page) — `--auto-provision-account` never got a chance on this target. **Fix:** widen with `/wp-login.php?action=register`; consider a body-marker check for GET-rendered registration forms.
- **LT-129 (open)** — `pkg/fingerprint` has no signatures for Dolibarr/Nextcloud/Webmin/DokuWiki (all four confirmed live here), each instead reverse-engineered into a bespoke one-off `misconfig` check; `Signature` has no version-capture mechanism at all. **Fix:** add header/body signatures for all four (e.g. Dolibarr's `DOLAPIKEY` CORS header) + `hostnameProductHints` tokens; add an optional `VersionRegex` to `Signature` so future products get CVE-correlation for free.
- **LT-130 (open)** — the D2 program-policy pre-flight runs only against the CLI's initial seed target, never against hosts recon fans out to (24 real hosts this round, zero individually policy-checked). `scan` itself is unaffected (explicit `--targets` only). **Fix:** re-run `preflight.Check` over the full in-scope host list after Wave 1.
- **LT-131 (open)** — `pkg/mcpserver` has zero wiring for Part A/B's new fields (`SSRFBodyParams`/`IDORSeedID`/`AutoProvisionAccount`/`SignupEndpoint`) — an MCP-driven agent can't reach LT-95/96/123 at all, CLI-only. **Fix:** add the fields to `scanInput`/`planInput`, thread into `cfg`/`baseCfg` the same way the CLI does. Minor: `handlePlan` silently discards a template-index load error where `runScan` warns — align them.

### Demo-target selection (2026-09-08/09/10)

Active recon + a focused `misconfig` pass over the four owned demo domains (`.engagements/owned-sites/scope.txt`) to pick the 2026-09-10 Web UI demo target.

**Demo-target verdicts — do not re-evaluate these for a demo:**
- **`nettix.com.pe` — ✅ the demo target.** Only one of the four with a reachable, un-walled surface and a confirmed actionable finding: `www.nettix.com.pe` serves the full WordPress author list unauthenticated at `/wp-json/wp/v2/users/` (incl. `admin`, still active) — CWE-200, feeds credential stuffing. Secondary: `erp`/`ixn.nettix.com.pe` expose Dolibarr ERP 23.0.3 (behind 23.0.4+, CVE-2026-81728 HIGH + CVE-2026-85401; the earlier "critical dol_eval RCE" read was wrong, NVD-corrected 2026-09-08).
- **`aalberts.com` — ❌ non-viable.** Hardened estate (Cloudflare/M365 SSO/HTTP Basic/S3 apex), ~3 scannable hosts, thin actionable set, source-IP-blocked mid-run. FP-regression fixture only. Full record: § "Live Testing — www.aalberts.com" below.
- **`andertone.com` — ❌ non-viable (demo).** HTTP surface CDN-403-walled; the one real exposure (FTP:21+MySQL:3306 on `staging.`, LT-23) has a real `netservice` detector now (Phase 8 Step 1, done 2026-09-11) but that's a single non-HTTP finding, not a demo-worthy walkthrough — this verdict is about demo viability, not detector coverage; do not re-evaluate.
- **`aceautowreckers.com` — ❌ non-viable.** Fully Cloudflare-walled, every host 403. Same dead-end class as valmo/shopify/ALSCO.

For an actionable demo independent of owned sites, crAPI (13 verified findings, 0 FP, full plan→execute) stays the strongest.

- LT-97 ✅ done 2026-09-08 — no first-party WordPress REST user-enum check (the nuclei template existed but a real-target corpus run reliably never dispatches it within budget) → `misconfig.checkWPUserEnum`, always-on, immune to corpus starvation.
- LT-98 split into the dispatch-order fix (✅ done 2026-09-08, below under LT-114) and the corpus-load-perf design item (✅ done 2026-09-09, LT-106).
- LT-99 ✅ done 2026-09-09 — no headless/JS-rendered crawl (non-headless katana on crAPI's SPA found 4 endpoints vs. 40 in its spec) → opt-in `--headless-crawl` (`recon`/`plan`, `--recon-depth full` only), self-provisions Chromium if needed. webui/MCP toggle deferred.
- LT-100 ✅ done 2026-09-09 — no hidden-parameter mining → first-party diff-oracle param miner (`--param-mining`, curated ~239-name wordlist, ≥2-signal corroboration gate, hard request cap). Live: 0% FP on crAPI decoys, 4 real undocumented Juice Shop params found. webui/MCP toggle deferred.
- ffuf-style multi-position fuzzing reaffirmed out of scope 2026-09-08 → [Parked](#parked--revisit-on-a-trigger-or-after-an-eval).

### Apex re-run 2026-09-08 — 24 hosts, 5 findings confirmed by hand

`recon --scope '*.nettix.com.pe' --recon-depth full` → 24 hosts / 66 endpoints / 66 tech facts. Hand-verified actionable surface (all read-only GET):

| # | Finding | Host(s) | Detector status |
| --- | --- | --- | --- |
| A | WordPress REST user enumeration | `www`, `soporte` | ✅ `checkWPUserEnum` |
| B | Dolibarr ERP 23.0.3 exposed, outdated | `erp`, `ixn` | ✅ `checkDolibarrOutdated` |
| C | Nextcloud `status.php` unauth version disclosure | `cloud01`, `cloud02` | ✅ `checkNextcloudStatus` |
| D | Nextcloud 28.0.5 outdated (EOL major + 4 CVEs) | `cloud01`, `cloud02` | ✅ `checkNextcloudStatus` |
| E | phpMyAdmin exposed | `chasqui03` | ✅ `checkPhpMyAdmin` |

Not hand-verified: Webmin :10000, Apache Guacamole (`guacamole01`), a webmail stack, DokuWiki (current, ruled out).

- LT-101 ✅ done 2026-09-08 — a `www.<domain>` seed starved Wave 1 subdomain enum (39-host surface collapsed to 1, since subfinder ran against the `www.` host, not the registrable domain). **Still open:** the registrable-domain reduction itself (this round worked around it by re-seeding at the apex by hand); real fix → Phase 8 Step 5/recon backlog.
- LT-102 ✅ done 2026-09-08 — `app_surface: none` suppressed the plan on a 24-host result where a majority of hosts were walled but several served real, distinct content → `classifyAppSurface` now weighs distinct-real-content hosts, not just the majority verdict.
- LT-103 ✅ done 2026-09-08 — a CMS login page that renders for many paths (Dolibarr/DokuWiki) was flagged `catchall` despite katana crawling real distinct routes on it → suppressed when katana itself extracted ≥2 distinct non-asset routes. **Residual (open):** depends on katana actually reaching the host inside the flat 60s wave cap on a large sweep (fixed for a focused 2-6-host scan, not a 24-host one) — filed toward LT-38/a `uniformwall` follow-up.
- **LT-140 ✅ done 2026-09-11** — `ReconResult.UniformResponse` was one fact, not one per host, so D6's corpus-skip (LT-59) only ever protected a single host per multi-host recon run — `aggregator.setUniformResponse` kept only "the first verdict seen," and all three frontends built `UniformWallHosts` (already a `map[string]string`) from that one fact. **Fix:** `UniformResponseFact` widened to a per-host collection — `ReconResult.UniformResponses []UniformResponseFact` (schema v1.13, a deliberately breaking rename: `uniform_response` → `uniform_responses`, agreed since no real external MCP consumer depended on the old shape yet); `aggregator.uniformResponses` keyed by `NormalizeHost` (per-host first-writer-wins, same convention as `addTech`'s LT-14 dedup); `classifyAppSurface` now judges "blind" against every walled host, not one named host; new `ReconResult.UniformWallHosts()` / `UniformResponseForHost(host)` helpers replace each of the 4 call sites' (`cmd/hackerfive/scan.go`, `pkg/webui/handlers_launch.go`, `pkg/mcpserver/tools_plan.go`/`tools_scan.go`) hand-rolled single-entry map. Also fixed two latent cross-host bugs the single-fact shape had baked in: `reconShowsAdminSurface` (LT-58) and `hostServesDynamicContent` (LT-67) in `pkg/registry/decisionengine.go` now resolve the wall verdict per endpoint/leaf host — before, a WAF wall on host A could blanket-suppress a real 401/403 admin signal or secret-grep leaf on unrelated host B in the same multi-host run. → [Phase 8](17-implementation-plan-ph8.md) Step 5 DoD (filed alongside LT-64/65/84's per-host fact attribution theme, not Step 6 as originally noted here — Step 6 is Eval Maturity + Release, a stale reference from before this doc's 2026-09-10 step renumbering).
- LT-104 ✅ done — scan-side face done 2026-09-08 (a second-canary `detectCatchAll` check suppresses exposed-path/dir-listing FPs on a confirmed soft-404 catch-all host); recon-side face done 2026-09-10 via LT-66's tail (above) — the phantom `wave3-common-path-probe` endpoints themselves are now dropped on a catch-all host.
- LT-105 ✅ done 2026-09-11 (bundled into Phase 8 Step 4, since LT-7's new hard-drop gate is exactly what turns this misparse from a scoring nuisance into an active false-negative risk) — a `WordPress:7.1` tech fact is a misparse (plugin/asset version bleeding into the core-product fact, cf. LT-21) — poisons any affected-version CVE gating. **Fix:** `aggregator.sanitizeWordPressCoreVersion` (`pkg/recon/wpplugins.go`) strips a "WordPress" core fact's version when it exactly matches a plugin/theme version `wordPressPluginFacts` separately recorded on the same host — the bleed-through mechanism itself, not a hardcoded plausibility ceiling (which would age out every WordPress release). Falls back to an unversioned "WordPress" fact, which `versionInAffectedRange` already treats as "can't rule a template out."
- LT-118 ✅ done 2026-09-09 — `misconfig-exposed-path-admin` structurally false-positives on any app whose `/admin` redirects to a login page (every WordPress site) → `looksLikeAuthLoginPage` suppresses it.
- Native product-fingerprint checks (Dolibarr/Nextcloud/phpMyAdmin/Webmin) ✅ done 2026-09-08 — `checkDolibarrOutdated`, `checkNextcloudStatus`, `checkPhpMyAdmin`, `checkWebmin`, all gated on a hard structural marker + an NVD-verified `KnownVulnerableVersions` CVE table shared across products. Negative control: DokuWiki correctly yields zero product findings.

### Baseline run 2026-09-08 — engine multi-host findings (pre-implementation)

Ran current `main` against `nettix.com.pe` before any new implementation: **the native product checks — the entire value of the work above — did not fire against the live Dolibarr hosts**, silently missing the demo's headline finding. All four causes ✅ done 2026-09-08 (`fix-baseline-lt113-111-114`):
- LT-111 recon's wave-time cap was a hard-coded 60s const, non-deterministically starving subdomain enum on a large sweep → `--wave-timeout` flag + env override, default unchanged. Host-count auto-scaling (LT-38's tail) still open.
- LT-112 subfinder emitted an FTP-banner-prefixed hostname (`220-sinchi01…`) that silently voided the entire httpx batch (zero output, no error) → `normalizeHostname` strips/rejects malformed entries; httpx-wave now warns on an unexpected zero-live-host result.
- LT-113 **#1 demo-blocker** — the misconfig check loop forfeited every remaining check once the host-error breaker tripped, and the 5 product-fingerprint checks were ordered *last* → they're now a `priorityChecks` tier, run first, exempt from the breaker; a check error is now non-fatal instead of aborting the whole detector. Root-response-cache dedup (reduces the request volume that trips the breaker at all) still open.
- LT-114 a multi-target corpus scan dispatched ~0 templates/host inside any sane budget (shared rate bucket ÷ N hosts, plus `cves/**`-first dispatch order wasting what little got through) → real dispatched-count reporting, native-before-corpus + severity-band dispatch ordering, and (via LT-106) a per-target rate-limit share once a large corpus is loaded.
- LT-106 ✅ done 2026-09-09 — corpus-load perf: `pkg/scanner/targetshare.go`'s `effectiveTargetConcurrency` caps cross-target concurrency so each in-flight target keeps ≥5 req/s of the shared bucket once a large corpus is loaded; `pkg/scanner/parsecache.go`'s fingerprinted sidecar cache skips a full ~9.6k-template parse on a tag-scoped re-run. Kill switch: `HACKERFIVE_DISABLE_PARSE_CACHE=1`. **Tail still open (low priority):** one extra dir-read pass on a cache miss; `--all-templates` can't use the cache to narrow, only to warm it.
- LT-107 ✅ done 2026-09-10 — coverage-gap ledger (`pkg/coveragegap.Ledger` + `registry.CoverageStatus`): a structured record per `(host, fingerprinted product/version)` that no loaded tag/detector matched. [Phase 7](16-implementation-plan-ph7.md) Step 7 (renumbered from "Step 8").
- LT-108 ✅ done 2026-09-10 — `hackerfive suggest <scan-output>` CLI command (`llmfallback.Suggest`): one stateless frontier call over LT-107's ledger + findings, printed-only proposed next actions. No auto-apply, no re-scan. Phase 7 Step 7, builds on LT-107.
- LT-109 (open) — `hackerfive templates promote <name>` + a webui review panel for an I4-drafted template sitting in `templates-proposed/` (today: manual `mv`). Still human-gated. → after LT-108, not yet scheduled into a step.
- LT-110 (open) — webui "Apply and Run": execute LT-108's mechanical suggestions (draft templates, queue second-pass leaves), present at the existing Plan Preview gate, launch a fresh scoped Job on approval. Needs a design pass (job-lineage model, iteration cap). → after LT-109, Phase 8/9 window.
- **LT-157 (✅ fixed 2026-09-19)** — evidence-gate LLM-asserted severity/confidence in `llmfallback.TriageFindings`/`Suggest`: don't let a triage response upgrade a finding's severity/confidence unless the specific `Finding.Evidence` field it cites actually contains the marker the claim depends on (a deterministic post-check, not another LLM call). Concrete motivating bug: the `misconfig-cors` detector's own overclaimed description got parroted verbatim into a triage rationale for a literal-wildcard CORS response that isn't actually exploitable without also satisfying the credentials rule — neither the detector nor triage caught it. Cross-project research (2026-09-14, see [discussions.md](discussions.md) if logged) backs this up from two directions: Strix's benchmarked design only ships a finding when a real PoC fires against it ("if the exploit doesn't fire, the finding doesn't ship"), while Cyber-AutoAgent's unverified self-reported `[CONFIDENCE] 95%` (the model grading its own claim, no independent check) is the failure mode to avoid. **Fixed:** since neither `RankedFinding.Rationale` nor `SuggestedAction.Description` can structurally mutate a `Finding`'s `Severity`/`Confidence` at all (both are already free-text-only per `types.go`'s doc comments — doc02's "detector-set, never agent-writable" invariant already held), the real gap was that free text; a triage/suggest rationale could still *assert* certainty a finding's own fields don't support. New `pkg/llmfallback/evidencegate.go`'s `evidenceGateText` is the deterministic post-check: a short, hand-curated, case-insensitive list of unhedged certainty phrases ("fully exploitable", "guaranteed", "grants full access", ...) is checked against the cited `Finding`'s own `Confidence`/`Severity` — the only structurally reliable evidence-quality signal available without spending another LLM call to verify the claim (a full semantic grounding check against arbitrary `Evidence` text would need either another model call or a fragile per-detector keyword→field table). A match on a finding that isn't `Confidence: "high"` and `Severity` in `{"high","critical"}` gets a `[unverified claim — ...]` caveat prepended — the model's own text is never rewritten or dropped, only flagged. Wired into both `TriageFindings` (gates each `RankedFinding.Rationale` against its own finding) and `Suggest` (gates `run_leaf`/`triage_group` actions' `Description` against `weakestFinding` of whatever `finding_id`/`finding_ids` the action's `Detail` names — actions with no finding reference, like `draft_template`/`redo_recon`, are left untouched since there's nothing to gate against). Tests: `pkg/llmfallback/evidencegate_test.go` (phrase detection, strong-vs-weak-finding gating, zero-value/empty-text no-ops, `weakestFinding` tie-breaking, `findingsFromDetail` single/list/unknown-id extraction) plus integration tests in `TestTriageFindings_OverclaimingRationaleOnWeakFinding_GetsCaveated`/`TestSuggest_RunLeafOverclaimingReferencedFinding_GetsCaveated`/`TestSuggest_ActionWithNoFindingReference_DescriptionUnaffected`.
- **LT-159 (open)** — `pkg/scriptexec`'s egress-proxy sidecar binary (`pkg/scriptexec/egressproxy/cmd/hf-egressproxy`, [93-implementation-plan-agent-orchestrator.md](93-implementation-plan-agent-orchestrator.md) M1) is cross-compiled on demand via `go build` at `script.explore` call time (`sandbox.go`'s `buildEgressProxyBinary`) — this works on any dev machine with a Go toolchain (confirmed live, 2026-09-14, against a real Docker daemon: both the golden-path and out-of-scope-egress-block scenarios pass end to end), but a distributed release build of `hackerfive` with no Go toolchain on the end user's machine would have `--allow-agent-scripts` fail outright. **Fix (unscheduled):** cross-compile `hf-egressproxy` for linux/amd64 and linux/arm64 at `hackerfive`'s own release-build time and `go:embed` both into the release binary, so `ensureEgressProxyBinary` extracts the matching one instead of invoking `go build`. Not blocking `hackerfive agent`'s own development/eval-harness use (M1-M5) — only a real distributed release of the feature.
- **LT-160 (✅ both items fixed — item 2 closed 2026-09-19)** — `pkg/orchestrator`'s M2 loop ([93-implementation-plan-agent-orchestrator.md](93-implementation-plan-agent-orchestrator.md)) had two deliberate scope-narrowings against the milestone's original sketch, both safe (fail-closed, never fabricate) but real gaps to close before M5's lab-target validation:
  1. ✅ fixed 2026-09-19 — `script.explore`'s evidence-reconfirmation pipeline (M1's "independently re-issue the discovered request through `pkg/scanner/httpclient` before packaging it as `Finding.Evidence`", closing LT-157's self-reported-confidence flaw) wasn't built — a script's stdout was only surfaced in turn history for a human/future turn to act on, with no code path turning it into a `Finding` at all. Fixed by new `pkg/orchestrator/scriptevidence.go`: `buildCatalog`'s `script.explore` tool description now tells the model to print one `HACKERFIVE_FINDING_CANDIDATE: {json}` line per candidate, naming a request and a falsifiable `confirm_status`/`confirm_contains` condition. `dispatchScriptExplore` hands the sandbox's stdout to `reconfirmScriptCandidates`, which independently re-issues each candidate's exact request via `pkg/scanner/httpclient` (never trusting the sandbox's self-reported exit code/stdout) and only ships a `Finding` — `Confidence: "high"`, `Evidence["confirmation_method"]` naming the re-issue — when the real re-issued response actually matches the candidate's own stated condition. A candidate with neither confirm field, one whose re-issued response doesn't match, one outside `cfg.BaseScanConfig.Scope`/the seed target's host, or a mutating method without `--allow-writes` (reusing the scan's existing gate, not a new one) is dropped and logged, never silently promoted. Covered by `pkg/orchestrator/scriptevidence_test.go` (confirmed-by-status, confirmed-by-substring, not-confirmed-on-mismatch, no-confirm-condition, out-of-scope, real-scope-file, mutating-method-gate, and stdout-parsing tests).
  2. ✅ fixed 2026-09-19 — `recon.refresh`'s result wasn't merged back into the running `PlanTree` (no leaf-shape-changing merge existed post-construction outside `registry.Resolve`'s own initial build) — it only returned a fact-count summary into turn history, so a mid-run recon refresh couldn't surface a genuinely new leaf the model could act on next turn. **Fixed:** new `mergeReconRefresh` (`pkg/orchestrator/orchestrator.go`) runs `dispatchReconRefresh`'s fresh `*recon.ReconResult` through the exact same deterministic `registry.Resolve` the initial tree build already uses — so a "new leaf" is never an LLM inventing one, only the same rule table re-applied to newly observed facts, the same additive-only pattern `llmfallback.MergeLLMProposals` already established for an LLM-proposed leaf. A host the fresh recon discovered that the tree never had at all gets its whole freshly-resolved subtree attached under root; a host the tree already has gets only its genuinely new leaves (deduped by a new shared `leafDispatchKey{Detector, Target, EndpointTemplate}` — also now used by the pre-existing `warnDuplicateLeafTargets`, so the two never drift into checking different notions of "identical leaf") attached via the existing `agenttask.AttachLeaf`; every existing leaf's own `Status`/`Confidence`/`Rationale` is left untouched — doc02 Design Principle 5 ("a later pass can only ever weaken a plan, never strengthen it... none can add a leaf") constrains leaf-*resolution* passes re-judging an existing leaf, which this isn't: it's growing the tree from new deterministic facts, the same operation `registry.Resolve`'s initial build already performs. The `recon.refresh` tool's own catalog description (`buildCatalog`) was updated to stop claiming it "does not change the plan tree's shape." Tests: `pkg/orchestrator/reconrefresh_test.go` (nil-input no-ops, brand-new-host subtree attachment, existing-host genuinely-new-leaves-only merge with a re-merge idempotency check, and an end-to-end `Run`-level test proving a `recon.refresh`-discovered leaf becomes immediately dispatchable via a later `scan.leaf` call in the same run).
- **LT-161 (✅ fixed 2026-09-19)** — `pkg/webui`'s Plan Preview execute flow (`handlers_plan_exec.go`'s `executePlan`, dispatching `planexec.RunPlan`) never set `Job.execCfg.Scope` — only `.ScopeFile` (a bare path). `RunPlan`'s B4 scope-creep gate (`executor.go`, `baseCfg.Scope != nil`) reads `.Scope` directly with no `ScopeFile` fallback of its own (only `scanner.Engine.loadScope` has that fallback, and `RunPlan` never goes through `Engine`) — so a plan-execution leaf dispatched from the Web UI ran with B4's scope-creep gate silently inert, regardless of whatever `--scope`/`scope_file` the operator entered at launch. Found during [93-implementation-plan-agent-orchestrator.md](93-implementation-plan-agent-orchestrator.md) M4 research (2026-09-15) while confirming how `pkg/orchestrator`'s own `BaseScanConfig.Scope` needed to be wired (M4's `runLaunchAgentJob` parses `form.ScopeFile` into a real `*scope.Scope` itself, sidestepping this gap for the new agent path — but the pre-existing Plan Preview path was untouched). Fixed exactly as scoped: `parseLaunchSubmission` (`handlers_launch.go`) now parses `form.ScopeFile` once via `scope.Parse` right after building `execCfg` and sets `execCfg.Scope` directly — a parse failure is now surfaced as a form-submission error (`h.rerenderLaunchWithErrors`) instead of only failing silently and separately inside `runLaunchRecon` later, with `execCfg.Scope` left nil either way. `cfgs`' own per-detector configs (the checked-tab flow) were left untouched — they go through `scanner.Engine`, whose `loadScope` already has the `ScopeFile` fallback, so they were never actually affected by this gap. Covered by two new tests in `handlers_launch_test.go`: `TestParseLaunchSubmission_ScopeFile_PopulatesExecCfgScope` (a valid scope file populates `execCfg.Scope` and it correctly allows/denies) and `TestParseLaunchSubmission_InvalidScopeFile_FailsAsFormError` (a bad path is a form error, `execCfg.Scope` stays nil).

- **LT-162 ✅ closed 2026-09-20 — gate met** — [93-implementation-plan-agent-orchestrator.md](93-implementation-plan-agent-orchestrator.md) M5's three-way FP/FN/cost/wall-clock table, re-run after five more fixes landed this round (evidence-gating overclaimed LLM rationale text — LT-157; `script.explore` reconfirmation and `recon.refresh` tree-merging — both halves of LT-160; the `NextAction` prompt's leaf-list cap — LT-165 item 1; the IDOR query-key dedup fix — LT-166's related bug; and I4 field-miss resolution in `pkg/orchestrator` — LT-138 item 1): **orchestrator mode now beats the deterministic baseline, 13/22 vs. 10/22** (full per-target table in the "Live Testing — G1 three-way eval re-verification, isolated lab targets (2026-09-20)" entry below). DVWA 4/10 (beats 3/10), Juice Shop 2/5 (ties), vAPI 5/5 (beats 4/5 — the only miss, `nuclei-php-detect`, is now found), crAPI 2/2 (beats 1/2 — `nuclei-http-missing-security-headers`, missed by *both* deterministic crAPI scenarios, is found). Per doc93's own standing rule ("do not point this mode at a real program until [this table] exists and shows a genuine improvement"), that condition is now met — **`hackerfive agent` may be considered for a real, appropriately-scoped, human-supervised program**, not lab/CLI-only by default. Two live-testing caveats worth carrying forward rather than treating this as an unconditional green light: (1) this re-run required manually isolating each target (stopping every other lab container) to get a trustworthy number at all — the real scope-model gap this exercise surfaced was tracked and fixed same-day as LT-167 (`Scope` now supports an optional `host:port` scope entry); (2) crAPI's run still hit the harness's 20-minute ceiling before producing a clean final `Result` (`cost`/`Iterations` for that leg are therefore unknown, not zero — LT-166, root cause now precisely diagnosed 2026-09-20, is the reason: every `scan.leaf` dispatch on that host redundantly reloads and refires the full template corpus, including a real crAPI-specific template, rather than testing just that leaf's own endpoint — inflating both turn count and raw finding volume for this row without invalidating the fixture-prefix win itself).

- **LT-163 (✅ all three items fixed — item 3 closed 2026-09-19)** — re-testing LT-162 against vAPI/crAPI surfaced two real gaps in `tests/eval/orchestrator_run.go`/`orchestrator_eval_test.go`, plus one more general robustness gap in `hackerfive agent` itself:
  1. ✅ fixed — `OrchestratorScenario` had no `--templates` override field, unlike every deterministic `Scenario` in `challenges.go` (which explicitly scopes vAPI to `./templates/` and crAPI's IDOR run away from the full corpus). Without it, `hackerfive agent` auto-appended the full ~2,500-tag-scoped synced corpus, and vAPI's already-documented "full corpus hangs against its dev server" gotcha (`docs/20-setup-testing-targets-macos.md`) made a single `scan.leaf` take 4+ minutes — with `Config.MinIterations` forcing several such turns, the run blew the harness's timeout with 0 tool_calls ever completing (an infra artifact, not a reasoning miss). Fixed by adding `OrchestratorScenario.Templates` and wiring `--templates` into the dispatched args; vAPI set to `"./templates/"`, crAPI to `templatesDir()` — both mirroring their deterministic-scenario counterparts. Live-verified: vAPI's orchestrator run went from timeout/0 findings to 4/5 (matching the deterministic baseline exactly).
  2. ✅ fixed — the per-scenario context timeout was a flat 10 minutes, copied from `TestAgentEvalHarness` (MCP-plan mode) before `Config.MinIterations` (LT-162's own fix) existed. Live-verified against crAPI: individual `NextAction` calls against `deepseek/deepseek-v4.1-flash` took 60-150s+, and `MinIterations`'s forced floor of dispatched turns (plus any rejected-stop retries, each its own `NextAction` round trip) pushed total wall-clock past 10 minutes on a target with more candidate leaves to consider than vAPI/DVWA/Juice Shop. Raised to 20 minutes in `orchestrator_eval_test.go`.
  3. **✅ fixed 2026-09-19** — found via the same crAPI run: it was killed by the (then-10m) timeout mid-run, after a real `misconfig-exposed-path-.env` (high severity — crAPI's own `.env` file, containing real secrets, is genuinely publicly exposed) had already been surfaced live via `OnLog`/turn history, but `hackerfive agent`'s CLI only printed its final `Result` JSON after `orchestrator.Run` returned cleanly — a killed process (this harness's timeout, but equally a real Ctrl-C, OOM, or host reboot) discarded every finding already discovered that run, with no partial output at all. Fixed by changing `cmd/hackerfive/agent.go`'s stdout/`--output` contract from one pretty-printed `Result` document written only at the end to a JSON-Lines stream (new `agentStreamEvent`/`agentEventWriter`): a `{"type":"finding",...}` line is written and flushed the instant `Config.OnFinding` fires (already called synchronously per real detector match, per `pkg/planexec.ExecOptions.OnFinding`'s own doc comment), and a final `{"type":"result",...}` line carries the complete `orchestrator.Result` once `Run` returns (with `error` set if `Run` itself errored, so a partial-but-real `Result` on a `NextAction` failure is never dropped either). `os.Create`/`os.Stdout` are both unbuffered Go-level writers, so a completed `Encode` call is durable in the OS pipe/file the instant it returns, regardless of what kills the process afterward. `agentEventWriter` serializes concurrent writes (a leaf's scanner engine can report findings from more than one worker goroutine) — covered by `cmd/hackerfive/agent_test.go`'s new `TestAgentEventWriter_*` tests (streaming order, a `Run`-error result event, and a 50-goroutine concurrent-write test asserting every line still parses independently). `tests/eval/orchestrator_eval_test.go` was updated to scan stdout line-by-line and collect `finding` events (falling back to them when a killed run's `result` event never arrives) instead of requiring one whole-buffer `json.Unmarshal` — this is what makes a killed run's real findings actually count in the FP/FN table (LT-166's redispatch issue no longer implies "0 findings observed" the way it did before this fix; see LT-166 for the actual redispatch root cause, still open).

- **LT-164 (✅ fixed 2026-09-19)** — crAPI's orchestrator run found 0/2 expected prefixes (vs. the deterministic baseline's 1/2, LT-163) because recon never discovered `/workshop/api/mechanic/mechanic_report` on its own (the deterministic `Scenario` is handed it directly via `--endpoint`). Investigated live against the real container before assuming a fix: crAPI does **not** expose a per-service OpenAPI/Swagger spec at any conventional path, authenticated or not (`/identity/`'s Spring gateway 401s *every* path under its prefix, real or not, before route resolution even runs — confirmed by diffing a known-real vs. a made-up path, both 401) — so extending `probeCommonPaths`' spec-walk to retry under discovered prefixes, the originally-proposed fix, would not have helped. The real, live-verified gap: crAPI's React bundle builds its request URLs at runtime from separately-declared string constants (`ig="workshop/"` + `sg.GET_SERVICE_REPORT="api/mechanic/mechanic_report"` + `"?report_id="+id`), and neither piece alone is endpoint-shaped enough for `extractJSEndpoints` (`jsstatic.go`) to ever see it. Fixed by adding a join pass (`extractJSPathJoinParts`/`buildJSJoinPairs`/`verifyJSJoinCandidates`, `pkg/recon/jsstatic.go`) that reconstructs prefix+base(+query-suffix) candidates from a bundle's own constants and live-verifies each one (bounded to `maxJSJoinProbes`) before emitting it as a `js-static-joined` EndpointFact — critically, verification diffs each candidate against a **per-prefix canary** (`fetchJSPrefixCanary`), not just a status-code allow-list, specifically because identity/'s blanket-401 behavior would otherwise "verify" a wrong prefix+base combination just as confidently as the real one, reintroducing the exact multi-candidate ambiguity this step exists to resolve (live-verified: an earlier version without the canary diff produced both `identity/api/mechanic/mechanic_report` and `workshop/api/mechanic/mechanic_report` as "verified," the canary diff correctly keeps only the real one). Two more real, unrelated bugs surfaced and were fixed along the way, both pre-existing and both silently capping recon on *any* real-world SPA, not just crAPI: (1) `maxJSStaticBodyBytes` truncated a captured JS body at 512 KiB — crAPI's single-file CRA bundle is 1.65 MB with its route constants sitting at byte ~1.6M, entirely past the old cap (raised to 4 MiB); (2) `runKatana`'s JSONL scanner (`crawl.go`) had a 1 MiB `bufio.Scanner` max-token-size — once the JS bundle's body pushed a single JSONL line past it, `Scan()` silently returned false and **the rest of that crawl's output, not just that line, was discarded with no error surfaced**, which is why `main.js` never appeared in `hackerfive recon`'s own endpoint list even though a manual `katana` invocation with identical flags found it every time (raised to 8 MiB, plus a new warning on `scanner.Err()` so a future overflow is loud, not silent). Live-verified end-to-end against the real crAPI container: `hackerfive recon --recon-depth full` now surfaces `js-static-joined http://.../workshop/api/mechanic/mechanic_report?report_id=` as a single, unambiguous candidate. `tests/eval/orchestrator_run.go`'s crAPI scenario was also switched from `Depth: "active"` to `Depth: "full"` — Wave 3 (crawl + JS-static analysis, where this whole fix lives) never runs below `"full"`, so the scenario couldn't have exercised this fix at all otherwise; every other `OrchestratorScenario` stays at `"active"` since none of their expected findings need Wave 3. **The candidate-generation gap itself is closed and confirmed**: with `Depth: "full"`, one orchestrator eval run did dispatch a real `scan.leaf` against the recon-discovered endpoint and produced 5 genuine `idor/high` findings (`report_id=1..5`, "otherToken retrieved real user data ... does not match the established denied-access baseline"). The eval scenario still doesn't pass end-to-end, though, for a separate, newly-discovered reason — see LT-166.

- **LT-165 (item 1 ✅ fixed 2026-09-19, item 2 deliberately not attempted — 2026-09-20 measurements in LT-172/LT-173 now supply the missing eval data: `NextAction` has no `max_tokens`/reasoning-effort cap, and 14 of 17 vAPI/DVWA turns were single-template dispatches that didn't need a model call)** — `pkg/llmfallback.Client.NextAction` calls against `deepseek/deepseek-v4.1-flash` (via OpenRouter) are slow in practice: 60-150s+ per call, live-observed during LT-162/163's re-verification. This is what made `Config.MinIterations`'s forced turn floor (LT-162's own fix) push crAPI's orchestrator run past a 10-minute budget (LT-163 raised the eval harness's timeout to compensate, but the underlying latency itself was unaddressed). Two independent angles were named:
  1. ✅ fixed — `buildOrchestratePrompt` (`pkg/llmfallback/orchestrate.go`) already capped turn *history* at `maxHistoryTurnsInPrompt` (LT-162's fix) but rendered the full plan-tree leaf list uncapped on every call — on a target with many leaves (crAPI's tree is larger than vAPI/DVWA/Juice Shop's), this was a real, still-uncapped contributor to prompt size (the tool catalog itself is a fixed ~6-entry list, negligible either way, so only the leaf list needed a cap). New `boundedLeavesForPrompt` caps the rendered leaf list at `maxLeafNodesInPrompt` (80), keeping every actionable (`StatusPending`/`StatusUnresolved`) leaf visible first — those are the only ones a `node_id` could usefully target this turn — and dropping an already-settled leaf (done/vetoed/escalated) first when the cap is hit; if even the actionable set alone exceeds the cap, it's truncated too (an omitted actionable leaf becomes visible on a later call once earlier ones resolve), rather than growing the prompt further. Tests: `TestBoundedLeavesForPrompt_UnderCap_ReturnsAllUnomitted`, `TestBoundedLeavesForPrompt_OverCap_KeepsActionableDropsSettledFirst`, `TestBoundedLeavesForPrompt_ActionableAloneExceedsCap`, `TestBuildOrchestratePrompt_CapsLeafListAndNotesOmission` (`pkg/llmfallback/orchestrate_test.go`).
  2. **Deliberately not attempted** — skipping `NextAction` outright when the tree has exactly one viable leaf was considered but not implemented: the catalog always also offers `recon.refresh`/`triage.rank`/`stop` alongside `scan.leaf`, so "one viable leaf" doesn't actually mean "one viable action" — forcing `scan.leaf` on that one leaf would remove the model's legitimate choice to triage already-collected findings, refresh recon first, or stop (e.g. a leaf that already escalated through its attempt/spend ceiling shouldn't be force-dispatched again). This is a real latency lever but a behavior-risking one without live eval data to confirm it doesn't regress recall — left for a future pass with an eval run to validate against, not guessed at here.

- **LT-166 (open — real root cause found and precisely diagnosed 2026-09-20, not yet fixed; re-confirmed later that day, and shown to corrupt the model's reasoning, not just cost time — see the 2026-09-20 "reasoning verification" section, LT-171)** — live-verified 2026-09-19, immediately after LT-164's fix: with `Depth: "full"`, the crAPI orchestrator eval run dispatched `scan.leaf` against `.../workshop/api/mechanic/mechanic_report?report_id={{id}}` and got 5 real `idor/high` findings — but the *identical* dispatch (same URL, same `report_id=1..5`, same "5 raw finding(s)" summary) then repeated two more times later in the same run, each a full ~1m20s re-scan, before the run exhausted the harness's 20-minute timeout and was killed. The 2026-09-19 pass ruled out two candidate causes by code reading (no live repro available that day): (1) a stale/re-dispatched leaf ID — `dispatchScanLeaf`'s guard, `planexec.RunPlan`'s unconditional `StatusDone` patch, and `PlanTree.Find`/`ApplyLeafUpdate`'s shared-mutex consistency form a provably-consistent chain, ruling this out; (2) `idorCandidatesAndSeeds`' own dedup (fixed as a real, separate, related bug that same day — see below) was confirmed not to be the mechanism either, since it dedups before `registry.Resolve`'s fan-out ever runs. A `warnDuplicateLeafTargets` diagnostic was added to catch "two leaves with an identical Detector+Target+EndpointTemplate at tree-build time," on the theory that some other candidate-generation path was producing true duplicates.

  **2026-09-20, with live crAPI access restored: the real mechanism was found, and it is not tree-level duplication at all.** A full re-run reproduced the exact symptom at scale — 13 separate `idor` leaves, each with a genuinely distinct, correctly-assigned `EndpointTemplate` (`/identity/api/auth/login?report_id={{id}}`, `/workshop/api/mechanic/download_report?report_id={{id}}`, etc. — confirmed directly from the run's own JSON `Result.Tree`, not inferred from the model's own turn-by-turn narration), yet **every single one of the 13 dispatches' real HTTP findings targeted the literal same 5 URLs** (`.../workshop/api/mechanic/mechanic_report?report_id=1..5`). `warnDuplicateLeafTargets` correctly did **not** fire — there is no tree-level duplication; each leaf really is distinct. Two synthetic reproductions were built to isolate where the divergence happens: one driving `pkg/scanner.Engine` directly with two different `EndpointTemplate` values in the same process, and one driving `pkg/orchestrator`'s own `dispatch()` against a two-leaf tree — **both came back clean**, proving `pkg/orchestrator`/`pkg/planexec`'s config plumbing and `pkg/scanner`/`pkg/detectors/idor`'s request-building are correct in isolation. The difference in both cases: neither synthetic repro had `--templates` set.

  **Root cause: `templates/idor/crapi-mechanic-report.yaml`** — a real, pre-existing, `idor`-tagged native template hardcoded to `{{BaseURL}}/workshop/api/mechanic/mechanic_report?report_id={{RangeInt(1|100)}}` (see [20-setup-testing-targets.md](20-setup-testing-targets.md)'s crAPI section; this template is genuinely correct and does real, valuable detection work in `hackerfive scan`). `pkg/planexec.RunPlan` has an LT-18 part (c) optimization: only the *first* leaf dispatched per host carries the additive full-template-corpus load (`corpusLeaves`, keyed by `seenHost` within one `RunPlan` call) — every other leaf on that host runs its own detector only, since re-loading/re-running the whole tag-scoped corpus per leaf would be needlessly expensive. That bookkeeping is scoped to a single `RunPlan` call, which is exactly right for `hackerfive scan`/webui Plan Preview (one `RunPlan` call dispatches every leaf together) — but `pkg/orchestrator`'s `dispatchScanLeaf` calls `RunPlan` **once per leaf**, each with every other leaf marked `Excluded`. From inside any one of those calls, the single non-excluded leaf always looks like "the first (and only) leaf on this host" — so **every one of the 13 `scan.leaf` dispatches independently reloaded and re-fired the full tag-scoped template corpus**, including `crapi-mechanic-report.yaml`, regardless of which leaf's own `EndpointTemplate` the model thought it was testing. The 65 total findings across the run are genuine (crAPI really is vulnerable at `mechanic_report`), but 12 of the 13 "leaf confirmations" are the same template re-firing on an unrelated leaf's turn, not that leaf's own idor-detector run against its own distinct endpoint (which most likely ran too and correctly found nothing, silently outnumbered by the template hits).

  **Not fixed this round** — needs a design decision on where "has the additive corpus pass already run for host X, this whole orchestrator run" is tracked, since `RunPlan`'s own per-call `corpusLeaves` map has no visibility across `dispatchScanLeaf`'s separate calls (candidates: thread a per-host-already-loaded set through `orchestrator.Config`/the `PlanTree` itself and pass it into a new `ExecOptions` field, vs. giving the orchestrator its own one-time-per-host corpus pass separate from `RunPlan`'s). **Practical implication for this session's already-committed eval table**: crAPI's "13/22 (isolated)" row and its "5 unexpected FPs" are not wrong about the fixture-prefix win (idor- and nuclei-http-missing-security-headers are both genuinely present), but the raw finding volume/per-leaf attribution behind that row is inflated by this bug, not a sign of the orchestrator genuinely testing 12 more real candidate endpoints. Left as an honest caveat here rather than silently correcting the already-committed table.

  Superseded by this finding but kept for history: `warnDuplicateLeafTargets` (`pkg/orchestrator/orchestrator.go`, unit-tested) remains a real, correct diagnostic for actual tree-level duplication — it simply isn't what LT-166 turned out to be.
  **A real, previously-unfound instance of "a different leaf ID resolving to an identical dispatch" was found and fixed 2026-09-19** (via a fresh static-code-reading pass, not a live crAPI re-run — no Docker/API key available in this environment; the exact crAPI trigger therefore remains unconfirmed, and this fix is reported honestly as *a* closed gap in the dedup chain, not *the* proven root cause). `idorCandidatesAndSeeds`'s two candidate sources formatted the *same* (path, key) two different ways: the per-endpoint `idShapedQueryCandidate` pass (an ID-*named* key, e.g. `report_id`) preserves every sibling query param in its template (`"?report_id={{id}}&foo=bar"`), while the separate LT-83 `numericQueryIDCandidates` pass (an unnamed-but-varying numeric key) drops all sibling params (`"?report_id={{id}}"`) — for a key whose name happens to also look ID-shaped *and* whose value varies across ≥2 observations, both passes fire and produce two different template strings for what is really one identifier position, defeating `seen[tmpl]`'s exact-string dedup and fanning out two `idor` leaves (LT-91) that dispatch an effectively identical check — precisely the class of bug LT-166 hypothesized, just located in candidate generation rather than in `Resolve`'s fan-out itself. **Fixed:** `numericQueryIDCandidates` (`pkg/recon/suggest.go`) now returns a `queryIDKey{path, key}` struct instead of a pre-formatted string; `idShapedCandidate`/`idShapedQueryCandidate` now also return the matched query key; `idorCandidatesAndSeeds` records each per-endpoint query match's `(path, key)` in a `seenQueryKey` set and skips a `numericQueryIDCandidates` result already covered by it, before ever formatting/comparing template strings. `SuggestSQLiTargets`'s own `numericQueryIDCandidates` caller was updated to use the new struct fields directly instead of parsing the old formatted-string return (a simplification, not a behavior change — it already merged both signals' params into one path-keyed target, so it was never exposed to this bug the way `idorCandidatesAndSeeds`' per-leaf fan-out was). Test: `TestSuggestIDOREndpointCandidates`'s new "an id-named query key varying across >= 2 observations must not also fan out via the LT-83 numeric path" case (`pkg/recon/suggest_test.go`) reproduces the exact two-source collision and asserts exactly one, sibling-preserving candidate survives.

- **LT-167 ✅ fixed 2026-09-20 — real gap, live-discovered the same day** — `pkg/scanner/scope.Scope.Allowed` (`scope.go:120-151`) matched by hostname only: it parsed `u.Hostname()` from both the scope entry and the target, discarding the port entirely, so a scope file containing a bare `localhost` (the exact convention `tests/eval`'s own `hostOnly()` helper and this session's manual `--scope` files both used) allowed a request to **any port** on that host, not just the one the operator actually pointed the run at. Live-discovered while re-verifying LT-162: with crAPI (port 8888), DVWA (port 80), Juice Shop (port 3000), and vAPI (port 8000) all running simultaneously for convenience, a manual `hackerfive agent -t http://localhost:8888 --scope <file with "localhost">` run had its `recon.refresh` action redirected entirely onto `http://localhost` (port 80, i.e. DVWA) — the run produced 35 real findings, every one of them DVWA's, zero of them crAPI's. Two design options were weighed: (a) grow `Scope`'s domain-entry format with an optional `:port` suffix, or (b) have `dispatchReconRefresh` validate a fresh `target` against the plan tree's own already-established host:port set instead of deferring to the coarser file-level `Scope`. Option (b) turned out to be far more expensive than it looked: `pkg/registry`'s whole `PlanNode.Target` convention is hostname-only end to end (`newEndpointLeaf`, every host node built in `decisionengine.go`) — the actual scheme+port is reconstructed only at dispatch time, from the original `recon.ReconResult`'s endpoints (`reconHostBaseURL`), never stored on the tree itself. Threading port-awareness through the tree just for this one guard would have meant touching dozens of leaf-construction sites for no other benefit. **Fixed via (a) instead**: `scope.Scope`'s domain entries (`domainEntry{pattern, port}`) now accept an optional trailing `:port` (parsed via `net.SplitHostPort`, so a bare unbracketed IPv6 entry is correctly left alone rather than misparsed); `Allowed` compares it against the target's own port (defaulted to 80/443 by scheme when the target omits one) only when the entry specifies one — an entry with no port keeps matching any port, so every scope file written before this fix keeps working unmodified. `dispatchReconRefresh` itself needed no change: `recon.Run`'s existing top-of-function `Scope.Allowed` check (`recon.go:445`) now genuinely catches the drift once the scope file pins a port. `tests/eval`'s `hostOnly()` helper (renamed `hostScopeEntry`, used by both `agent_eval_test.go` and `orchestrator_eval_test.go`) now writes `host:port` instead of a bare host, closing the exact reproduction vehicle this bug was found through. New tests: `TestAllowed_PortPinnedEntry_RejectsOtherPortsOnSameHost`, `TestAllowed_PortlessEntry_StillMatchesAnyPort`, `TestAllowed_PortPinnedWildcardEntry`, `TestEntries_PortPinnedEntry_RoundTrips` (`pkg/scanner/scope/scope_test.go`). Note this is opt-in: an operator's existing bare-hostname scope entry is unaffected until they actually add `:port` — the fix gives `--scope` the vocabulary to express a port boundary, but doesn't retroactively tighten a scope file nobody has edited.

**Baseline finding inventory (20 findings post-dedup, before the fixes above):** `www` got WP-user-enum + 2 FPs; `soporte` got one lucky corpus hit; `cloud02` got its Nextcloud findings but `cloud01` (identical) got 0 — all from LT-113. `erp`/`cloud01`/`gateway` got 0 findings each. `wiki` produced a false `misconfig-exposed-path-swagger-ui.html` (the scan-side face of LT-104). Missed vs. the hand-verified table: Dolibarr on `erp`/`ixn` (LT-113), Webmin on `gateway:10000` (port never scanned), phpMyAdmin on `chasqui03` (host never discovered, LT-111).

### Web UI review 2026-09-08 (Playwright, demo-prep)

Drove `hackerfive serve` against a live `erp.nettix.com.pe` to rehearse the demo — the scan itself was clean (5 findings, 0 FP). Three UI gaps, all ✅ done 2026-09-09 unless noted:
- LT-115 ✅ scoped MVP — the Launch form had no multi-target input (a focused N-host scan needed N launches) → an optional "Additional targets" textarea, one Job fans across all targets. **Deferred to a fuller version:** recon still runs once, against the primary host only — its tech-narrowing/field-fill then apply to every target, so a mixed-tech host set should leave tech-stack narrowing unchecked. Per-host recon + a merged multi-host result is the larger follow-up, still open.
- LT-116 ✅ the "Plan Preview" link was invisible during a running scan (only appeared after a manual reload) → moved into an SSE-swapped block, appears the moment recon finishes. Also renamed the page "Suggested Checks" for clarity (label only).
- LT-117 ✅ the recon Endpoints table was swamped by katana-crawled JS-library internals (`includes/jquery/…`, `*.js.php`) → widened static-asset/vendor-path recognizer (`IsNonRouteAssetPath`) used by both the table collapse and the idor/authbypass/ssrf candidate filters.

## Live Testing — www.aalberts.com (2026-09-09)

First full recon+scan of an owned domain only ever recon'd before. A hardened corporate estate — Cloudflare, M365/SharePoint SSO gateways, catch-all SPA shells, HTTP Basic auth, S3-fronted apex — real scannable surface is 3 of 10 hosts. 17 findings post-dedup, actionable set thin (1 `misconfig-cors` worth a look, ~3 valid missing-header rows); the adaptive throttle correctly aborted the scan mid-run on a source-IP block. **Demo verdict: not a candidate, do not re-evaluate** (see the Demo-target selection block above) — kept as an FP-regression fixture only.

All four FP fixes below ✅ done 2026-09-09 (`fix-webui-plan-preview-and-misconfig-fps`):
- LT-119 `/.well-known/security.txt` flagged as an exposed path, despite RFC 9116 *requiring* it be public → dropped from `ExposedPaths` (whole `/.well-known/` namespace).
- LT-120 `checkDisallowedMethods` treated a uniform 401 (HTTP Basic wall) as "method accepted" → `401`/`407` added to `rejected()` alongside `403`.
- LT-121 `misconfig-cors` fired high/high off an auth-wall-only 401 response → down-ranked to medium/medium with a note, when every observed status on the host is an auth-wall status.
- LT-122 ✅ done 2026-09-09 — the Scan form started blank after every `serve` restart (in-memory job store) → client-side-only `localStorage` prefill of non-secret fields (never auth tokens/headers/the authorized checkbox/`allow_writes`).

**Datapoints for existing items (no new LT):** LT-6's info-severity header-noise case reappeared on `tmo.aalberts.com` (5 separate info rows); LT-26's "generic cloud-provider fact contributes no usable tag" gap reconfirmed (S3/AWS facts on the apex produced no `aws`/`s3` tag that round — since closed by Phase 8 Step 3).

## Scan-Engine Request Efficiency

Design-review follow-on to LT-18, closing the remaining redundant-request waste. Both ✅ done 2026-09-07 (`ph7-step4b`, D5):
- LT-54 the template executor had no cross-request response cache (same URL re-fetched once per template) → `pkg/template/nuclei/respcache.go`, a 512-entry FIFO cache keyed on method+URL+headers+body; carved out: interactsh/timing/path-correlated/multi-payload/non-GET/`raw:` requests always hit the network.
- LT-55 the scan re-probed paths recon already knew were 404 → `ReconResult.DeadPaths()` → `scanner.Config.KnownDeadPaths`, skips a lone single-request matcher-only template on a known-dead path (gated to an unauthenticated scan only).
- **LT-56 (open)** — PlanTree seeding is best-effort and single-case: `ExecOptions.SeedFn` lets a completed leaf's finding fill a still-pending one, but there's no explicit `DependsOn` edge and the extractor only pulls URLs from `Finding.Target`/`Evidence`. **Fix (design, unscheduled — trigger-gated):** explicit `PlanNode.DependsOn []string` + a real two-phase in-class dispatch, plus a broader `Finding→Config` seed extractor. **Trigger:** a real multi-leaf agent run observed dropping a seed that mattered.

## Low-priority / trigger-gated open items (roll-up)

Small residual tails, genuinely open but not worth front-loading. Pick up opportunistically; none is on the critical path.

- **LT-6 tail** ✅ done 2026-09-10 — `reporter.DropSupersededNucleiFindings` is the template-ID skip-list (weak-HSTS's native/nuclei pair); wired into all three finding-producing paths (CLI, webui `exportJSON`, MCP scan-tool output — the latter two previously called neither `Dedup` nor `SplitAggregates` at all). Test: `tests/unit/reporter_supersedednuclei_test.go`.
- **LT-29** — `TestEngineRun_HundredTargetsPerformance` lands ~13s over its 2m doc03 budget, deterministically (misconfig's request fan-out grew since doc03 was written, not an engine regression; CI's default `go test ./...` doesn't run this `integration`-tagged test). Re-baseline to ~2m30s with the floor-math comment, or measure a rate-limit-normalized figure.
- **LT-40 tail (a)** — GraphQL SDL/introspection walking. Deferred until a live target with a real GraphQL endpoint justifies the design work.
- **LT-56** (see Scan-Engine Request Efficiency above).
- **LT-61 tail** — per-resolved-IP ASN classification; a `lookupASN` test seam. Both small; the table itself already ships.
- **`report_intents` create — unconfirmed live** (see Reporting & Integrations below).
- **GitHub Action `scan-action`** (see Reporting & Integrations below).

## Parked — revisit on a trigger or after an eval

Genuinely deferred: real value, not yet actionable. Each entry names what would un-park it.

- **Template signing.** Un-parks when a community repo starts accepting outside template submissions.
- **DOM-based XSS via Chromedp.** Un-parks when a Phase 7/8/9 eval shows reflected-XSS live yield justifies the dependency + sandboxing cost.
- **Playwright/Caido-style richer recon signal.** Largely overlaps Phase 8 Steps 3+6; un-parks once those land and a concrete residual delta is worth sizing.
- **ffuf-style multi-position fuzzing as its own tool.** Reaffirmed out of scope 2026-09-08 — content discovery rides `httpx -path`, param discovery is served by LT-100's diff-oracle, and true multi-position volume doesn't reconcile with the shared rate limiter. Un-parks only if a live engagement shows a surface LT-100+`httpx -path` provably can't reach.
- **Baseline-mode account-provisioning guidance for a real bounty/VDP target.** LT-123 automates the common case; this is narrower operator-facing guidance for the residual manual cases. Un-parks when a real engagement needs it.
- **Self-hosted `interactsh-server`.** The public-server default covers owned-site scanning. Un-parks when a real third-party engagement needs a private OOB server.

## Reporting & Integrations

- **`report_intents` create still unconfirmed live.** `ListWeaknesses`/`ListStructuredScopes` are live-verified; the create-report request-body schema is unexercised against the real API — a deliberately incomplete trial call's `422` body is the fastest confirmation.
- **GitHub Action (`scan-action`).** Thin CI wrapper for continuous self-scanning, a different audience than opportunistic hunting. Not started; parallel track, not blocking any phase.
- **LT-158 (open) — SARIF 2.1.0 exporter.** Every `Exporter` today serializes JSON/CLI/markdown/html only; SARIF is the missing format for GitHub code-scanning / generic CI integration. Raised 2026-09-14 from cross-project research — Strix ships SARIF alongside JSON/markdown as a maturity/interop feature, unrelated to its LLM design. Not started; no dependency on any open item above.

## Testing & Verification Gaps

- **Auth-bypass integration tests** (crAPI/vAPI) ✅ re-verified live 2026-09-10 against a fresh crAPI+vAPI Compose bring-up — `TestAuthBypassAgainstCRAPI`/`…VAPI_API1`/`…VAPI_JWTUser` all pass. [Phase 7](16-implementation-plan-ph7.md) Step 6 (renumbered from "Step 7") — which also now owns the credentialed crAPI recon→plan→scan→export round trip, itself live-verified 2026-09-10 (7 real `idor-*` + 8 real `authbypass-*`/`coverage-gap-*` findings incl. 3 critical alg:none JWT bypasses).
- **`--scope` live verification** against a real authorized target ✅ done 2026-09-10 — `localhost` vs. `127.0.0.1` (same target, different literal hostname) scans/skips correctly per `--scope`. Folded into Phase 7 Step 6.
- **LT-29** (see the roll-up above).
- **✅ done 2026-09-03 — CI coverage gate 79.0%→77.0%→80.0%.** Real coverage had drifted to ~77% unnoticed (an earlier network-flakiness failure was masking it); gate re-closed to 80.0% after new unit tests brought real coverage to 83.5%. Reusable hazard found and fixed: a naive "happy path" test on a dev machine with real recon binaries on `PATH` silently shelled out to them — fixed via `isolateFromInstalledReconBinaries` test helpers (`cmd/hackerfive` + `pkg/mcpserver` + `pkg/webui`).

## See also
- [02-architecture-and-tech-stack.md](02-architecture-and-tech-stack.md) — protocol scope and template-security boundaries referenced above
- [03-development-roadmap.md](03-development-roadmap.md) / [09](09-implementation-plan-ph1a.md) / [10](10-implementation-plan-ph1b.md) / [11](11-implementation-plan-ph2.md) / [13](13-implementation-plan-ph4.md) — implementation detail behind the items above
- [15](15-implementation-plan-ph6.md) / [16](16-implementation-plan-ph7.md) / [17](17-implementation-plan-ph8.md) / [18](18-implementation-plan-ph9.md) — phase plans scheduling the deferred items above; doc17 § "Execution order" is the single cross-phase backlog
- [follow-up-archive.md](follow-up-archive.md) — the unabridged original write-up behind every compressed item above
